# DMS 마일스톤

7인 팀 프로젝트 문서를 디스코드 대신 한 곳에서 관리하기 위한 시스템.
목표: **주말 안에 팀이 실제로 쓰기 시작하는 것.**

---

> 프로젝트 배경과 아키텍처는 `HANDOFF.md`, 코딩 규칙과 함정은 `CLAUDE.md`에 있다.

---

## 확정된 설계 결정

인터뷰로 확정한 것들. 이후 판단이 흔들릴 때 여기로 돌아온다.

| 항목 | 결정 | 이유 |
|---|---|---|
| 문서 단위 | **1문서 = 1파일** | 버전 관리가 명확해짐. 여러 파일은 zip 또는 문서 분리 |
| 접근 제어 | **디스코드 길드 멤버십** | 팀원 초대/제거가 디스코드 하나로 끝남 |
| 권한 | 전원 동등 | 7명 규모에 역할 구분은 과잉 |
| 삭제 | **소프트 삭제(휴지통)** + 휴지통에서 **영구삭제**(2026-08-25 추가) | 아무나 지울 수 있으므로 복구 가능해야 함. 영구삭제는 원래 범위 밖이었으나(`HANDOFF.md` 가 "휴지통 비우기는 범위를 넓히지 말 것"이라 적어 뒀다) 사용자가 요청해 넣었다. **휴지통 문서만 대상**이라 소프트 삭제를 둔 이유는 지켜진다 — 활성 문서를 한 번에 지우는 경로는 만들지 않았다 |
| 상태 라벨 | 없음 | 최신 버전이 곧 최신 상태. 태그로 대체 가능 |
| 업로드 고아 정리 (2026-08-30) | **만든 쪽이 그 자리에서 지운다** — 신설 `POST /api/uploads/discard` 를 취소·생성 실패 때 브라우저가 부른다 | 사후 스캔 방식은 **원리상 불가능하다** — IAM `dms-app` 에 `s3:ListBucket` 이 없어 `ListObjectsV2` 가 403 이다(2026-08-24 실측). 삭제 권한은 `keyToken` 이 전부이고(5분 안에 우리가 발급한 키만), **지울지는 서버가 참조 수로 정한다** — 그 키를 쓰는 버전 행이 있으면 안 지운다. 소프트 삭제 경로는 손대지 않았다(복구가 깨진다). 실측 9/9 — `npm run test:e2e:orphan` |
| `keyToken` 재사용 차단 (2026-08-30) | **DB 유일 제약이 최종 방어선** — `@@unique([s3Key])` + 두 라우트의 사전 조회 | 토큰은 검증돼도 소모되지 않아 5분간 재사용된다. 1회용화는 사용 기록 저장소(스키마 변경)를 부르므로 **이미 있는 사용 기록**(그 키의 `DocumentVersion` 행)을 대신 읽는다. 사전 조회만으로는 동시 두 발을 못 막아서(check-then-act) 제약을 같이 걸었다 — 실측 `201/400`·버전 행 1개. 사전 조회를 남긴 이유는 **S3 왕복 앞에서** 끊기 위해서다. 부작용으로 `s3_key` 에 유일 인덱스가 생겨 그 조회가 seq scan 이 아니게 됐다 |
| 버전 롤백 | **다운로드만** | 되돌릴 일이 드물고, 받아서 재업로드하면 됨 |
| 첫 화면 | 전체 문서 목록(최근 수정순) | "어디 있는지 못 찾겠다"에 가장 직접적인 답 |
| 일괄 업로드 | **필요** | 디스코드의 기존 문서를 한 번에 이전해야 함 |
| 문서 상세 | **별도 페이지** (`/documents/[id]`) | URL이 생겨 디스코드에 링크 공유 가능. 미리보기+버전이력을 담을 공간 확보 |
| 컴포넌트 | **shadcn/ui** (2026-08-26 뒤집음, 원래는 직접 작성) | 원래 근거는 "필요한 건 모달·진행바 정도 + Tailwind 4·Next 16 에서 shadcn 초기 설정 리스크"였는데, **필요한 것이 모달·진행바로 안 끝났다** — M3·M4 를 거치며 파괴적 확인 3곳·`window.prompt` 2곳·`window.alert` 7곳이 쌓였고 브라우저 기본 다이얼로그로는 길이 검증도 `aria-live` 도 못 붙인다. 초기 설정 리스크는 실제로 겪어 보니 없었다. **Vercel 은 컴포넌트 라이브러리를 배포하지 않는다** — `@vercel/geist-ui` 는 npm 에 없고(404), `geist` 패키지는 폰트뿐이며 `@geist-ui/core` 는 2022년이 마지막이고 레포도 아카이브됐다. Nova 프리셋(Lucide + Geist)이 이 리포 구성과 그대로 맞는다 |
| 시각 언어 | **무채색 토큰 + Geist 폴백 체인** (2026-08-26) | 회색에서 파란기를 뺐다(옛 `#101828`·`#667085` 은 slate 계열). 주 액션색은 indigo → 검정. `globals.css` 의 `@theme` 이 `--color-*` 를 정의하면 Tailwind 가 유틸리티를 다시 만들어 주므로 **토큰 교체만으로 화면 전체가 바뀐다 — 컴포넌트는 한 줄도 안 고쳤다.** Geist 는 교체가 아니라 **폴백 체인**이다 — 한글 글리프가 없어서 교체하면 한글이 시스템 폰트로 떨어진다. 체인이면 브라우저가 글자 단위로 폴백해 한글은 Noto Sans KR, 숫자·영문만 Geist 가 받는다 |
| 확인·알림 UI | **파괴적 확인 3곳은 AlertDialog, 알림 7곳은 sonner 토스트** (2026-08-26) | 문서 삭제·영구삭제·폴더 삭제만 확인을 받는다. **업로드 취소 확인 2곳은 `window.confirm` 그대로 둔다** — 이미 열려 있는 모달 안이라 모달 위 모달이 되고, 데이터를 지우는 동작도 아니다. 폴더 생성·이름변경의 `window.prompt` 2곳은 Dialog 폼으로 — `prompt` 는 길이 검증을 못 붙여 서버가 100자로 튕길 때까지 알 방법이 없었다 |
| 미리보기 | **PDF·이미지·html·xlsx** (2026-08-28 뒤집음, 원래는 PDF·이미지만) | 원래 근거는 "docx/xlsx는 **서버 변환**이 필요해 주말 범위 밖"이었는데 **그 전제가 성립하지 않는다** — 서버 변환이 아니라 **브라우저 파싱**으로 간다. 파일은 여전히 앱 서버를 안 거친다(불변식 유지). 사람이 요청했다("앞으로 xlsx 업로드가 많아질 것 같다"). **실데이터가 결정적이었다** — 활성 문서 29건 중 html 16 · xlsx 10 으로 **26건(90%)이 미리보기가 없고**, 되는 3건(pdf 2·png 1)은 전부 `dms-preview-test.*` 테스트 산출물이다. **html 은 서버·클라이언트 코드가 이미 다 있다** — iframe 이 이미 S3 로 307 되고 `Content-Type: text/html` 이면 브라우저가 렌더하므로 `previewKind` 한 줄이다. `sandbox` 는 걸지 않는다 — 실행 오리진이 `*.amazonaws.com` 이라 앱 쿠키·DOM 에 닿을 수 없고(SVG 를 수용한 것과 같은 논거), 팀 화면설계서가 인터랙티브라 스크립트를 막으면 정적 껍데기가 된다. **docx 는 여전히 범위 밖이다**. **xlsx 는 2026-08-28 완료** — 서식은 값·병합·열너비·채우기색·글꼴·테두리까지 재현하고 조건부서식·차트·이미지·틀고정은 뺐다. 날짜는 3종 서식을 재현하지 않고 `YYYY-MM-DD` 로 통일한다. **착수 전 기록이 실측으로 뒤집힌 곳이 하나 있다** — "채우기색이 argb 가 아니라 theme+tint 라 팔레트를 하드코딩하면 절반이 틀린다"고 적혀 있었으나, 전수 실측은 채우기의 **95%가 argb** 이고 theme 채우기를 쓰는 5건이 **전부 같은 2013 팔레트**임을 보였다. `_themes` 파싱은 그래도 넣되 읽기 실패 시 기본 팔레트로 폴백한다. 근거는 `HANDOFF.md` "다음 작업" 3번. **운영 실측 완료** (2026-08-28, `107e558` 배포) — xlsx 10건 전수 정상, CORS 오류 0 |
| 글자·모서리 토큰 | **Vercel Geist 스케일에 맞춘다** (2026-08-28) | 색은 이미 Geist 계열이었는데(`#171717`·`#ebebeb`·`#fafafa`) 글자와 모서리만 Tailwind 기본값이라 갈려 있었다. **모서리는 Geist 가 "4px·6px·9999px 이외를 쓰지 말라"고 못박는다** — 계단식 `calc(var(--radius) * n)` 이 만들던 7종(4·4.8·6.4·8·11.2·14.4·full)을 4px(배지·칩)과 6px(카드·인풋·버튼)로 접었다. **글자는 `text-xl` 이 이름만 같고 20px 대 24px 로 갈리던 것**을 24px + 자간 -0.04em 으로 맞췄고(Geist 는 크기가 커질수록 자간이 음수로 강해진다), 스케일 밖 하드코딩 3종(`[10px]`·`[11px]`·`[0.8rem]`)을 `text-xs`(12px, Geist 최소)로 흡수했다. **굵기는 Geist 가 400·500·600 셋뿐**이라 `font-bold`(700) 18곳을 `font-semibold`(600)로 내렸다. 출처는 공식 문서가 아니라 커뮤니티 정리본이라 확정값은 아니다 |
| 재업로드 시 제목 | **자동 생성된 제목만 새 파일명을 따라간다** (2026-08-28) | 원래는 `Document`를 아예 안 건드렸는데, v2 를 올려도 제목이 v1 파일명인 채로 남는 것이 실측에서 걸렸다. **항상 덮는 안은 버렸다** — 최초 제목은 파일명에서 자동 생성되지만 그 뒤 `document-meta-editor` 로 사람이 고칠 수 있고, 고친 제목을 재업로드가 지우면 되돌릴 수단이 없다. 그래서 "현재 제목 == 이전 파일명에서 자동 생성한 값"일 때만 따라간다. 판정은 순수 함수 `retitleOnReupload`(`src/lib/title.ts`)에 있고 클라이언트·서버가 같은 규칙을 공유한다 |
| 모바일 | 보기만 대응 | 업로드 등은 PC 기준 |
| 파일 업로드 경로 | 브라우저 → S3 직접(presigned) | 파일이 앱 서버를 안 거쳐 저사양 EC2도 무관 |
| 마일스톤 순서 | **M4 → M6 → M5** (2026-08-24) | 제품 존재이유 3개 중 미해결로 남은 하나("어디 있는지 못 찾는다")가 M4 다. 반대 논거(배포가 채택과 알림 실측을 동시에 사는 가장 싼 순간)는 `HANDOFF.md` "다음 작업"에 기록해 뒀다. **7명 규모에서 검색이 실제로 필요한지는 측정되지 않았다** |
| 배포처 | **Vercel** (2026-08-25 변경, 원래는 EC2) | `hymn.pem` 이 개발 PC 에 없어 EC2 에 접근할 수 없다. 그리고 DB 를 Neon 으로 옮기면서 **앱이 EC2 안에 있어야 할 이유(VPC 안의 RDS)가 사라졌다.** RDS 에 이어서 쓸 DMS 데이터도 없다(확인). M6 항목 5개 중 3개(탄력적 IP·Nginx·Let's Encrypt)가 없어지고 hymn 과 자원을 다투지 않는다. **Hobby 무료 티어는 비상업적 용도 한정** — 이 프로젝트는 기간 한정 비상업이라 조건을 만족한다. 상업적 용도가 되면 이 결정은 깨진다 |
| 개발/운영 DB | **같은 Neon 프로젝트, 다른 브랜치** (2026-08-26) — 운영 `production` · 개발 `dev` | 원래는 한 DB 였다 (배포처를 Vercel 로 바꾸면서 딸려온 결정이고 분리 여부는 검토되지 않았다). **비용이 실제로 드러나서 나눴다** — `db push` 가 팀이 쓰는 DB 로 직행하고, 개발 중 테스트 업로드가 팀 문서 목록에 섞였다(문서 15건 중 8건이 휴지통, 상당수가 테스트). 영구삭제가 있는 앱이라 개발 실수가 팀 문서에 닿는다. **프로젝트가 아니라 브랜치로 나눈 이유**는 리전·요금·설정을 두 번 관리하지 않기 위해서다. **대가: `prisma/migrations` 가 없어서 스키마를 두 번 밀어야 한다** — dev 확인 후 운영. 빼먹으면 배포된 앱만 런타임에 죽고 타입 검사·빌드는 못 잡는다. 절차는 `SETUP.md` 1-2 절. `prisma migrate` 도입은 범위를 넓히는 일이라 안 한다. **S3 는 안 나뉜다**(버킷 하나) |
| 함수 리전 | **`icn1` 서울** (2026-08-25) · `vercel.json` 에 고정 | **자원이 갈려 있다** — 사용자와 S3(`ap-northeast-2`)는 서울, Neon 은 싱가포르라 어느 쪽을 골라도 하나는 멀다. 사용자↔함수 왕복은 요청당 **한 번**이지만 함수↔DB 는 **쿼리 깊이만큼** 곱해지므로, 원래는 DB 옆(`sin1`)이 정석이다. 그런데 목록 페이지의 실제 깊이가 3 이 아니라 **1~2** 였고(layout∥page), 업로드 마무리는 `headObjectSize` 때문에 S3 옆이 유리해 서울이 근소하게 앞선다. **순차 DB 쿼리 깊이가 3 을 넘으면 `sin1` 로 돌아올 것** — 계산표는 `HANDOFF.md` "배포 성능 실측" 절. **Neon 리전은 선택지가 없다** (서울 미지원 + 기존 프로젝트 변경 불가) |
| 업로드 자동 분류 | **파일명 규칙 기반 + 폴더별 별칭** (2026-08-26) | 파일이 앱 서버를 안 거쳐 **내용은 볼 수 없다** — 쓸 수 있는 신호는 파일명뿐이다. 별칭은 별도 표가 아니라 `Folder.aliases` 로 **폴더에 붙인다** — 독립 표는 관리 UI·고아 별칭·폴더 삭제 시 정리 세 가지가 딸려오는데, 폴더에 붙이면 폴더를 지울 때 같이 사라지고 편집 자리(폴더 생성·이름변경 Dialog)도 이미 있다. **정규화가 별칭보다 먼저다** — 구분자·유니코드·버전/날짜 토큰은 규칙이 처리하고, 별칭은 정규화로 안 되는 **의미 차이**(와이어프레임 = 화면설계서)에만 쓴다. 순서를 바꾸면 별칭 표가 정규화로 풀릴 것들로 금방 지저분해진다 |
| 자동 분류의 실패 | **미분류로 보낸다. 추측해서 넣지 않는다** (2026-08-26) | 애매하면 안 넣는 것이 파일명 분류 도구들의 일관된 원칙이다. "미분류"는 이 앱에 이미 있으므로 새로 만들 게 없다. **분류 실패는 에러가 아니라 정상 결과다** |
| 자동 분류 깊이 | **2뎁스 — `카테고리 > 하위`** (2026-08-31) | 1뎁스로는 폴더가 목록을 못 좁힌다 — dev 실측에서 `화면설계서` 한 폴더에 11건이 쌓였다. **하위 이름은 매칭이 1차, 추출이 폴백이다** — 카테고리를 정한 뒤 그 폴더의 **자식들만** 후보로 다시 매칭하고, 붙는 게 없을 때만 파일명에서 뽑는다. 이 순서가 제품명 접두사(`냥멍케어`) 문제를 공짜로 푼다: 첫 1건만 `냥멍케어 건강기록` 으로 제안되고 사람이 `건강기록` 으로 고치면 이후는 전부 매칭으로 붙는다. **제외어(stopword) 목록은 만들지 않는다** — 별칭 표를 안 만든 것과 같은 이유(관리 UI·고아 항목이 딸려온다)이고, 실측에서 걸리는 건 3건뿐이다. **하위 이름이 여러 토큰이면 쪼개지 않는다**(`로그인 회원가입`) — 1문서=1폴더라 한 문서를 두 폴더에 못 넣는다 |
| 새 카테고리 제안 | **제안은 1뎁스까지만** (2026-08-31) | 카테고리가 안 붙는 파일에서 2뎁스를 만들려면 어느 토큰이 카테고리인지 정해야 하는데 **파일명 안에 근거가 없다** — `02_IA 구조도` 가 `IA > 구조도` 인지 `IA 구조도` 인지 못 가른다. §'실측에서 드러난 것'이 이미 *"원리상 못 가른다 — 정규식 문제가 아니다"* 로 결론낸 벽이다. 틀렸을 때 피해도 2배다(잘못된 부모 + 그 안에 숨은 잘못된 자식). **잃는 것은 없다** — 새 카테고리는 첫 파일 1건에서만 생기고, 그 폴더가 선 다음부터는 카테고리 매칭이 되므로 2뎁스를 정상적으로 받는다. 2뎁스를 포기하는 게 아니라 한 세대 늦추는 것이다 |
| 기존 문서 소급 이동 | **한다 — 일회성 스크립트 + dry-run + 롤백 스냅샷** (2026-08-31, 사람이 요청해 뒤집음) | 원래 §'하지 않는 것'이 *"업로드하는 순간에만 분류한다"* 였다. **2뎁스에서는 그 결정이 성립하지 않는다** — 이미 쌓인 26건이 1뎁스로 남으면 새 규칙이 신규 업로드에만 걸려 폴더 구조가 두 세대로 갈라진다. **UI 버튼이 아니라 스크립트인 이유**: 26건짜리 1회 작업이고, UI 는 만든 뒤 영구히 유지·검증 대상이 된다. **롤백 스냅샷은 필수** — 없으면 잘못 옮겼을 때 26건을 손으로 되돌린다. 운영 DB 는 팀 7명이 실제로 보는 데이터라 **dev 확인 후 사람이 직접 실행한다** |
| judge 예산 상한 | **$5 유지** (2026-08-24) | 실측 2점($4.72 통과 / $5.08 사망)으로 기본값을 올리지 않는다. 초과해도 사인이 남고 산출물이 파킹되므로 되돌릴 수 있다. 필요시 `BUDGET_JUDGE=8` 로 그때그때 |
| 카테고리 폴더 본문 | **문서 표 안에 자식 폴더 행을 먼저, 그 아래 직계 문서** (2026-09-05 결정 · 2026-09-06 모양 정정) | 2뎁스를 넣으면서 `folderFilterWhere` 의 완전일치가 옛 전제("폴더 = 그 안의 문서 전부")에 남아 카테고리가 빈 화면이 됐다. 대안은 **자손 문서 합산**이었는데 `화면설계서` 를 열면 16건이 그대로 나와 폴더가 목록을 좁힌다는 2뎁스의 목적이 반쯤 죽는다. 그래서 목록 쿼리는 **한 글자도 안 바꾸고**(직계 완전일치 유지) 본문에 자식 폴더 목록을 새로 얹었다. 폴더별 문서 수는 폴더 조회에 `_count` 를 얹어 **DB 왕복을 늘리지 않는다**. 제목은 동명 하위 폴더(`화면설계서 > 마이페이지` · `기능명세서 > 마이페이지`)를 가리려고 브레드크럼이 됐다. **모양은 화면을 보고 한 번 뒤집었다** — 처음엔 별도 카드 격자였는데 테두리가 둘로 갈려 한 목록으로 안 읽혔다. `DocumentTable` 에 optional `folders` prop 을 얹어 같은 표 안에 넣고, 폴더에 없는 값(올린 사람·크기·수정일)은 `colSpan` 한 칸으로 합쳤다. 제목·브레드크럼은 크기를 안 섞는다(전 화면 14px) |

명시적으로 안 물어보고 정한 것 (이견 있으면 알려줄 것):
- 파일 크기 상한 **100MB**
- 확장자 제한 **없음** (팀 내부 도구)
- 검색 범위 **제목·설명·태그**. 파일 *내용* 검색은 범위 밖

---

## M0 — 기반 · ✅ 완료

- Next.js 16 + TypeScript + Tailwind 4 스캐폴딩
- Prisma 7 스키마 6개 모델 (`User` `Folder` `Document` `DocumentVersion` `Tag` `DocumentTag`)
- 디스코드 OAuth 로그인 + 길드 멤버 검증
- jose JWT 쿠키 세션 · 전 구간 보호 미들웨어
- S3 presigned 업로드/다운로드 유틸
- 환경변수 zod 검증

> 로그인 **로직**은 있으나 `/login` **페이지가 없어** 현재는 실행 시 404.

## M1 — 로그인이 실제로 동작한다 · ✅ 완료 (2026-08-21)

- [x] EC2 SSH 터널로 RDS 연결 (`dms` DB 생성) + `db:push`로 스키마 반영
- [x] `/login` 페이지 (디스코드 버튼, `?error=` 메시지 표시)
- [x] 앱 셸: 상단바(로고·검색창·아바타·로그아웃) + 좌측 사이드바 자리
- [x] `/` 를 기본 템플릿에서 문서 목록으로 교체 (DB 연결 후 데이터 표시)

**완료 기준** — 디스코드로 로그인해 빈 목록 화면까지 도달하고, 팀 서버 멤버가 아닌 계정은 거부된다.

> 2026-08-21 실측: 로그인 성공 후 `users` 테이블에 행 생성 확인.
> 멤버가 아닌 계정의 거부는 **아직 미검증** (다른 계정으로 시도해봐야 한다).
> 로컬 포트는 3000이 아니라 **3002**다 (neemba 컨테이너가 3000 점유 — `SETUP.md` 참조).

## M2 — 업로드 + 목록 · ✅ 완료 (2026-08-21) ★ 여기까지가 진짜 최소 제품

- [x] 스키마에 `Document.deletedAt` 추가
- [x] `POST /api/documents/presign` — S3 키 발급 + 업로드 서명 URL
- [x] `POST /api/documents` — 업로드 완료 후 메타데이터 저장(v1 생성)
- [x] **드래그&드롭 다중 파일 업로드** 모달 + 파일별 진행률
- [x] 문서 목록 테이블 (제목 · 폴더 · 최종 수정 · 올린 사람 · 크기), 최근 수정순
- [x] 다운로드 (presigned GET, 원본 파일명 유지)

**완료 기준** — 디스코드에 흩어진 문서를 한 번에 끌어다 올리고, 목록에서 찾아 내려받을 수 있다.

> 2026-08-21 실측: xlsx 22,422바이트 1건을 브라우저에서 올려 S3 객체와
> `documents`/`document_versions` 행(`versionNo=1`)이 일치하는 것까지 확인,
> 다운로드도 성공. 실제 자격증명으로 왕복이 검증됐다.
>
> **여러 파일 동시 업로드는 확인됐다** (2026-08-26, 3개를 한 번에 끌어다 놓음).
> **길드 비멤버 거부도 확인됐다** (2026-08-25, 부계정 실측 — M6 절).
> **아직 검증 안 된 것**: 100MB 상한 근처의 큰 파일.

**이 시점부터 팀에 공유해도 된다.** 이후 마일스톤은 전부 "불편함 줄이기".

## M3 — 문서 상세 + 버전 이력 · 약 3시간

- [x] `/documents/[id]` 상세 페이지
- [x] 재업로드 → v2, v3 누적 (변경 메모 입력)
- [x] 버전 타임라인 + 버전별 다운로드
- [x] 제목·설명 수정
- [x] 소프트 삭제 · 휴지통 · 복구

**완료 기준** — 같은 문서를 여러 번 올려도 이력이 남고, 실수로 지워도 되살릴 수 있다.

> 소프트 삭제·휴지통·복구는 2026-08-22 구현 (직렬 에이전트 파이프라인 첫 주행).
> 스키마 변경 없음 — `deletedAt`이 M2에서 이미 들어가 있었다.
> `npm test`(18) · `lint` · `build` 통과.
>
> 2026-08-22 브라우저 실측: 삭제 → 휴지통 표시 → 복구 → 목록 복귀, 빈 휴지통 안내까지 확인.
> 삭제 상태에서 `/api/documents/<id>/download` 직접 접근이 404(`문서를 찾을 수 없습니다.`)로
> 막히고, 복구 후 같은 URL로는 파일이 내려오는 것까지 대조 확인했다.
>
> 아직 안 본 것: 복구 버튼 연타(삭제 버튼은 `window.confirm`이 UI를 막아 연타 자체가
> 불가능하다).

> **위 4항목은 2026-08-24 구현 (파이프라인 `document-detail` 2차 주행) + 같은 날 브라우저 실측 완료.**
> "코드 완성"과 "검증됨"을 나눠 적는 것이 이 리포의 규칙인데, 이번엔 둘 다 채웠다.
> 스키마 변경·패키지 추가 없음. `npm test`(97) · `lint` · `build` 통과, 빌드 출력에
> `ƒ /documents/[id]` · `ƒ /api/documents/[id]/versions` 등록 확인 (2026-08-24 실측).
> `/code-review high` 7건 중 문서 4건·다이얼로그 1건을 수정했고, `keyToken` 재사용은
> 기존 결정대로 고아 객체 정리 때 함께 막는다 (`HANDOFF.md` 미룬 표).
>
> **브라우저 실측 9/9 통과** (2026-08-24, Playwright · `npm run test:e2e`). `DESIGN.md` §7.2 의
> B1·B3·B4·B5·B6·B7·B8·B9 + V3. 판단검증이 미확인으로 남겼던 셋이 여기서 해소됐다 —
> **V3(중첩 create 도 부모 `@updatedAt` 을 갱신한다. 폴백 불필요)**, `hour12: false` 필요성,
> P2025 → 404 경로. 근거는 `HANDOFF.md` "현재 위치".
>
> **B10·B11 도 확인됐다** (2026-08-26). B10(재업로드 중 취소)은 e2e 가 안 덮는
> 타이밍 의존 항목이라 수동으로 했고, **취소 창은 DevTools 의 Slow 4G 스로틀링 +
> 업로드 지연 10초 임시 코드를 겹쳐서 만들었다** — 둘 중 하나만으로는 창이 안 열린다.
> B11(디스코드 임베드)은 `DISCORD_WEBHOOK_URL` 을 채워 재배포하면서 풀렸다 (M5 절).

## M4 — 폴더 + 태그 + 검색 · ✅ 완료 (2026-08-25)

- [x] 폴더 생성/이름변경/삭제, 사이드바 트리
- [x] 문서를 폴더로 이동
- [x] 태그 부여 + 태그 필터
- [x] 검색 (제목·설명·태그, PostgreSQL)

**완료 기준** — 폴더·태그·검색 세 경로 중 아무거나로 원하는 문서에 도달한다.

> **자리만 잡아둔 UI 두 개를 여기서 같이 살린다.** 둘 다 지금은 `disabled` 다.
> - 헤더 검색창 (`app-header.tsx`) — `/search` 라우트를 만들면서 `disabled` 를 푼다.
>   원래 `<form action="/search">` 로 살아 있었는데 라우트가 없어 엔터 한 번에 404 였다
>   (2026-08-23 `ee7f11d` 에서 막음).
> - 사이드바 "새 폴더" 버튼 (`app-sidebar.tsx`) — 처음부터 `disabled` 였다.
>
> 폴더는 **읽기 경로가 이미 반쯤 있다**: `page.tsx` 가 `doc.folder?.name` 을 표시하고
> `api/documents/route.ts` 가 `folderId` 를 받는다. 만들고 배정하는 UI 만 없어 항상 `—` 다.

> **2026-08-25 구현 (파이프라인 `m4` 주행) + 같은 날 브라우저 실측 완료.**
> 스키마 변경·패키지 추가 없음 — `Folder`·`Tag`·`DocumentTag` 는 M0 에 이미 있었다.
> `npm test`(97 → **164**) · `lint` · `build` 통과, 판정은 셸이 했다. 빌드 출력에
> `ƒ /search` · `ƒ /api/folders` · `ƒ /api/folders/[id]` · `ƒ /api/documents/[id]/tags`
> 등록 확인.
>
> 로직을 `src/lib/` 순수 함수로 뺐다 (`folder.ts`·`tag.ts`·`search.ts`). jsdom 이 없어
> 컴포넌트 렌더 테스트를 못 쓰므로 **검증 가능한 쪽으로 구조를 민 것**이다.
> `disabled` 두 개(헤더 검색창·사이드바 "새 폴더")도 예정대로 살렸다.
>
> **브라우저 실측 11/11 통과** (2026-08-25, 수동). `DESIGN.md` §5 의 B1~B11.
> B11(DB 끊김 시 레이아웃 생존)은 터널이 아니라 `DATABASE_URL` 을 못 닿는 주소로 준 채
> dev 를 띄워 재현했다 — `.env` 만 고치고 새로고침하는 것으로는 재현되지 않는다.
> `src/lib/prisma.ts` 가 클라이언트를 `globalThis` 에 붙여 두어 연결 문자열이 최초
> import 때 한 번만 읽히기 때문이다. **프로세스를 다시 띄워야 바뀐다.**
>
> **파이프라인이 놓친 것 하나를 손으로 고쳤다** (`4dc0e00`). 폴더를 열어 둔 채
> 업로드하면 문서가 미분류로 떨어졌다 — 서버는 처음부터 `folderId` 를 받는데
> (`api/documents/route.ts:14`) 업로드 다이얼로그가 안 보냈다. `DESIGN.md` §6 이
> 업로드 다이얼로그를 범위 밖으로 뒀기 때문인데, **M4 가 폴더 필터 화면을 만들면서
> "폴더 안에서 업로드"라는 맥락이 처음 생긴 것**을 설계가 못 봤다. 설계 위반이 아니라
> 설계의 사각이다.
>
> **실측 환경이 RDS 가 아니라 Neon 이다.** 개발 PC 에 `hymn.pem` 이 없어 SSH 터널을
> 못 열었고, `SETUP.md` 가 대안으로 적어 둔 Neon 무료 티어로 갈아탔다. 즉 위 실측은
> 팀 RDS 데이터가 아니라 빈 DB 에 직접 만든 데이터 위에서 이뤄졌다.
> **RDS 로 되돌리지 않기로 했다** — 배포처를 Vercel 로 바꾸면서 개발도 운영도 같은
> Neon 을 쓴다 (위 "확정된 설계 결정" 표, 아래 M6).
>
> **아직 안 본 것**: 영문 태그의 대소문자 정책. 한 문서 안에서는 `Plan`/`plan` 이
> 합쳐지는데(정규화) 필터는 완전일치라 문서 간에는 갈린다. 설계가 의도한 수용이지만
> (`DESIGN.md` §4) 영문 태그를 실제로 쓰기 시작하면 헷갈릴 수 있다.

> **업로드 폴더는 그 뒤 셀렉트가 됐다** (2026-08-26, `5da27b4`). `4dc0e00` 은 열어 둔
> 폴더에 **조용히** 배정하고 "X 폴더에 업로드됩니다" 안내만 띄웠는데, 그 자리를 셀렉트로
> 바꿔 미분류로도 보낼 수 있게 했다. 폴더 목록은 목록 페이지가 넘기고 `activeFolder`
> 조회와 `Promise.all` 로 묶었다 — 순차로 두면 DB 왕복 깊이가 하나 늘고 함수 리전이
> 서울이라 그 한 번이 95ms 다 (아래 "확정된 설계 결정"의 함수 리전 항목과 같은 계산표).
> **파일을 담은 뒤에는 셀렉트를 잠근다** — `addFiles` 가 드롭 즉시 업로드를 시작하고
> 워커 루프가 그 시점의 클로저를 쥐고 있어서, 중간에 바꾸면 한 배치가 두 폴더로 갈라진다.

## UI 정비 · ✅ 완료 (2026-08-26) — 마일스톤 외

마일스톤에 없던 작업이다. M1~M4 를 거치며 쌓인 브라우저 기본 다이얼로그와
임시 색 토큰을 한 번에 걷었다. **기능은 늘지 않았다** — 설계 결정 3건이 바뀐 것이
본체이므로 근거는 위 "확정된 설계 결정" 표의 컴포넌트 · 시각 언어 · 확인·알림 UI 항목에 있다.

- [x] 색 토큰 무채색화 + Geist 폴백 체인 (`15d8c7e`)
- [x] 업로드 시 저장 폴더 선택 (`5da27b4`)
- [x] shadcn/ui 도입, `window.alert` 7곳 → sonner, 파괴적 확인 3곳 → AlertDialog,
      `window.prompt` 2곳 → Dialog 폼 (`b2a6ee6`)
- [x] 파괴적 버튼(`폴더 삭제`·`영구삭제`)의 빨강 복구 — `asChild` className 병합 (2026-08-27)

> **패키지가 늘었다** — 이 리포에서 패키지 추가는 M0 이후 처음이다.
> `shadcn` · `radix-ui` · `sonner` · `class-variance-authority` · `clsx` ·
> `tailwind-merge` · `tw-animate-css` · `geist`. `next-themes` 는 **일부러 뺐다** —
> 이 앱은 라이트 단일이라 `ThemeProvider` 가 없고, 그러면 OS 가 다크일 때 토스트만
> 검게 뜬다. `sonner.tsx` 의 `useTheme` 을 걷고 `theme` 을 `light` 로 고정했다.
>
> **`shadcn init` 이 건드린 것 두 가지를 되돌렸다.** 되돌리지 않으면 조용히 깨진다.
> - `layout.tsx` 에 `next/font/google` 의 Geist 를 넣고 `--font-sans` 를 그 값으로
>   잡았다 — `geist` 패키지의 것과 중복이고, **우리 폴백 체인을 덮어 한글이
>   Noto Sans KR 로 못 떨어진다.**
> - `globals.css` 의 `@theme inline` 도 `--font-sans` 를 자기 값으로 덮었다.
>
> **shadcn 의 `:root` 색 변수는 `@theme` 토큰을 가리키게 묶었다.** 색을 두 군데서
> 관리하면 반드시 갈라진다. `--radius` 는 **0.5rem 으로 둔다** — `@theme inline` 이
> `--radius-lg` 를 덮으므로 shadcn 기본값 0.625rem 이면 기존 `rounded-lg` 가 전부 바뀐다.
>
> **브라우저 실측에서 색이 깨져 있었다 — 고쳤다** (2026-08-26). `npm run build` 는
> 통과했는데 화면이 깨졌다. **`@theme` 과 shadcn 의 `@theme inline` 이 같은 이름 두 개를
> 서로 다른 뜻으로 정의하고 있었다.**
>
> | 이름 | `@theme` (이 프로젝트) | `@theme inline` (shadcn) | 결과 |
> |---|---|---|---|
> | `--color-border` | `#ebebeb` | `var(--border)` — 그런데 `:root` 가 `--border: var(--color-border)` | **순환 참조**. 무효값이라 `border-border` 가 currentColor 로 떨어지고, `@layer base` 의 `* { @apply border-border }` 때문에 **화면 전체 테두리가 잉크색**이 된다 |
> | `--color-accent` | `#171717` (주 액션색) | `var(--accent)` = `--color-accent-soft` = `#f5f5f5` (연한 hover 배경) | `--primary: var(--color-accent)` 까지 `#f5f5f5` 로 떨어져 **버튼이 흰 배경에 흰 글씨**가 된다 |
>
> **커밋이 `--radius`·`--font-sans` 충돌은 잡았는데 색 두 개를 놓쳤다.** 같은 종류의 함정을
> 두 개는 보고 두 개는 못 본 것이라, `shadcn add` 로 컴포넌트를 새로 받을 때마다
> `@theme inline` 블록을 통째로 대조해야 한다.
>
> 고친 방법: `@theme inline` 에서 그 두 줄을 지워 **이름은 `@theme` 것이 이기게** 했다.
> shadcn 쪽 의미가 필요한 자리는 `select.tsx` 한 줄뿐이라 값이 똑같은 `secondary` 로
> 옮겼다 (`--secondary`·`--secondary-foreground` 가 shadcn 의 accent 의도값과 동일).
> `npm run build` · `lint` · `npm test`(**164 → 169**) 통과.
>
> **배포본에도 나가 있었다** — `https://nyangmeong-care-dms.vercel.app` 의 CSS 청크에서
> `--color-accent:var(--accent)` 를 직접 확인했다. **빌드 통과가 화면 통과가 아니다** —
> CSS 변수 충돌은 타입 검사도 린트도 못 잡고 빌드도 조용히 성공한다.
>
> **동작 실측 8종 완료 (2026-08-27).** 토스트 3경로(별칭 길이 초과·이름 중복 409·
> 삭제 실패 500) · Dialog 폼 2곳(새 폴더·이름 바꾸기, 별칭 프리필 정상) ·
> AlertDialog 3곳이 전부 뜨고 동작한다. 화면은 `test/e2e/shots/UI*.png`.
> **토스트 겹침은 문제 아님으로 닫았다** — 2개가 동시에 뜨면 sonner 가 뒤엣것을 접지만,
> 사람이 보고 받아들일 만하다고 판단했다.
>
> **발견 ① — 파괴적 버튼의 빨강이 죽어 있었다. ✅ 고쳤다 (2026-08-27).**
> `폴더 삭제`·`영구삭제` 두 곳이 코드로는 `bg-danger`(`#c50000`) 인데 화면에는
> 검정(`rgb(23,23,23)`)으로 나왔다. `alert-dialog.tsx:158-161` 의
> `<Button variant="default" asChild>` 가 Radix Slot 으로 className 을 **문자열로
> 이어붙이기만** 하고 tailwind-merge 를 안 거쳐서, `bg-primary` 와 `bg-danger` 가 둘 다
> 남고 CSS 생성 순서로 `bg-primary` 가 이겼다.
> **위 "색이 깨져 있었다" 와 같은 계열이다** — 빌드도 린트도 못 잡고, 되돌릴 수 없는 동작
> 두 개가 일반 버튼과 구분이 안 됐다.
>
> `className` 을 Action 이 아니라 **Button 에 넘겼다** — Button 이 그것을
> `buttonVariants({ variant, size, className })` 에 넣어 `cn`(twMerge)이 충돌 클래스를
> 떨어뜨린다. `AlertDialogCancel` 도 같은 모양이라 **같이 고쳤다** (지금은 `variant="outline"`
> 이라 증상이 안 났을 뿐 구조가 같다).
>
> **대조로 판정했다** — `test/e2e/danger-color.mjs` (`npm run test:e2e:danger`) 가
> `getComputedStyle` 로 두 버튼의 배경을 읽는다. 고친 뒤 `rgb(197,0,0)`, `git stash` 로
> 되돌리면 `rgb(23,23,23)` 이 나온다. **되돌린 쪽을 실제로 돌려 본 것이 핵심이다** —
> 안 그러면 하네스가 그냥 통과하는 것인지 구분이 안 된다. `취소`(outline) 는 흰색
> 그대로인 것도 같이 봤다(빨강을 전면 도포한 게 아니라는 대조).
> 이 결함 계열은 `npm test`·`lint`·`build` 가 **원리상 못 잡는다** — 셋 다 통과했었다.
>
> **`shadcn add` 로 받은 컴포넌트는 `asChild` 의 className 병합도 대조해야 한다**—
> `@theme inline` 충돌(위 "색이 깨져 있었다")에 이어 두 번째다.
>
> **`window.confirm` 2곳이 남아 있는 것은 의도다** (2026-08-27 재확인). 자동 분류 #5 실측 때
> Playwright 가 이 창을 기본값(dismiss)으로 닫아 취소가 안 먹으면서 눈에 띄었고 처음엔
> "안 걷힌 자리"로 봤는데, **위 "확정된 설계 결정" 표의 확인·알림 UI 행에 근거가 이미
> 적혀 있다** — 이미 열려 있는 모달 안이라 모달 위 모달이 되고, 데이터를 지우는 동작도
> 아니다. `upload-dialog.tsx:278` · `version-upload-dialog.tsx:45`. 실측이 그 결정을
> 뒤집을 근거를 만들지 못했으므로 **그대로 둔다.**
>
> **다만 자동화에는 대가가 있다.** Playwright 는 대화상자를 기본값으로 dismiss 하므로
> `page.on('dialog', (d) => d.accept())` 가 없으면 **취소 경로가 조용히 안 돈다.**
> 업로드 취소를 스크립트로 검증할 때는 반드시 붙일 것.
> `ink-subtle` 대비가 3.23:1 로 본문 기준(4.5) 미달로 남아 있다 — 옛 값 2.58:1 보다는
> 나아졌고, `#767676` 이면 4.54:1 이지만 `ink-muted` 와 가까워져 3단 위계가 무너진다.
> 트레이드오프라 그대로 뒀다.

## 업로드 자동 분류 · ✅ 완료 (구현 2026-08-26 · 브라우저 실측 2026-08-27)

업로드할 때 폴더를 손으로 고르는 대신 **파일명으로 폴더를 정한다.**

**왜 지금인가.** M4 가 폴더를 만들었는데 **문서 15건이 전부 미분류다** (2026-08-26 DB 확인).
폴더 2개(`요구사항정의서`·`화면설계서`)를 만들어 놓고 아무것도 안 넣었다. 손으로 고르는
단계가 실제로는 안 밟힌다는 뜻이다.

### 실데이터 기준선 (2026-08-26, 활성 문서 7건)

> **⚠ 이 표는 1뎁스 시절의 기준선이다. 판정 기준은 2026-08-31 부터 아래 §'폴더 2뎁스 자동 분류'의 새 표다.** 지우지 않고 남기는 이유는 그때의 근거(정규화가 왜 필요했는지, 별칭이 왜 폴더에 붙었는지)가 여기 걸려 있기 때문이다. **대조에는 쓰지 말 것.**

이 표가 검증의 기준이었다.

| 파일명 | 기대 결과 |
|---|---|
| `냥멍케어 화면설계서 — 건강기록 (HLT) v0.2.html` | 화면설계서 |
| `03_마이페이지_화면설계서_v0_3_260817.html` | 화면설계서 |
| `03_메인페이지_화면설계서_v0.3_20260819.html` | 화면설계서 |
| `01_요구사항 정의서_v0.3_2026_08_17.xlsx` | 요구사항정의서 — **공백 제거가 있어야 맞는다** |
| `02_IA 구조도_v0.2_2026_08_17.xlsx` | 갈 곳 없음 |
| `04_건강기록_와이어프레임_HLT_v0_2b (1).html` | 갈 곳 없음 (별칭을 넣으면 화면설계서) |
| `06_로그인_회원가입_와이어프레임.html` | 갈 곳 없음 (별칭을 넣으면 화면설계서) |

**지금 규칙 없이는 7건 중 4건만 붙고, 그중 1건은 정규화가 있어야 붙는다.** 예외가 주변부가
아니라 절반이다.

> **이 표는 2026-08-26 의 폴더 상태를 전제한다. 그 전제가 이미 깨졌다** (2026-08-27 확인).
> dev DB 에는 `IA 구조도` 폴더가 생겼고 `화면설계서` 에 별칭 `와이어프레임` 이 들어가
> 있어서, **"갈 곳 없음" 3행이 전부 기존 폴더로 붙는다.** 표가 틀린 게 아니라 상태가
> 변한 것인데, "규칙을 바꾸면 이 표로 다시 대조한다"고 적혀 있어 다음에 볼 때 헷갈린다.
> **대조 전에 폴더·별칭 상태부터 확인할 것.** 제안 그룹을 보려면 어떤 폴더와도 안 맞는
> 파일을 하나 따로 만들어야 한다 (실측에서는 `07_회의록_2026_08_27.html` 을 썼다).
>
> **⚠ 그 별칭도 지금은 없다** (2026-08-31 실측). dev 의 폴더 6개 모두 `aliases` 가 **빈 배열**이다. 언제 지워졌는지는 기록이 없다. 와이어프레임 파일이 `화면설계서` 로 붙는다는 서술은 **현재 상태에서 거짓**이다 — 별칭을 다시 넣어야 성립한다.

### 처리해야 할 예외

**매칭**
- 갈 곳이 없다 (가장 흔하다) → **폴더 자동 생성 + 사람 확인**. 확인 없이 만들지 않는다
- 폴더가 하나도 없다 → 전 건 미분류. 조용히 아무 일도 안 하는 것이 정상 동작이다
- 여러 폴더에 동시 매칭 → 점수로 고르고, **최고점이 임계값 미만이면 미분류**
- **같은 이름 폴더가 여러 곳에 있을 수 있다** — 스키마가 `@@unique([parentId, name])` 라
  부모가 다르면 같은 이름이 공존한다. 문자열 일치만으로는 못 고른다

**파일명 정규화** (실데이터가 전부 증명한다)
- 구분자 흔들림: `요구사항 정의서`(공백) · `요구사항_정의서`(밑줄) · 폴더는 `요구사항정의서`(붙임)
- 버전·날짜 토큰: `v0.2` `v0_3` `v0_2b` `2026_08_11` `260817` `20260819`
- **숫자 prefix 를 믿지 말 것**: `03_` 은 화면설계서 2건인데 와이어프레임은 `04_`·`06_` 로 갈린다
- 브라우저 중복 접미사: `(1)` · ` - 복사본`
- **macOS 한글 NFD** — 이 팀엔 맥 사용자가 있다 (`f95d106` 이 증거). 맥에서 올린 파일명은
  한글이 자모 분리된 채 와서 `요구사항` ≠ `요구사항` 이 되고 **화면상 구분이 안 된다.**
  NFC 정규화가 없으면 조용히 실패한다
- 영문 대소문자 — M4 가 태그에서 미해결로 남긴 문제(`DESIGN.md` §4)가 그대로 재발한다
- **확장자는 신호가 아니다** — `.html` 이 화면설계서와 와이어프레임 양쪽에 걸쳐 있다

**배치·동시성**
- **"한 배치는 한 폴더" 전제가 깨진다.** `upload-dialog.tsx:221` 이 파일을 담은 뒤 셀렉트를
  일부러 잠근다 (주석 213줄: "한 배치가 두 폴더로 갈라진다"). 자동 분류는 그 갈라짐을
  **의도적으로** 하는 것이라 잠금의 존재 이유가 바뀐다
- **워커 3개(`MAX_PARALLEL = 3`)가 같은 이름 폴더를 동시에 만들면 `@@unique([parentId, name])`
  위반(P2002)이 난다.** → **확인받은 뒤 폴더를 먼저 다 만들고 그다음 업로드를 시작하면
  이 문제가 통째로 사라진다.** 동시성 처리보다 순서를 바꾸는 게 싸다
- 부분 실패: 일부만 분류 실패해도 나머지는 그대로 진행한다
- 취소: 자동 생성된 폴더가 남지 않아야 한다

### UI — 올라가기 전에 보여준다

**조용히 배정하지 않는다.** `upload-dialog.tsx:211` 주석이 그 이유를 적어 뒀다 —
*"조용히 배정하면 '왜 여기 올라갔지'가 된다."* 어제 셀렉트를 넣은 근거가 이것이고,
자동 분류는 그 결정을 뒤집는 것이므로 **미리보기가 필수다.**

```
기존 폴더로 이동        5건
새 폴더 생성 후 이동    2건   [ ] 만들지 않음
미분류                  1건
```

- 새 폴더 생성만 따로 끌 수 있어야 한다 — 애매하면 "일단 미분류로" 를 고를 수 있게
- **왜 그 폴더로 갔는지 근거 한 줄**(`'화면설계서' 일치`)을 보여준다. 없으면 사용자가 전체를 의심한다
- 개별 건을 고칠 수 있어야 한다

### 브라우저 실측 · ✅ 9종 완료 (2026-08-26 착수 · 2026-08-27 완료)

- **별칭이 실제로 동작한다.** 폴더 Dialog 에서 `화면설계서` 에 별칭 `와이어프레임` 을
  넣으니 와이어프레임 파일이 새 폴더 제안 없이 그 폴더로 분류됐다. `DESIGN.md` §4.2 의
  7번이고, **이번 기능의 핵심 경로다** — "정규화가 별칭보다 먼저, 별칭은 의미 차이 전용"
  이라는 설계 결정이 실제로 성립한다는 뜻이다. 와이어프레임이 화면설계서인지는 문자열로
  알 수 없는 사람만 아는 판단이고, 그것을 사람이 한 번 알려 주면 그다음부터 기계가 한다.
- **제안 폴더 생성도 동작한다** (아래 절 — 다만 이름이 문제다).

**나머지 7종도 2026-08-27 에 끝났다.** `DESIGN.md` §4.2 기준으로 전항목 통과.

| # | 항목 | 결과 |
|---|---|---|
| 1 | 미리보기 3그룹·건수·근거 줄 | 8건 → 기존 7 / 새 폴더 1 / 미분류 0 |
| 2 | "만들지 않음" 체크 | 제안 1건이 미분류로 이동, 업로드해도 폴더 0개 생성 |
| 3 | 개별 셀렉트로 목적지 수정 | 화면설계서 → 요구사항정의서로 고친 대로 저장 |
| 4 | 미리보기에서 닫기 | 문서 0 · 폴더 0 |
| 5 | 업로드 직후 취소 | 올라간 문서와 그 폴더는 남고 빈 폴더만 삭제 (2개 → 1개) |
| 6 | 수동 모드 보존 | 폴더 고르면 미리보기 없이 즉시 업로드 · 폴더 연 채 열면 기본값이 그 폴더 |
| 7 | 별칭 실증 | 근거 줄이 `별칭 '와이어프레임' 일치` 로 뜨고 화면설계서행 |
| 8 | 폴더 0개 상태 | 안내문 표시 · 미리보기 없이 조용히 미분류 |
| 9 | NFD 파일명 | 자모 분리 한글이 NFC 폴더에 붙음 — **단 프록시다(아래)** |

**어떻게 했나.** `HANDOFF.md` "실측 방법" 절의 결정대로 파이프라인을 쓰지 않았고,
e2e 하네스도 먼저 짓지 않았다. 임시 Playwright 스크립트로 **DB 대조가 판정하는 것**을
몰아 돌리고, 판정이 눈이어야 하는 것은 스크린샷으로 남겼다(`test/e2e/shots/AC*.png`).
스크립트는 확인 뒤 지웠다 — 회귀 테스트는 고칠 대상이 정해진 뒤에 쓰는 것이 싸다는
같은 절의 판단을 따랐다.

**#5 는 설계가 옳았다.** `page.route` 로 S3 PUT 을 붙잡아 취소 창을 만들었다 —
B10 때처럼 **DevTools 스로틀링도, 소스에 임시 지연을 넣었다 빼는 일도 없었다.**
첫 건만 통과시키고 나머지를 매달아 두니 "올라간 것은 남고 빈 폴더만 지워지는" 대조가
그대로 나왔다. `DESIGN.md` §4.2 를 갈랐을 때 "자동화가 이득"으로 꼽은 유일한 항목이
실제로 그랬다.

**#8 은 파괴적이라 스냅샷 후 복원했다.** 폴더를 전부 지워야 하는 항목이다. 폴더 4개와
문서 배정 4건을 JSON 으로 뜨고, 실측 뒤 같은 id·`created_at`·`aliases` 로 되돌렸다
(`documents.folder_id` 는 `onDelete: SetNull` 이라 같이 날아간다). 최종 상태가 시작 전과
완전히 같은 것을 대조했다 — 문서 20건(휴지통 8), 미분류 16 / 요구사항정의서 3 / IA 구조도 1.

**아직 안 본 것: 맥 실기 하나뿐이다.** #9 는 Windows 에서 파일명을 NFD 로 만들어 올린
**프록시**다. 분류 로직은 실증됐지만 "맥에서 올린 실제 파일"은 아니다. 맥 팀원이 올린
한글 파일명 1건은 여전히 미검증으로 남는다.

### 고쳤다 — 제안 이름 편집 · ✅ 완료 (구현 2026-08-27 · 브라우저 실측 2026-08-28)

아래 "실측에서 드러난 것"을 고친 결과다. 파이프라인 4차 주행(`suggest-name-edit`)이
완주해 `8e3ef4b` 가 나왔고, 브라우저 실측 11/11 통과 후 결함 1건을 `5769ad5` 로 고쳐
`main` 에 머지했다. **이번에는 실측을 끝내고 넣었다** — 자동 분류 때는 순서가 뒤집혔다.
회귀 테스트는 `npm run test:e2e:sne`.

- **제안 행에 이름 입력칸.** 고치면 `destTouched` 라 "만들지 않음" 일괄 체크가 안 덮는다
- **`findExistingFolderByName` (설계에 없던 추가).** 고친 이름이 기존 폴더의 이름·별칭과
  정규화 기준으로 맞으면 그 폴더로 흡수한다. 이게 없으면 정확일치는 409 로 튕겨
  미분류가 되고, 정규화만 같은 이름은 유사 중복 폴더가 조용히 생긴다
- **흡수 판정은 업로드 시작 시점에만.** 타이핑 중 행이 그룹을 옮기면 입력칸이
  unmount 되어 포커스를 잃는다
- **`extractCore` 가 끝의 영문 대문자 2~4자 코드를 뗀다** (토큰 1개 남으면 중단).
  보조일 뿐이다
- **미리보기 레이아웃 3건** — 드롭존 한 줄 접기 · 그룹 요약을 스크롤 밖에 고정 ·
  잠긴 폴더 셀렉트를 텍스트 한 줄로

`npm test` **224 → 240**, `lint`·`build` 통과. **`upload-dialog.tsx` 의 자동 회귀망은
0이었다** — 기존 e2e 가 이 컴포넌트를 지나지 않았다(판단검증 #29).
`test/e2e/suggest-name-edit.mjs` 11항목이 그 구멍을 덮는다.

> **원본 실측 절차(`VERIFY.md` §5)는 잃었다.** `.pipeline/` 이 gitignore 대상이라
> git 어디에도 없고 Windows worktree 에만 있었다. 위 11항목은 `8e3ef4b` 의 코드와
> 커밋 메시지에서 역산한 재구성본이라 **원본과 항목 번호가 다르다.**
> 근거와 재발 조건은 `HANDOFF.md` "지뢰" 의 `.pipeline/` 항목.

**실측 결과 (2026-08-28, dev 브랜치 · 1440×780)**

| | 결과 |
|---|---|
| 제안 이름 편집 → 그 이름으로 폴더 생성 | 통과 |
| 기존 폴더명으로 고치면 흡수 | 통과 — `화면설계서` 1개 유지, 중복 안 생김 |
| 끝의 영문 코드 제거 | `08_점검항목_HLT` → `점검항목` |
| 고친 행을 "만들지 않음"이 안 덮음 | 통과 |
| 빈 이름 → 시작 잠김 | 통과 |
| 101자 입력 | **`maxLength` 가 100자로 자른다** — `folderNameError` 의 101자 분기는 화면에서 도달 불가 (그 분기는 `folder.test.ts:105` 가 덮는다) |
| 첫 화면 판단 대상 | **2.4행 → 3.7행** (8건 담아 스크롤 넘긴 상태에서 측정) |

행 자체는 이름 편집칸 때문에 100px → 138px 로 커졌는데 그걸 상쇄하고도 남았다.

> **측정을 한 번 틀렸다.** 파일 1건만 담고 재서 2.5행이 나왔고 "개선이 거의 없다"고
> 적을 뻔했다. 다이얼로그가 `max-h-[85vh]` + `flex-col` 이라 **내용이 적으면 `flex-1`
> 스크롤러가 뷰포트가 아니라 내용 크기로 줄어든다** — 그러면 `영역/행` 은 "몇 행이
> 들어가는가"가 아니다. e2e 는 `scrollHeight > clientHeight` 를 단언으로 걸어
> **측정이 무효인 채로 통과하는 것**을 막는다.

**실측에서 나온 결함 1건 — 셀렉트 라벨이 편집한 이름을 안 따라갔다.** 이름을 고치면
셀렉트는 `새 폴더 '건강기록 와이어프레임'`(최초 제안), 입력칸은 `와이어프레임`(고친 값)
이라 한 행이 두 이름을 말했다. 실제로 만들어지는 것은 입력칸 쪽이다. 라벨이
`item.result.proposedName`(담는 순간 고정)을 그렸던 것을 `dest.name` 으로 바꿨다
(`upload-dialog.tsx:411`). **대조로 판정했다** — 되돌리면 e2e 의 A3 가 FAIL 한다.
**자동화로는 안 잡히는 종류다**: "두 라벨이 다르면 실패"라는 assert 를 미리 쓰려면
이미 그 결함을 알고 있어야 한다.

**미검증으로 남긴 것 셋** — 별칭 경로 흡수(dev 의 별칭이 전부 비어 있다) · 제안 이름의
**적절성**(문자열로 판정 불가) · 목적지를 미분류로 바꿨다 되돌리면 편집값이 리셋되는 것
(`upload-dialog.tsx:337`, 수용 여부 미정). `HANDOFF.md` 미검증 표에도 넣었다.

> **`classify.ts` 의 매칭 방향을 헷갈리지 말 것.** `classify.ts:105` 는
> `haystack.includes(normalized)` 이고 `haystack` 이 **파일명**, `normalized` 가
> **폴더 이름**이다. 즉 **파일명이 폴더 이름을 통째로 품어야** 붙는다.
> 폴더 `로그인 회원가입 와이어프레임` 이 있어도 `09_건강기록_와이어프레임_HLT.html` 은
> **안 붙고 제안으로 간다**(2026-08-28 실측). HANDOFF 에 한때 반대로 적혀 있었다.

### 실측에서 드러난 것 — 다음 마일스톤에서 고친다 (2026-08-26)

**제안 폴더 이름이 카테고리가 아니라 문서 제목으로 나온다.** 브라우저 실측 중 확인했다.
구현은 설계대로이고 테스트도 통과한다 (`classify.test.ts:37`) — **설계 자체의 한계다.**

| 파일명 | 제안 | |
|---|---|---|
| `02_IA 구조도_v0.2_2026_08_17.xlsx` | `IA 구조도` | 좋다 — 카테고리다 |
| `04_건강기록_와이어프레임_HLT_v0_2b (1).html` | `건강기록 와이어프레임 HLT` | **나쁘다 — 문서 제목이다** |
| `06_로그인_회원가입_와이어프레임.html` | `로그인 회원가입 와이어프레임` | **나쁘다** |

**파일명만으로는 이 둘을 구분할 신호가 없다.** 어떤 팀은 파일명에 카테고리를 쓰고 어떤
팀은 문서 제목을 쓴다. `extractCore` 를 아무리 다듬어도 원리상 못 가른다 — 정규식 문제가
아니다.

따라오는 진짜 피해는 **폴더 폭발**이다. 문서 1건짜리 폴더가 파일 수만큼 생긴다.

**고칠 방향: 미리보기에서 제안 이름을 편집할 수 있게 한다.**

`DESIGN.md` §6 이 일부러 뺀 것이다 — "틀린 제안은 '만들지 않음' 이나 미분류로 처리하면
된다"가 근거였다. **그 근거를 폐기한다. 그건 폴더를 포기한다는 뜻이라 이 기능의 의도와
어긋난다.** 미리보기는 이미 사람이 개입하는 자리이고 목적지는 고칠 수 있는데 이름만 못
고친다. 입력칸 하나가 빠져 있는 것이다.

곁들일 것: `extractCore` 가 **끝에 붙은 짧은 영문 코드**(`HLT` 같은)를 떨어뜨리면 제안
품질이 조금 오른다. 보조일 뿐이고 근본은 사람이 고칠 수 있게 하는 것이다.

**같이 처리할 것 — 미리보기 첫 화면 배분** (2026-08-27 실측에서 측정). 이름 편집칸이
행마다 붙으면 **한 행이 지금(100px)보다 커지므로 이 문제는 나빠진다.** 별건으로 고치면
두 번 일한다.

| | 1280×900 | 1440×780 (노트북) |
|---|---|---|
| 본문에 담긴 높이(8건) | 1542px | 1542px |
| 화면에 보이는 높이 | 641px | 539px |
| 첫 행까지 밀리는 거리 | 278px | 278px |
| 첫 화면에 보이는 행 | **3.4행** | **2.4행** |

**스크롤 자체는 문제가 아니다** — 8건이면 1542px 라 어떤 화면에도 안 들어간다.
`max-h-[85vh]` + `overflow-y-auto` 는 옳고 바꾸지 말 것. 문제는 그 278px 의 내역이다:
드롭존 140px(파일을 이미 담았는데 자리를 지킨다) + 저장할 폴더 셀렉트 66px(**이 시점엔
`disabled` 다**) + 나머지 72px. **판단하라고 만든 화면인데 판단 대상이 노트북에서 2.4개만
보인다.**

- 드롭존을 미리보기 중에는 한 줄(`+ 파일 더 담기`)로 접는다 — 140px → 40px, 약 +1행.
  자동 모드는 시작 전에 파일을 더 담을 수 있으므로 **기능은 유지해야 한다**
- **그룹 요약(`7 / 1 / 0`)을 상단 고정.** 이쪽이 더 근본이다 — 이 화면의 결정은
  "이 배치가 이렇게 갈라져도 되는가"이고 그 답은 건수로 나온다. 개별 행은 뭔가
  이상해 보일 때만 본다. 지금은 스크롤하면 건수가 화면 밖으로 사라진다
- 잠긴 폴더 셀렉트를 한 줄 텍스트로 (66px → 24px, 부수적)

> **"2건 이상 모일 때만 폴더를 제안한다"는 검토했고 버렸다** (2026-08-26). 폴더 폭발은
> 막지만 **의도를 깬다.** `plannedFolderNames` 는 **한 배치 안의 목적지만** 보므로
> "2건 이상"은 실제로는 "한 번에 같이 올린 2건 이상"이다. 같은 종류를 따로 올리면
> 양쪽 다 미분류가 되고, **어떤 카테고리든 맨 처음 파일은 항상 1건이라 새 카테고리는
> 구조적으로 폴더를 못 얻는다.** "폴더를 만들어 놓고 아무것도 안 들어간다"는 원래 문제와
> 같은 자리다. **1건이어도 분류되는 것이 의도다.**

> **별칭이 이미 답인 경우와 헷갈리지 말 것.** 위 두 와이어프레임 건은 원래 기준선 표가
> **"별칭을 넣으면 화면설계서"** 로 적어 둔 것들이다. `화면설계서` 에 별칭 `와이어프레임`
> 을 넣으면 새 폴더 제안 자체가 안 나온다. **A·B 는 별칭으로 안 풀리는 나머지를 위한 것**
> — 즉 진짜로 새 폴더가 필요한 파일들이다.

### 하지 않는 것

- ~~**기존 문서의 소급 분류.**~~ **폐기됐다 (2026-08-31).** 원래 근거는 *"활성 7건 중
  자동 분류 대상이 4건뿐이라 손으로 옮기는 것이 압도적으로 싸다 (4번 클릭, 코드 0줄)"* 였다.
  **그 산수가 더는 안 맞는다** — 활성 문서가 26건(테스트 2건 제외)이고 2뎁스로 접으면
  **26건 전부**가 이동 대상이다. 손으로는 26번 클릭에 하위 폴더 16개를 먼저 만들어야 한다.
  그리고 안 하면 폴더 구조가 1뎁스·2뎁스 두 세대로 갈라진다. 새 결정은 위 "확정된 설계 결정"
  표의 *기존 문서 소급 이동* 행과 아래 §'폴더 2뎁스 자동 분류'에 있다
- **파일 내용 기반 분류** — 파일이 앱 서버를 안 거친다(`CLAUDE.md` 불변식). 내용을 볼 수 없다
- **LLM 분류** — 업로드 경로에 외부 API 왕복을 넣지 않는다. 규칙 기반으로 간다
- **재업로드(v2+) 자동 분류** — `[id]/versions/route.ts` 에 폴더 개념이 아예 없다.
  문서가 이미 폴더를 갖고 있으므로 정상이다. 여기까지 넓히지 말 것
- **사용자가 고친 것을 별칭으로 자동 학습** — 오분류를 학습해 굳힌다. 제안까지가 한계이고
  이번 범위 밖이다
- 태그 자동 부여 — 실무에선 이쪽이 근본 해법에 가깝지만(폴더는 하나만 고를 수 있어서
  애매함이 치명적인데 태그는 여러 개 붙는다) 이번엔 폴더로 간다. 대안이 있었다는 기록만 남긴다

### 스키마 변경 (사람이 승인함 — 2026-08-26)

```prisma
/// Folder
aliases String[] @default([])
```

**`db push` 완료 (2026-08-26).** 개발(`dev`)·운영(`production`) 두 브랜치 모두에
`aliases` 컬럼이 있는 것을 `information_schema` 로 확인했다. 운영 쪽은 DB 분리 **전에**
밀렸고 dev 는 그 이후 복제라 양쪽이 같다 — 근거와 함의는 `SETUP.md` 1-2 절.

## 폴더 2뎁스 자동 분류 · 실측 완료 (2026-09-04) — **dev 소급 이동까지 끝남**

파일명이 `로그인_화면설계서` 면 `화면설계서 > 로그인` 으로 **두 단계**에 넣는다.

**왜 지금인가.** 1뎁스 분류는 동작하지만 폴더가 목록을 못 좁힌다 — dev 실측에서
`화면설계서` 한 폴더에 **11건**이 들어 있다. 폴더를 열어도 여전히 파일명을 읽어야 한다.

### 진행 상황 (2026-09-04 갱신)

| 단계 | 상태 |
|---|---|
| 파이프라인 design·judge·impl·verify | ✅ 전부 DONE |
| `npm test` · lint · build | ✅ **387건** 통과 (제품명 제거 테스트 2건 추가) |
| 브라우저 실측 B1~B10 | ✅ **26/26** — `test/e2e/folder-depth2.mjs` 로 자동화 |
| dry-run 26행 대조 | ✅ **26/26 일치** |
| dev `--apply` | ✅ 폴더 20개(카테고리 3 + 하위 16 + 테스트 1) · 문서 28건 유실 0 |
| S1 무변경 · S4 `updated_at` 불변 · S5 롤백 | ✅ 통과 (롤백 24/24 전건 복귀, 재적용 왕복 28/28 동일) |
| S6 낙관적 잠금 · B4 흡수 힌트 | ❌ **미검증** |
| main 머지 | ❌ 안 함 — 아래 §'카테고리 폴더 본문'과 짝이라 같이 올린다 |

**실측에서 결함 2건이 나왔고 둘 다 고쳤다.**

1. **제품명 접두사가 하위 폴더를 갈랐다** (`9264b9c`). `냥멍케어_기능명세서_건강기록` 이
   `건강기록` 이 아니라 `냥멍케어 건강기록` 폴더를 만들었다. "매칭 우선이라 정착 뒤엔
   흡수된다"는 전제는 **그 카테고리에 같은 주제 문서가 2건 이상일 때만** 성립하고,
   `기능명세서 > 건강기록` 은 문서가 1건뿐이라 매칭할 상대가 영영 없다.
   설계가 이름 품질을 수용한 근거("사람이 미리보기에서 고친다")도 **소급 이동에는
   안전판이 없어** 성립하지 않는다. `coreTokens()` 에서 걷는다.
2. **자식이 생기는 카테고리를 "비게 되는 폴더"로 보고했다** (`7199ccf`). 문서 수만 세고
   자식 폴더를 안 봤다. 안내대로 지우면 `onDelete: Cascade` 로 하위가 통째로 날아가고
   스냅샷에는 폴더가 없어 되돌릴 수도 없다. 판정을 `plan.working`(계획 이후)에서 한다.

### 진행 상황 (2026-09-02 최초 기록)

파이프라인 `folder-depth2` 가 `../dms-folder-depth2`(`feature/folder-depth2`)에서 완주했다.

| 단계 | 상태 |
|---|---|
| design · judge | ✅ 사람 승인 완료 (judge 36개 주장 전수 검증 — 반박 3 · 미확인 2) |
| impl | ✅ `src/` 6파일, 신규 `scripts/reclassify-{folders,rollback}.mjs` |
| verify | ✅ **통과** — `npm test` 26파일 **385건**(이전 240) · `npm run lint` · `npm run build` |
| 커밋 | ✅ 4개 (`6472a83` 설정 · `df4056a` 기능 · `535d120` 스크립트 · `0ba90b1` 파이프라인 모델) |
| main 머지 | ❌ **안 함 — 실측 전에는 하지 않는다** |

**"통과"가 덮지 못하는 구간이 남아 있다.** `npm test` 는 순수 함수만 지나므로
`upload-dialog.tsx` 는 한 줄도 실행되지 않았다. judge #29 가 짚은 대로 `ClassifyFolder` 에
`parentId` 를 넣어도 호출부가 이미 `FolderAliasRow` 를 넘겨서 **타입 오류가 안 난다** —
경로 라벨·`createFolder(parentId, name)`·흡수 판정은 **컴파일러가 침묵한다.**
체크리스트는 그 worktree 의 `.pipeline/folder-depth2/VERIFY.md` §4 (브라우저 B1~B10 ·
스크립트 S1~S7).

**`VERIFY.md` 의 S0 은 이미 해소됐다** — `node -p "process.features.typescript"` = `strip`
(2026-09-01 실측, v24.10.0). `--experimental-strip-types` 는 필요 없다. verify 세션이
핸드오프를 안 읽어 미확인으로 올린 항목이다.

**소급 스크립트는 아직 한 번도 실행된 적이 없다.** `src/lib/reclassify-script.test.ts` 는
소스 **텍스트**를 grep 하는 테스트라 "실행하면 돈다"를 말해주지 않는다.

**미해결이던 판단 1건은 닫혔다** — `DESIGN.md` §3.2 stage 1 의 "루트 0개" 분기는
`classify.ts:188` 이 `unclassified(REASON_NO_MATCH)` 로 고정 반환한다(judge #28 권고대로).
`DESIGN.md:373` C18 기대값에 남은 "또는 루트 제안" 문구만 낡았다.

### 착수 시점 실측 (2026-08-31, dev DB 읽기 전용 조회)

| 폴더 | 문서 | 성격 |
|---|---|---|
| `화면설계서` | 11 | 카테고리 |
| `기능명세서` | 8 | 카테고리 |
| `요구사항정의서` | 0 | 카테고리 (비어 있음) |
| `로그인 회원가입 와이어프레임` | 1 | **카테고리+하위가 합쳐진 폴더** |
| `플레이스 와이어프레임` | 1 | **같음** |
| `dms preview test` | 2 | 테스트 산출물 |
| (미분류) | 5 | |

활성 문서 28건(테스트 2건 포함). **폴더 6개 모두 `aliases` 가 빈 배열이다** — 위
§'실데이터 기준선'의 인용 블록이 별칭 `와이어프레임` 이 들어 있다고 적어 둔 것은 현재 거짓이다.

> **문서 두 곳이 낡아서 같이 고쳤다.** ① 위 별칭 서술 ② `CLAUDE.md` 의 *".env 에 더미 값이
> 들어 있다"* — 이 조회가 로컬 `.env` 로 dev 브랜치에 실제로 붙었다.

**합성 폴더 2개가 폴더 폭발의 실물이다.** `로그인 회원가입 와이어프레임`·`플레이스
와이어프레임` 은 각각 문서 1건짜리이고, 2뎁스 구조에서는 `화면설계서 > 로그인 회원가입`·
`화면설계서 > 플레이스` 여야 한다.

### 새 판정 기준선 (2026-08-31, 활성 26건 · 테스트 2건 제외)

**규칙을 바꾸면 이 표로 대조한다.** 위 1뎁스 표를 대체한다.
`화면설계서` 에 별칭 `와이어프레임` 이 들어 있는 것을 전제한다.

| 파일명 | 기대 목적지 | 비고 |
|---|---|---|
| `01_요구사항 정의서_v0.3_2026_08_17.xlsx` | `요구사항정의서` (루트) | 나머지 없음 → 카테고리 루트 |
| `02_IA 구조도_v0.2_2026_08_17.xlsx` | 새 폴더 제안 `IA 구조도` (**1뎁스**) | 카테고리 미매칭 → 2뎁스 안 만든다 |
| `03_마이페이지_화면설계서_v0_3_260817.html` | `화면설계서 > 마이페이지` | |
| `03_마이페이지_화면설계서_v0_4_260825.html` | `화면설계서 > 마이페이지` | |
| `03_메인페이지_화면설계서_v0.3_20260819.html` | `화면설계서 > 메인페이지` | |
| `03_메인페이지_화면설계서_v0.6_20260826.html` | `화면설계서 > 메인페이지` | |
| `03_AI매니저_화면설계서_v0.1_2026_08_17.html` | `화면설계서 > AI매니저` | |
| `03_AI매니저_화면설계서_v0.2_2026_08_26.html` | `화면설계서 > AI매니저` | |
| `03_공통레이아웃_화면설계서_v0.1_2026_08_15.html` | `화면설계서 > 공통레이아웃` | |
| `03_공통레이아웃_화면설계서_v0.2_2026_08_26.html` | `화면설계서 > 공통레이아웃` | |
| `06_커뮤니티_화면설계서_v0_2_260826.html` | `화면설계서 > 커뮤니티` | |
| `07_고객센터_화면설계서_v0.2_260826.html` | `화면설계서 > 고객센터` | |
| `냥멍케어_화면설계서_건강기록_HLT_v0.3.html` | `화면설계서 > 건강기록` | **추출만으로는 `냥멍케어 건강기록`** — 매칭 우선이라야 맞는다 |
| `냥멍케어 화면설계서 — 건강기록 (HLT) v0.2.html` | `화면설계서 > 건강기록` | 같음 |
| `04_건강기록_와이어프레임_HLT_v0_2b (1).html` | `화면설계서 > 건강기록` | 별칭 경유 |
| `06_로그인_회원가입_와이어프레임.html` | `화면설계서 > 로그인 회원가입` | 토큰 2개를 **한 폴더로** |
| `06_로그인_회원가입_와이어프레임_v0.2_260826.html` | `화면설계서 > 로그인 회원가입` | 지금은 합성 폴더에 있다 |
| `05_플레이스_와이어프레임_v0.2_260826.html` | `화면설계서 > 플레이스` | 지금은 합성 폴더에 있다 |
| `04_기능명세서_v0.1_2026_08_26.xlsx` | `기능명세서` (루트) | 나머지 없음 |
| `03_메인페이지_기능_명세서_v0.2_20260826.xlsx` | `기능명세서 > 메인페이지` | **카테고리가 두 토큰에 걸친다** (아래 함정) |
| `04_기능명세서_마이페이지_v0.1_260826.xlsx` | `기능명세서 > 마이페이지` | 카테고리가 앞에 와도 된다 |
| `05_플레이스_동물병원_기능명세서_v0.1_260826.xlsx` | `기능명세서 > 플레이스 동물병원` | 토큰 2개를 한 폴더로 |
| `06_로그인_회원가입_기능명세서_v0.1_260826.xlsx` | `기능명세서 > 로그인 회원가입` | |
| `고객센터_기능명세서_v0_2.xlsx` | `기능명세서 > 고객센터` | 번호 prefix 없음 |
| `냥멍케어_기능명세서_건강기록_HLT_v0_1.xlsx` | `기능명세서 > 건강기록` | 제품명 접두사 |
| `커뮤니티_기능명세서_v0_2_.xlsx` | `기능명세서 > 커뮤니티` | 끝에 구분자가 남는다 |

**추출 규칙만으로 맞는 것이 26건 중 23건이고, 틀리는 3건은 전부 원인이 같다**(제품명
`냥멍케어`). 매칭 우선 구조가 이걸 첫 1건으로 줄인다.

### 확정 규칙

1. **2단계 매칭.** 카테고리(루트 폴더)를 먼저 정하고, 그 폴더의 **자식들만** 후보로 다시
   매칭한다. **평면 매칭을 유지하면 안 된다** — 현재 점수는 키의 정규화 길이라
   (`classify.ts:105-110`) 자식 `로그인`(3)이 부모 `화면설계서`(5)에게 항상 진다. 즉 하위
   폴더를 만들어도 다음 업로드에서 재사용되지 않고 전부 부모 루트에 쌓인다
2. **하위 이름은 매칭이 1차, 추출이 폴백.** 붙는 자식이 없을 때만 파일명에서 뽑는다
3. **추출 = 노이즈 제거 → 카테고리 구간 제거 → 남은 것.** 노이즈는 기존 `extractCore` 와
   같다(번호 prefix·버전·날짜·복사본·확장자·끝 영문코드)
4. **나머지가 비면 카테고리 루트에 넣는다.** 미분류로 떨구면 지금보다 나빠진다
5. **하위 폴더도 별칭을 본다.** 부모와 같은 규칙 하나로 간다
6. **새 카테고리 제안은 1뎁스까지.** 근거는 "확정된 설계 결정" 표
7. **빈 폴더 정리는 이번 배치 생성분에 한한다.** 자식이 비면 같이 만든 부모도 지운다

### 구현이 반드시 걸리는 함정

**카테고리 구간 제거를 토큰 단위로 하면 안 된다.** 실데이터에
`03_메인페이지_기능_명세서_v0.2_20260826.xlsx` 가 있고, 카테고리 `기능명세서` 가
**`기능`·`명세서` 두 토큰에 걸쳐 있다.** 토큰을 하나씩 비교하면 안 지워져서 하위 이름이
`메인페이지 기능 명세서` 가 된다. 반대로 정규화 문자열에서 잘라내면 지워지긴 하는데
구분자가 이미 사라져 `로그인회원가입` 같은 붙임말이 나온다.

→ **연속된 토큰 부분열의 정규화 결합이 카테고리 키와 같으면 그 구간을 통째로 제거**한다.
표기(공백·원문 대소문자)는 보존하면서 여러 토큰에 걸친 카테고리도 지운다.

### 소급 이동 (일회성)

**선행 작업 — 사람이 UI 에서 먼저 한다.** 스크립트가 대신하지 않는다.

1. `화면설계서` 에 별칭 `와이어프레임` 추가. 없으면 와이어프레임 4건이 안 붙는다
2. 합성 폴더 2개(`로그인 회원가입 와이어프레임`·`플레이스 와이어프레임`) 삭제.
   **순환을 끊기 위한 것이다** — 그 폴더가 살아 있으면 점수 13점으로 별칭 6점을 이겨서
   소급 스크립트도 계속 그리로 보낸다. 삭제하면 안의 문서는 미분류로 떨어지고
   (`schema.prisma:54` `onDelete: SetNull`) 스크립트가 정상 경로로 재분류한다

**스크립트 규약**

- `--dry-run` 이 기본. 이동 예정 표를 찍고 **아무것도 안 바꾼다**
- 실행 전 `(문서 id, 이전 folderId)` 스냅샷을 파일로 남긴다. 되돌리기 스크립트를 같이 만든다
- **하위 폴더는 만들지만 새 카테고리는 만들지 않는다.** 소급은 구조를 접는 작업이지
  카테고리를 세우는 작업이 아니다 — `02_IA 구조도` 1건은 미분류로 남고 사람이 UI 에서 처리한다
- **폴더를 지우지 않는다.** `schema.prisma:35` 가 `onDelete: Cascade` 라 폴더 삭제는 자식
  폴더까지 데려간다. 비게 된 폴더는 dry-run 결과에 보고만 하고 삭제는 사람이 UI 에서
- dev 에서 dry-run → 표 확인 → dev 실행 → 눈으로 확인 → **운영은 사람이 직접 실행**
- 파이프라인에 넣지 않는다. 되돌리기 어려운 데이터 변경은 에이전트에 위임하지 않는다

**받아들인 대가: 폴더 수가 6개 → 20개가 된다** (카테고리 3 + 하위 16 + 테스트 1).
그중 **문서 1건짜리 하위 폴더가 10개**다. 1뎁스 시절 "폴더 폭발"이라 부르며 피하던 모양인데
이번엔 의도한 결과다 — 그때는 *카테고리 자리*에 문서 제목이 올라온 것이 문제였고, 지금은
*하위 자리*에 화면 이름이 들어가는 것이라 층위가 다르다.

### 하지 않는 것

- **3뎁스 이상.** 파일명에 그만한 구조가 없다
- **재업로드(v2+) 재분류.** 기존 결정 그대로 — 문서가 이미 폴더를 갖고 있다
- **소급 이동을 UI 버튼으로.** 26건짜리 1회 작업에 영구 유지 대상을 만들지 않는다

## 카테고리 폴더 본문 · 착수 (2026-09-04 결정)

**카테고리를 열면 빈 화면이 나온다.** 소급 이동 뒤 `화면설계서` 는 문서 0건이고 16건이
전부 하위에 있는데, 목록이 자손을 안 보여주고 본문에 자식 폴더도 안 그린다.
사이드바에만 2뎁스가 보인다 (2026-09-04 브라우저 실측).

**원인.** `src/lib/search.ts:30` 의 `folderFilterWhere` 가 `{ folderId: raw }` 완전일치다.
1뎁스 시절엔 모든 폴더가 잎이라 "폴더 = 그 안의 문서 전부"가 항상 참이었고, 2뎁스를
넣으면서 그 전제가 깨졌는데 이 쿼리만 옛 전제에 남았다. **`DESIGN.md`·`JUDGE.md`·이 문서
어디에도 이 항목이 없다** (전수 grep). 설계가 통째로 놓친 자리다.

**이건 제품의 존재 이유와 부딪친다.** "한눈에 안 들어온다 / 어디 있는지 못 찾는다"를
풀려고 만든 폴더인데, 카테고리를 열면 빈 화면이다. 소급 이동 전에는 11건이 보이던
자리라 팀 입장에서는 **기능이 퇴보한 것으로 보인다.**

### 정한 방향 — 본문에 자식 폴더를 그린다

파일 탐색기처럼 **자식 폴더 먼저, 그 아래 문서**. 자식 폴더는 이름과 문서 수를 보여주고
누르면 그 폴더로 들어간다.

**왜 이쪽인가.** 대안은 "자손 문서까지 목록에 합쳐 보여주기"였다. 그쪽은 변경이 더
작지만 `화면설계서` 를 열면 16건이 그대로 나와 **폴더가 목록을 좁힌다는 2뎁스의 목적이
절반만 달성된다.** 탐색 경로를 명시적으로 만드는 편이 세 가지 문제 중 "어디 있는지
못 찾는다"에 더 곧게 닿는다.

### 하지 않는 것

- **3뎁스 이상의 재귀 렌더링.** 폴더는 2뎁스가 상한이다 (§'폴더 2뎁스 자동 분류')
- **드래그 앤 드롭 이동.** 범위 밖
- **자손 문서 합산 표시를 목록에 섞기.** 위 대안이고, 채택하지 않았다

### 진행 상황 (2026-09-05 기록)

파이프라인 `folder-children-view` 가 `../dms-folder-depth2-pipeline-folder-children-view`
(`pipeline/folder-children-view`)에서 **완주했다.**

| 단계 | 상태 |
|---|---|
| design · judge | ✅ 사람 승인 완료 (judge 41개 주장 전수 검증 — 반박 0 · 미확인 2) |
| impl | ✅ `src/lib/folder.ts`·`src/lib/search.ts`(주석만)·`src/app/(app)/page.tsx`, 신규 `src/components/folder-children.tsx` |
| verify | ✅ **통과** — `npm test` 26파일 **404건**(이전 387) · `npm run lint` · `npm run build`(라우트 20개) |
| 브라우저 실측 | ✅ **37/37 통과** (2026-09-06) — `npm run test:e2e:children`, 신규 `test/e2e/folder-children-view.mjs` |
| 화면 검토 후 모양 정정 | ✅ 카드 격자 → **문서 표 안의 폴더 행** (`7a418f9`) · 제목·브레드크럼 14px 통일 |
| main 머지 | ✅ **완료** (2026-09-06) — §'폴더 2뎁스 자동 분류'와 **함께** 올렸다 |
| S7 운영 소급 이동 | ✅ **완료** (2026-09-06) — 폴더 16개 생성 · 문서 27건 이동 · **미분류 0** · 문서 총수 28 유지 |

**빈 화면이 실제로 사라졌다** (2026-09-05 실측). `화면설계서` 를 열면 자식 폴더 9줄이 표
맨 위에 뜨고 점선 박스가 안 뜬다. 기대값은 문서에서 베끼지 않고 매번 SQL 로 센다 —
`select count(*) from folders where parent_id = …` 와 화면의 폴더 행 수를 대조하는 방식이라
dev DB 가 바뀌어도 테스트가 안 썩는다.

**남아 있던 미확인 하나가 풀렸다 — 관계 `_count` 의 `where` 는 런타임에서 실제로 적용된다.**
Prisma 7 + `@prisma/adapter-pg` 조합에서 이 리포가 관계 `_count` 를 처음 쓰는 자리였고
(`grep -rn "_count" src test scripts` 0건이었다) **타입검사·린트·빌드가 원리상 못 잡는**
구간이었다. B6 이 실증했다: 테스트 전용 문서를 `화면설계서 > 마이페이지` 에 넣어
폴더 행이 `3개 문서` → 휴지통으로 보내 `2개 문서` → 복구해 `3개 문서`. 숫자가 실제로 줄었으므로
`DESIGN.md` D2 의 groupBy 폴백은 **필요 없다.**

실측이 건드린 데이터는 전부 되돌렸다 — 실행 전후로 폴더 20 · 활성 문서 28 · 휴지통 13 동일
(팀 문서는 애초에 안 건드린다. 테스트가 자기 문서를 만들어 그것만 휴지통에 넣는다).

**모양을 한 번 뒤집었다** (2026-09-06). 처음 구현은 별도 카드 격자였는데 화면을 보니
문서 표와 테두리가 둘로 갈려 "한 목록"으로 안 읽혔다. `DocumentTable` 에 optional
`folders` prop 을 얹어 문서 행 위에 폴더 행을 그린다 — 폴더에는 올린 사람·크기·수정일이
없어 `colSpan` 한 칸을 통째로 링크로 쓰고, 빈칸을 4열씩 늘어놓지 않는다. `/search` 는
`folders` 를 안 넘기므로 무변경이다. **`.pipeline/folder-children-view/` 의 `DESIGN.md`·
`JUDGE.md` 는 카드 격자를 전제로 쓰였다** — 그 문서를 읽을 때 이 정정을 감안할 것.

**실측 중에 범위 밖 결함을 하나 봤다 — 이번 변경과 무관한 기존 동작이다.**
`app-sidebar.tsx:25` 가 `pathname === href` 로 활성 항목을 정하는데 `usePathname()` 은
쿼리스트링을 뺀다. 그래서 `/?folder=…` 에서 사이드바의 **'전체 문서'와 해당 폴더가 동시에
`aria-current="page"`** 다(스크린샷 `test/e2e/shots/FCV-B1-category.png` 에서 둘 다 강조돼 보인다).
2뎁스·폴더 목록과 무관하게 1뎁스 시절부터 있던 것이고(`a32259c` 이후 무변경) 이번 스트림에서는
고치지 않았다. 고친다면 `/?folder=`·`/?tag=` 가 붙었을 때 '전체 문서'를 비활성으로 두는
한 줄이다.

## M5 — 미리보기 + 디스코드 알림 · ✅ 완료 (2026-08-28)

- [x] PDF·이미지 인라인 미리보기 — **완료 (2026-08-28), 브라우저 실측 통과.**
      `canPreview`(사용처 0곳이었다)를 순수 모듈 `src/lib/preview.ts` 의 `previewKind` 로
      옮겼다 — 페이지 컴포넌트가 판정 하나 때문에 `S3Client` 와 env 검증을 만드는
      `s3.ts` 를 끌어들일 이유가 없다. 상세 페이지가 `previewKind` 로 iframe(PDF)·
      `<img>`(이미지)·폴백 세 갈래로 갈린다. 서버 컴포넌트 정적 JSX 라 클라이언트 JS 0,
      추가 DB 왕복 0. **실측에서 결함 둘이 나왔고 둘 다 고쳤다** — 재업로드한 버전이
      새로고침 없이는 미리보기에 안 반영되던 것(주소가 버전과 무관해 브라우저가 캐시를
      계속 보여줬다)과, 제목이 v1 파일명인 채로 남던 것. 실측 7종은
      `.pipeline/inline-preview/VERIFY.md`
- [x] 그 외 확장자는 파일 아이콘 + 다운로드 버튼 — **완료 (2026-08-28).**
      `previewKind` 가 `'none'` 이면 확장자 배지 + 안내 + 다운로드 링크
- [x] 업로드 시 디스코드 웹훅 알림 — **완료 (2026-08-26).** `DISCORD_WEBHOOK_URL` 을 채워
      재배포했고 지금도 업로드할 때마다 알림이 온다. 코드는 처음부터 연결돼 있었다 —
      `notifyUpload` 가 `api/documents/route.ts:75` 와
      `api/documents/[id]/versions/route.ts:101` 에서 호출되고, 막고 있던 건
      `discord.ts:78` 의 `NODE_ENV !== 'production'` 하나뿐이라 **M6 배포가 곧 이 항목의
      스위치였다.** 이 항목에 쓴 코드는 0줄이다

> **실측하려면 파일부터 올려야 했다.** dev 활성 문서 7건이 전부 `.html`(5)·`.xlsx`(2) 라
> **PDF·이미지가 0건이었다.** 다음에 미리보기를 건드릴 때도 같은 준비가 필요하다.

**완료 기준** — PDF를 클릭하면 내려받지 않고 바로 열리고, 누가 올리면 디스코드 채널에 뜬다.

## M6 — 배포 (Vercel) · ✅ 완료 (2026-08-25 배포 · 2026-08-26 완료 기준 확인)

- [x] Vercel 프로젝트 생성 + GitHub 저장소 연결
- [x] 환경변수 11개 입력. **`APP_URL` 은 배포 주소(끝 슬래시 금지)**,
      **`DATABASE_URL` 은 Neon pooled 엔드포인트**(`-pooler`)
- [x] 디스코드 OAuth 콜백에 `https://<배포주소>/api/auth/callback` 추가 (localhost 것은 남긴다)
- [x] `infra/s3-cors.json` 의 `AllowedOrigins` 에 배포 주소 추가 → S3 콘솔에 반영
- [x] **길드 비멤버 거부 확인** — 부계정 실측 (2026-08-25). 판정 근거는 화면 문구가
      아니라 **주소창의 `?error=`** 다 — 같은 문장이 로그인 화면 하단에 상시 안내문으로도
      떠 있다. 근거와 안 한 것은 `HANDOFF.md` "현재 위치"

**완료 기준** — 팀원 6명이 각자 접속해 로그인한다. **✅ 충족됐다** (2026-08-26 DB 확인).

`users` 6행이 **전부 서로 다른 실제 디스코드 계정**이고, `documents` 를 올린 사람이
**5명**이다. 로그인만으로는 개발 중 생긴 행과 구분이 안 되지만 **업로드는 사람이 파일을
고른 것**이라 이쪽이 더 강한 증거다. 특히 **4명이 08-24 20:22~20:39 의 17분 안에 몰려 있다** —
개발·e2e 로는 나올 수 없는 패턴이다.

```sql
select created_at, username from users order by created_at;
select created_by, count(*), min(created_at), max(created_at) from documents group by 1;
```

**전부 2026-08-25 하루 안에 일어났다** (KST). 09:01 개발자 본인 확인 → 13:44 배포 →
13:41~15:53 팀원 5명 유입(배포 직후 전원 공유) → 21:00 길드 비멤버 거부 확인.
**배포 → 공유 → 유입 → 방어선 검증 순서가 지켜졌다.** 타임라인은 `HANDOFF.md`
"M6 채택 타임라인" 절.

> **DB 시각은 UTC 로 저장된다.** 위 KST 는 +9 한 값이다. `created_at` 이
> `timestamp without time zone` 이라 드라이버가 로컬로 파싱하고, 거기에 `.toISOString()`
> 을 걸면 9시간이 밀린다. 2026-08-26 에 이걸로 "팀이 배포 하루 전에 쓰고 있었다"고
> 오판했다 — 없는 사실이었다. 근거는 `HANDOFF.md` 지뢰 절.

**배포 주소** — https://nyangmeong-care-dms.vercel.app

> **2026-08-25 배포 + 실측.** 위 5개 중 4개 완료.
>
> **리전은 고쳤다** (2026-08-25). 배포 직후에는 싱가포르로 뒀다고 적었으나 **실측해 보니
> 함수가 `iad1`(미국 동부)에서 돌고 있었다.** 지금은 `icn1`(서울)이고
> `x-vercel-id: icn1::icn1::...` 로 확인된다 — **두 번째 칸**이 함수 리전이다.
> `/login` TTFB 웜 315ms → 172ms. **대시보드에서 고르고 재배포해도 한 번은 안 먹어서**
> `vercel.json` 의 `regions` 로 못 박았다 (파일이 대시보드를 이긴다).
> 서울을 고른 근거와 `sin1` 로 돌아올 조건은 `HANDOFF.md` "배포 성능 실측" 절.
>
> **배포 환경에서 전 경로가 확인됐다.** 디스코드 로그인 → JWT 세션 → presign 서명 →
> 브라우저 → S3 직접 PUT → DB 기록. 업로드 5건의 **DB `size_bytes` 와 S3 `ContentLength` 가
> 바이트 단위로 일치**하는 것까지 대조했다. presigned PUT 이 통했다는 것 자체가 S3 CORS 가
> 실제로 먹었다는 증거다 — origin 이 어긋났으면 preflight 에서 막힌다.
>
> **남은 하나: 길드 비멤버 거부.** 다른 디스코드 계정이 없어 못 했다. M1 부터 미검증인
> 항목이고 **접근 제어가 길드 멤버십 하나뿐이라 이게 유일한 방어선이다.**
> 팀에 URL 을 뿌리기 전에 확인할 것 — 뿌리고 나면 그 URL 은 어디로든 샌다.
>
> **디스코드 알림은 아직 끄고 있다.** `DISCORD_WEBHOOK_URL` 을 빈 값으로 두고 배포했다.
> 테스트 업로드가 팀 채널에 쏟아지지 않게 한 것이고, 값을 채워 재배포하면 켜진다
> (`discord.ts:78` 게이트는 Vercel 이 production 이라 이미 열려 있다).
> **EC2 에서 Vercel 로 바꿨다** (2026-08-25). 근거는 위 "확정된 설계 결정" 표.
> 없어진 항목: 탄력적 IP, 로컬 빌드 후 전송 + PM2, Nginx + Let's Encrypt.
> `DATABASE_URL` 을 RDS 로 되돌리는 항목도 없어졌다 — 개발도 운영도 같은 Neon 을 쓴다.
>
> **pooled 엔드포인트를 쓰는 이유.** `src/lib/prisma.ts:16` 의 `max: 5` 는 **인스턴스당**
> 5개다. 서버리스는 함수 인스턴스가 여러 개 뜨면 그만큼 곱해진다. Neon 은
> `max_connections` 가 901 이라(2026-08-25 실측) RDS 의 79 보다 여유롭지만,
> 커넥션은 이 리포가 이미 한 번 데인 자리다 (`CLAUDE.md`). 개발용 `db push` 는
> direct 를 그대로 쓴다 — pooled 로는 스키마 변경이 어긋난다.
>
> **배포하는 순간 디스코드 알림이 켜진다.** `discord.ts:78` 의 `NODE_ENV !== 'production'`
> 게이트가 유일한 차단막인데 Vercel 은 production 이다. `notifyUpload` 는 이미
> `api/documents/route.ts:75` 와 `[id]/versions/route.ts:101` 에서 호출된다.
> **M5 의 알림 항목이 여기서 함께 끝난다.** 배포 직후 테스트 업로드가 팀 채널에
> 쏟아지지 않게 `DISCORD_WEBHOOK_URL` 을 잠시 비우거나 개인 채널로 돌려 둘 것.
>
> **S3 CORS 를 빠뜨리면 업로드가 조용히 실패한다.** 파일이 브라우저에서 S3 로 직접
> 가는 구조라 origin 이 안 맞으면 PUT 이 막힌다. 화면에는 이유가 안 나온다.
>
> **되돌릴 조건**: 상업적 용도가 되면 Hobby 티어를 못 쓴다. 그때는 Railway·Render
> 같은 컨테이너 호스팅이나 원래의 EC2 를 다시 본다 (`hymn.pem` 확보가 선행).

---

## 주말 배치

| 시점 | 목표 | 누적 |
|---|---|---|
| 금 밤 (3h) | M1 | 로그인 동작 |
| 토 낮 (6h) | M2 | **공유 가능** |
| 토 밤 (3h) | M3 | 버전 관리 |
| 일 오전 (4h) | M4 | 찾기 편해짐 |
| 일 오후 (2h) | M5 | 미리보기·알림 |
| 일 밤 (1h) | M6 | 배포 완료 (Vercel 로 바꿔 3h → 1h) |

총 21시간. 빠듯하므로 **시간이 밀리면 M2 직후 배포(M6)로 건너뛰고 M3~M5는 다음 주에 이어간다.**
팀이 못 쓰는 완성품보다 팀이 쓰는 미완성품이 낫다.
