# 인수인계 아카이브

끝난 스트림의 일지·판정 기록. **세션 시작 시 읽지 않는다** — `HANDOFF.md` 만 읽으면 된다.
여기 있는 것은 전부 해소됐거나 무관해진 항목이다. 아직 살아 있는 항목은 `HANDOFF.md` 본문에 있다.

> **2026-09-07 에 7개 절이 내려왔다** (권한 경계 스트림을 닫고 백로그가 비면서). 본문에
> 남긴 것은 상시분뿐이다 — 지뢰 · 미룬 리뷰 8건 · 아키텍처 · 미검증 표(살아 있는 행만) ·
> 파이프라인 결함 2번 · 배포 절차 · 이월 미완 2건. **무손실로 검증했다** (원본 1190줄 대비
> 사라진 줄 0, 스크립트 대조). 항목 4 의 "운영 배포 전에 할 일"만 아직 살아 있어 본문에 뒀다.

> **2026-08-28 에 8개 절이 내려왔다** (xlsx 미리보기 스트림을 닫으면서). 옮기기 전에
> 상시 정보 3건은 `HANDOFF.md` 의 "지뢰" 로 올렸다 — Neon 브랜치 만료 · Neon 리전 변경
> 불가 · `s3:GetBucketCORS` 권한 없음. **이동은 무손실로 검증했다** (원본 대비 사라진 줄 0).

---

### 코드 리뷰 결과 (2026-08-22, `690aeeb`~`3948ea5` 4개 커밋) — 수정·검증 완료

돌렸다. 7건 중 **3건이 "고쳤다고 선언한 것을 절반만 고친" 상태**였다.
`./orchestrate.sh review-fixes` 로 묶어서 처리했고 **2026-08-23 파이프라인 통과**
(`npm test` 58개 · `lint` · `build`).

**"코드 완성" 과 "검증됨" 은 다르다** — 셋 다 브라우저까지 갔다
(최종 검증: `npm test` 63개 · `lint` · `build`, 2026-08-23 실측):

| 리뷰 | 코드 | 단위 테스트 | 브라우저 실측 |
|---|---|---|---|
| 1 `?v=1e21` 500 | ✅ | ✅ | ✅ 2026-08-23, 대조 포함 |
| 2 presign 대기 중 취소 | ✅ | ✅ | ✅ 2026-08-23, 대조 포함 |
| 3 404 가 탭을 JSON 으로 | ✅ | ✅ | ✅ 2026-08-23 |

#### 수정본에 대한 2차 리뷰 (2026-08-23, `/code-review high`)

고친 코드 자체는 리뷰를 안 거쳤기에 커밋 전에 한 번 더 돌렸다. 3건 나왔다.

**고친 것 — 배너에 임의 문장을 실을 수 있었다.** 리다이렉트가 문구 자체를
`?error=문서를 찾을 수 없습니다.` 로 싣고 페이지가 그대로 렌더해서, 누구든
`/?error=<원하는 문장>` 링크로 앱의 공식 경고 배너를 흉내낼 수 있었다. React 가
이스케이프하므로 XSS 는 아니지만 내용이 전부 링크 작성자 것이었다. 같은 리포의
`proxy.ts:38` 이 로그인 리다이렉트에서 `url.search = ''` 로 쿼리를 버리는 패턴을
이 변경이 깬 것이다. `src/lib/page-error.ts` 로 코드→문구 매핑을 만들어 닫았다.
객체 리터럴이 아니라 `Map` 인 이유는 `?error=toString` 이 `Object.prototype` 속성을
주워 오기 때문이다 (테스트로 고정).

**미룬 2건** — 아래 "미룬" 표에 합쳤다.

아래는 1차 리뷰 당시 진단 원문이다 (수정 근거로 남겨둔다).

**1. `?v=1e21` 이 여전히 500** (`src/lib/version.ts:12`) — `f7ef0b8` 이 없애려던 그 증상.
`Number.isInteger(Number('1e21')) === true` 라 파서를 통과하고, Prisma 가
`PrismaClientValidationError: Unable to fit value 1e+21 into a 64-bit signed integer` 를
**DB 연결 없이** 던진다 (2026-08-22 실측). 라우트에 try/catch 가 없어 그대로 500.
`versionNo` 가 `Int`(int4)라 `2147483648` 도 같은 계열로 봤는데, 상한(`MAX_VERSION_NO`)이
파서에서 먼저 막으므로 **DB 가 int4 범위를 어떻게 거절하는지는 알 필요가 없어졌다**
(2026-08-23 배너 확인). 이 추정은 해소된 것이 아니라 무관해진 것이다.

> **결정**: 파서를 `/^[1-9]\d*$/` + 상한 `2147483647` 로 좁힌다. 지수(`1e3`)·16진수(`0x10`)·
> 공백(` 5 `) 표기를 전부 거부한다. 사람이 주소창에 칠 버전 번호는 `1`,`2`,`3` 뿐이다.
> `src/lib/version.test.ts:23` 의 `expect(parseVersionParam('1e3')).toBe(1000) // 경계 명시`
> 는 **구멍을 의도된 경계로 문서화해 둔 것**이므로 같이 뒤집는다.

**2. presign 대기 중 취소가 안 먹는다** (`src/components/upload-dialog.tsx:98`) —
`close()` 가 `batch.cancelled` 를 세우고 `inFlight` 의 XHR 을 abort 하지만, 그 순간 워커가
presign fetch 를 await 중이면 이후 `putToS3` 가 **새 XHR 을 만들어 끝까지 전송**한다.
`batch.cancelled` 검사는 PUT 이 끝난 뒤(106행)뿐이다. 80MB 3개를 presign 대기 중 취소하면
240MB 가 백그라운드로 올라가고 전부 고아 객체가 된다. `putToS3` 호출 직전 검사가 필요하다.

> 정정 (2026-08-23): 처음에 "`onStart` 에서 즉시 `xhr.abort()`" 도 함께 적었는데 **그 방식은
> 동작하지 않는다.** WHATWG XHR §3.5.7 의 `abort()` 는 `send() invoked` 플래그가 서 있을 때만
> 동작하고, `send()` 전에는 `open()` 앞이든 뒤든 완전한 no-op 이라 뒤이은 `send()` 가 정상
> 전송된다. 이중 안전장치는 `putToS3` 안 `xhr.send(file)` **직전의 동기 검사**로 넣는다
> (`.pipeline/review-fixes/DESIGN.md` §4.3.1). 호출 사슬 어디에 `await` 가 끼어도 우회되지 않는다.

**3. 404 가 탭을 JSON 으로 바꾼다** (`download/route.ts:27,41`) — `03a530a` 가 401 에 대해
고친 것을 404 는 그대로 갖고 있다. 이 엔드포인트는 목록에서 평범한 `<a href>` 로 열린다
(`src/app/(app)/page.tsx:67,93`). 남이 방금 지운 문서 링크를 누르면 탭 전체가
`{"error":"문서를 찾을 수 없습니다."}` 가 된다.

---

### 실측 방법 (2026-08-26 결정) — 남은 실측을 어떻게 할 것인가

남은 실측(자동 분류 7종 + UI 정비 전부)을 **어떻게** 할지 정했다. 실측 자체는 아직 안 했다.

**1. 파이프라인(`orchestrate.sh`)을 쓰지 않는다.** 셸이 판정할 수가 없다. 파이프라인의
전부는 `run_verify()` 가 `npm test`·`lint`·`build` 를 돌려 **종료 코드로 판정**하는
물리적 게이트인데, e2e 스크립트는 vitest 가 안 집는다. 셋 다 녹색인 채로 **한 번도
안 돌아간 스크립트가 `DONE` 을 받는다** — 위 지뢰의 "타입 검사가 꺼진 채 `DONE` 을 받는
것이 실제로 일어난 사고다"와 같은 자리다. verify 가 진짜로 돌리게 하려면 worktree 에
`.env`·`npm install`·`prisma generate` 에 더해 백그라운드 `npm run dev` 까지 세워야 하고,
그러면 **에이전트가 실 S3 와 dev DB 에 쓴다.** `s3:ListBucket` 이 없어 teardown 을
빠뜨린 고아 객체는 영영 못 찾는다. 비용도 안 맞는다 — 3차 주행이 $17.57 인데
**비싼 절반(design+judge $8.69)은 `DESIGN.md` §4.2 로 이미 끝나 있다.**

> **파이프라인이 맞는 것은 0-1(제안 이름 편집)이다.** 성질이 정반대다 — 진짜 기능이고,
> `DESIGN.md` §6 이 뺐던 근거를 폐기하는 **설계 결정**이 걸려 있어 judge 가 할 일이 있고,
> 유닛 테스트로 덮이는 로직이라 **verify 가 실제로 판정할 수 있다.** 4번째 실측 점도 쌓인다
> (`BUDGET_JUDGE` $5 유지 판단이 아직 3점이다).

**2. e2e 하네스를 먼저 짓지 않는다. 눈으로 먼저 보고, 고칠 대상이 정해진 뒤에 굳힌다.**
지금까지 결함을 실제로 잡아낸 것은 전부 사람 눈이다. 제안 이름 건(`건강기록 와이어프레임
HLT`)이 그 증거다 — **코드는 설계대로였고 `classify.test.ts:37` 도 통과했다.** 어떤
assert 로도 안 잡힌다. "이건 카테고리가 아니라 제목이다"는 보는 사람만 하는 판정이다.
회귀 테스트는 **고칠 대상이 정해진 뒤에** 쓰는 것이 싸다.

**3. §4.2 를 갈라 보면 자동화가 실제로 이득인 것은 한 항목뿐이다.**

| | 항목 | |
|---|---|---|
| 상태(부작용) 검증 | #2 만들지 않음 · #3 셀렉트 수정 · #4 닫기 → 폴더0·문서0 · #6 수동 모드 · #7 별칭 · #8 폴더 0개 | 전부 **DB 대조**다. UI 검증이 아니라 부작용 검증이라 스크립트가 잘한다 |
| **눈이어야 하는 것** | #1 의 "사양 스케치대로 뜨는지" · 제안 이름의 적절성 · 토스트 7곳이 읽히는지·겹치는지 · AlertDialog 문구 · 무채색 전환 | 문자열 매칭으로 판정이 안 된다 |
| **자동화가 이득** | **#5 업로드 직후 취소** | 손으로 하려면 B10 때처럼 **Slow 4G + 소스에 임시 10초 지연**을 넣었다 빼야 한다. `page.route` 로 S3 PUT 을 붙잡으면 그게 없어지고 **임시 코드를 소스에 넣는 일도 사라진다** |

#### 손대기 전에 알아야 할 것 셋

- **`npm run test:e2e` 가 지금 깨져 있을 가능성이 높다.** `document-detail.mjs:38` 의
  `page.on('dialog', (d) => d.accept())` 는 `window.confirm` 전제인데, UI 정비에서
  **파괴적 확인 3곳이 AlertDialog 로 바뀌었다**(`document-row-actions.tsx:59`). B6 이 그
  핸들러에 의존하므로 `AlertDialogAction` 클릭으로 바꿔야 한다. **새 스크립트를 쓰기 전에
  기존 것부터 돌려 볼 것** — M3 검증이 통째로 죽어 있으면 그게 먼저다
- **teardown 에 폴더 삭제가 없다.** `helpers.mjs` 의 `purgeDocument` 는 `documents` ·
  `document_versions` · S3 객체만 지운다. **자동 분류는 폴더를 만드는 기능**이라 실측을
  돌릴 때마다 dev DB 에 제안 폴더가 쌓인다. `purgeFolders` 가 하나 더 필요하다
- **이 PC 에 Playwright 브라우저가 없다.** `~/AppData/Local/ms-playwright/` 가 비어 있다
  (2026-08-31 주: 이 경로는 **Windows 시절 기록**이다. 지금 머신은 macOS 라
  `~/Library/Caches/ms-playwright` 를 보고, 거기엔 설치돼 있다)
  (2026-08-26 확인). `npx playwright install chromium` 이 선행이다

#### 그 셋의 결과 (2026-08-27)

**1. Playwright 설치 완료.** `chromium-1234` 외 3개.

**2. 기존 e2e 는 실제로 죽어 있었다. 고쳤다** (`5f67633`). 3회 돌려 6/9 · 7/9 · 6/9 가
나왔고 원인이 두 갈래였다.

- **B6 은 진짜 파손.** 예상대로 `page.on('dialog')` 전제가 AlertDialog 전환으로 무효가
  됐다. 트리거만 눌리고 확인이 안 눌려 문서가 안 지워졌다. **아이콘과 확인 버튼의
  접근성 이름이 겹친다** — 트리거는 `aria-label` 이 `"제목 휴지통으로 이동"` 이고 확인은
  `"휴지통으로 이동"` 이라, 이름으로만 고르면 트리거를 두 번 누르는 꼴이 된다.
  아이콘은 `aria-label` 접미사로, 확인은 다이얼로그 안에서 골라야 한다
- **B7·B8 은 파손이 아니라 경합이었다** (예상 못 한 것). `count()` 는 기다리지 않는데
  RSC 는 뼈대를 먼저 흘려보내고 본문을 나중에 붙인다. 회차마다 `셸=0`/`셸=2` 로 갈린
  것이 증거다. **2026-08-24 의 "9/9 통과" 는 이 경합에서 이긴 결과였을 수 있다** —
  UI 정비로 초기 청크가 늘면서 타이밍이 밀린 것으로 보인다

**3. `purgeFolders` 를 붙였다** (`9b08e3a`). 이름이 아니라 **생성 시각**으로 고른다 —
자동 분류는 기존 폴더에도 문서를 넣으므로 이름으로 지우면 팀 폴더를 지운다.
문서가 남은 폴더도 건너뛴다. **시간대 함정을 실제로 밟았다** — `folders.created_at` 은
`timestamp without time zone` 이고 값은 UTC 인데 `Date` 를 그대로 넘기면 node-postgres 가
로컬시(KST)로 직렬화해 **9시간 미래와 비교하고 아무것도 안 지운다.**
`toISOString()` + `::timestamp` 로 맞췄다.

### 실측에서 나온 결함 (2026-08-27) — ✅ 해소됨

**파괴적 버튼의 빨강이 죽어 있었다.** `폴더 삭제`·`영구삭제` 두 곳이 코드로는
`bg-danger`(`#c50000`) 인데 화면에는 검정(`rgb(23,23,23)`)으로 나왔다. 되돌릴 수 없는
동작 두 개가 일반 버튼과 구분이 안 됐다.

```
class="... bg-primary text-primary-foreground ... bg-danger text-white ..."
computed background-color: rgb(23, 23, 23)   ← bg-primary 가 이겼다
```

원인은 `alert-dialog.tsx:158-161` 이었다. `<Button variant="default" asChild>` 안에서
Radix Slot 이 두 className 을 **문자열로 이어붙이기만** 하고 tailwind-merge 를 안 거친다.
`bg-primary` 와 `bg-danger` 가 둘 다 남고, 클래스 순서가 아니라 **CSS 생성 순서**가
승자를 정했다.

**고쳤다** (2026-08-27). `className` 을 Action 이 아니라 **Button 에 넘긴다** — Button 이
그것을 `buttonVariants({ variant, size, className })` 에 넣으므로 `cn`(twMerge)이 충돌
클래스를 떨어뜨린다. `bg-primary`·`text-primary-foreground`·`hover:bg-primary/80` 이
클래스 문자열에서 아예 사라지고, `bg-clip-padding` 처럼 그룹이 다른 것은 남는다(실측).
**`AlertDialogCancel` 도 같은 모양이라 같이 고쳤다** — 지금은 `variant="outline"` 이라
증상이 안 났을 뿐 구조가 같다.

**판정은 대조로 했다.** `test/e2e/danger-color.mjs`(`npm run test:e2e:danger`) 가
`getComputedStyle` 로 두 버튼의 배경을 읽는다.

| | 폴더 삭제 | 영구삭제 | 취소(outline) |
|---|---|---|---|
| 고친 뒤 | `rgb(197,0,0)` | `rgb(197,0,0)` | `rgb(255,255,255)` |
| `git stash` 로 되돌림 | `rgb(23,23,23)` | `rgb(23,23,23)` | `rgb(255,255,255)` |

**되돌린 쪽을 실제로 돌려 본 것이 핵심이다** — 안 그러면 하네스가 결함을 잡는 것인지
그냥 통과하는 것인지 구분이 안 된다. 취소 버튼이 양쪽 다 흰색인 것도 같이 봤다
(빨강을 전면 도포한 게 아니라는 대조). `npm test` 224 · `lint` · `build` · 기존 e2e 9/9 도
통과하지만 **이 셋은 이 결함을 원리상 못 잡는다 — 결함이 있을 때도 전부 녹색이었다.**

**"색이 깨져 있었다"(2026-08-26)와 같은 계열이다** — 빌드도 린트도 못 잡는다.
`shadcn add` 로 받은 컴포넌트는 `@theme inline` 뿐 아니라 **`asChild` 의 className 병합도**
대조해야 한다는 것이 이번에 추가된 교훈이다.

> **파이프라인을 쓰지 않았다** (2026-08-27 판단). `run_verify()` 는 `npm test`·`lint`·
> `build` 의 종료 코드로 판정하는데 셋 다 결함이 있는 채로 녹색이다 — **`DONE` 이 그냥
> 나온다.** 설계 결정도 없어 judge 가 할 일이 없다. 위 "실측 방법" 절이 자동 분류 실측을
> 파이프라인에서 뺀 것과 같은 이유다.

> **`window.confirm` 2곳은 결함이 아니다.** 실측 중 눈에 띄어 처음엔 "안 걷힌 자리"로
> 봤는데, `MILESTONES.md` "확정된 설계 결정" 표의 확인·알림 UI 행에 **의도로 적혀 있다**
> (모달 위 모달이 되고, 데이터를 지우는 동작도 아니다). 다만 자동화에는 대가가 있다 —
> Playwright 는 대화상자를 기본값으로 dismiss 하므로 `page.on('dialog')` 가 없으면
> **취소 경로가 조용히 안 돈다.**

> **토스트 겹침은 문제 아님으로 닫았다** (2026-08-27). 2개가 동시에 뜨면 sonner 가
> 뒤엣것을 접지만 사람이 보고 받아들일 만하다고 판단했다.

### 파이프라인 부검 (2026-08-24, `f315622`·`256c567`) — 완료

`./orchestrate.sh` 가 하루에 세 번 죽었는데 **세 번 다 로그에 사인이 한 줄도 안 남았다.**
사람이 매번 `*.stream.jsonl` 을 jq 로 파서 원인을 알아냈다. 그걸 고쳤다.
지시서는 리포 루트의 `PIPELINE-FIX.md` 였고, 작업이 끝나 삭제했다 (내용은 이 절과
커밋 메시지에 남았다). **손으로 고쳤다** — 파이프라인으로 돌리면 실행 중인 셸을
그 실행이 수정하게 된다.

핵심은 하나다. **사인 확보를 exit code 검사보다 앞으로 옮겼다.** claude 가 0 이 아닌
코드로 죽어도 스트림 마지막 result 이벤트에 이유가 들어 있다 — 셸이 그 파일을 손에
쥐고도 숫자 하나만 보고 버리고 있었다.

| 무엇 | 지금 |
|---|---|
| 사인 | `FAIL_LOG.md` 에 `subtype`·`errors`·턴·비용·`terminal_reason` 이 남는다. result 이벤트가 없으면 "사인 확인 불가"를 정직하게 남긴다 |
| 온전한 산출물 | `STATUS: DONE` 이면 `<파일>.crashed` 로 파킹하고 사람에게 묻는다 (`force=1` — `AUTO=1` 에서도 멈춘다). 파킹은 게이트를 띄우기 **전에** 한다 — 제자리에 두면 다음 실행의 재사용 로직이 게이트 없이 되살린다 |
| 증거 | 스트림·result 를 `attempt<N>` 번호로 보존한다. 재시도가 1차 증거를 덮던 것을 막는다 |
| 상한 | 단계별로 갈렸다 (`TURNS_*`/`BUDGET_*`). 기본은 impl 80턴/$8, 나머지 40턴/$5 |
| 런처 계약 | `STATE.md` 에 `## 다음 행동` 블록이 생겼다. 계약이 문서에만 있어 안 지켜졌다 |

**실측 4점** (2026-08-24, `document-detail` 주행). 다음 주행에서 점을 더 쌓을 것:

| 단계 | 턴 | 비용 | 결과 |
|---|---|---|---|
| design | 20 | $4.80 | 통과 (아슬아슬) |
| judge | 28 | $4.72 | 통과 |
| judge (다음 주행) | 34 | $5.08 | **돈 초과사** |
| impl | 41 → 42 | $3.84 → $2.50 | **턴 초과사**(40 상한) → 통과 |

> `BUDGET_JUDGE` 는 **$5 그대로 두기로 했다** (2026-08-24 결정). 실측 2점으로 기본값을
> 올리지 않는다. 이제 초과해도 사인이 남고 산출물이 파킹되므로 되돌릴 수 있다.
> 필요하면 `BUDGET_JUDGE=8 ./orchestrate.sh <feature>` 로 그때그때 넘긴다.

`/code-review high` 가 5건을 잡았고 **전부 고쳤다.** 두 건은 재현까지 확정했다:

- **이전 주행 산출물을 이번 것으로 오인했다.** 파일 존재만 봤다. `FRESH_DESIGN=1` 로
  버리라고 명시한 설계를 셸이 "온전해 보인다"며 되살리라고 내밀었다. 지금은 실행 전
  내용 지문과 비교한다. **mtime(`-nt`)을 안 쓴 이유**: 이 머신의 `/bin/bash` 는 3.2.57 이고
  mtime 을 **초 단위로만** 비교해서, 같은 초에 끝난 단계의 산출물이 전부 오판됐다
- **프리플라이트 게이트를 런처 모드에서 영영 통과할 수 없었다.** 승인 대상이 `STATE.md`
  였는데 `state()` 가 불릴 때마다 `updated:`·`pid:` 를 새로 찍어 해시 마커가 즉시 낡았다.
  승인 후 재실행 3회 전부 exit 4 (수정 전 코드로 대조 확인). 지금은 내용이 환경에만
  의존하는 `PREFLIGHT.md` 를 승인 대상으로 쓴다

나머지 3건: 파킹본이 이전 파킹본을 덮던 것, 크래시 산출물이 `BLOCKED` 여도 막힌 이유가
안 가던 것, 죽은 경로가 모델 교체 감시를 건너뛰던 것.

**테스트 37 → 50** (`bash test/run-tests.sh`, API 0회). 기존 37개 전부 통과 유지.

> **판정 하나를 정정한다.** 리뷰어와 나의 첫 재현 둘 다 "exit 4가 세 번 났다"만 보고
> **어느 게이트인지**를 안 봤다. 수정 후 2차의 exit 4 는 실제로 다른 게이트(설계 검토)였다.
> 수정 전 코드로 대조해서야 원 진단이 맞았음이 확정됐다. **여러 게이트가 같은 exit
> 코드를 쓰는 파이프라인에서 exit 코드만으로 단언하지 말 것** — 테스트도 그래서
> "어디까지 갔는가"(`DESIGN.md` 존재)로 단언한다.

### 파이프라인 3차 주행 실측 (2026-08-26, `auto-classify`)

**한 번에 완주했다** — 재시도 0회, 게이트 승인 2회(JUDGE·DESIGN), 예산 초과 없음.

| 단계 | 턴 | 비용 | 권한 거부 |
|---|---|---|---|
| design | 17 | $4.61 | 0 |
| judge | 33 | $4.08 | 2 |
| impl | 54 | $4.18 | 5 |
| verify | 24 | $4.70 | 1 |

합계 **$17.57**. `BUDGET_*=8`, `TURNS_JUDGE=60 TURNS_VERIFY=50` 으로 돌렸는데
**넷 다 $5 아래였다** — M4 주행(judge $5.27 · verify $5.12)보다 오히려 쌌다.
범위가 넓다고 항상 비싼 것은 아니다. **다만 올려 둔 덕에 죽지 않았는지는 알 수 없다** —
상한에 안 닿았으므로 이 주행은 $5 유지 여부에 대한 증거가 못 된다.

**빌드 게이트를 켜고 돌린 첫 주행이다.** worktree 에 `.env` 복사 + `npm install` +
`prisma generate` 를 미리 해서 프리플라이트를 녹색으로 만들었다. 검증 명령이
`npm test, npm run lint, npm run build` 셋이 됐다.

#### 권한 거부 16 → 8, 그런데 원인이 `.claude/settings.json` 에 있다

기준선(`document-detail` 주행)은 design 1 · impl 5 · judge 7 · verify 3 = **16건**이었다.
이번엔 design 0 · judge 2 · impl 5 · verify 1 = **8건**. 절반이다.

**하지만 남은 8건 중 4건이 `PowerShell(...)` 이다** — `npm run build` 2회(impl),
`npm test` 2회(judge·verify). `.claude/settings.json` 의 allow 는 **전부 `Bash(...)`** 라
같은 명령이라도 도구 이름이 다르면 안 걸린다.

**이것이 "결함 2번(`impl` 의 `npm run build` 권한 거부가 재발한다)"의 진짜 원인이다.**
M4 주행 기록은 "`Bash(npm run build)` 가 있는데도 거부됐다"고 적었는데, 정확히는
**에이전트가 `PowerShell` 도구로 불렀기 때문**이다. 규칙이 안 듣는 게 아니라 다른
도구를 막은 것이다. 윈도우 환경이라 에이전트가 PowerShell 을 자연스럽게 고른다.

고치는 법: allow 에 `PowerShell(npm test:*)` · `PowerShell(npm run build:*)` ·
`PowerShell(npm run lint:*)` 를 나란히 추가한다. **아직 안 했다.**

> 치명적이진 않다 — 판정권은 셸에 있고 `run_verify()` 가 별도로 돌린다. 이번에도
> 거부된 뒤 다른 도구로 재시도해 결국 돌았다. 하지만 **거부 한 건이 턴을 태운다.**

### 파이프라인 4차 주행 실측 (2026-08-27, `suggest-name-edit`)

**완주했다.** 재시도 0회, 게이트 승인 2회(JUDGE·DESIGN), 예산 초과 없음.
`FAIL_LOG.md`·`MODEL_LOG.md` 둘 다 비어 있다 — 모델 교체도 없었다.

| 단계 | 턴 | 비용 | 시간 |
|---|---|---|---|
| design | 13 | $3.80 | 4m |
| judge | 32 | $4.00 | 6m |
| impl | 44 | $3.20 | 6m |
| verify | 19 | $3.03 | 4m |

합계 **$14.03** (3차 $17.57 대비 −20%).

**`BUDGET_*` 기본값($5, impl $8)으로 돌렸고 넷 다 안 닿았다.** 3차 주행은 8로 올려
돌아서 "$5 로 충분한가"의 증거가 못 됐는데, 이번이 **그 4번째 점이다** — 기본값 유지의
첫 실증이다. 범위는 3차와 비슷한데(순수 함수 3개 + 컴포넌트 1개) 더 쌌다.

#### 권한 거부 8 → 1. 0-3 조치는 들었다

3차 주행 8건 중 4건이 `PowerShell(...)` 이었고 0-3 이 그걸 넣었다. 이번엔 **1건**이고,
그 1건은 `PowerShell(npx tsc --noEmit -p tsconfig.json)` 이다 — 0-3 이 넣은 6줄에 없는
명령이다. `Bash(npx tsc:*)` 는 있는데 PowerShell 짝이 없다. **같은 패턴이 하나 더 남아
있었던 것**이고, `npm test`·`lint`·`build` 는 한 건도 안 걸렸다(= 조치가 실제로 들었다).

**단, 그 결과는 조건부다.** `settings.local.json` 은 전역 gitignore 대상이라
**worktree 에 안 따라간다** (아래 "지뢰" 절에 이미 적혀 있던 사실이다). 이번엔 런처가
worktree 로 **손으로 복사해서** 돌렸다. 안 했으면 규칙이 없는 채로 돌아 "8 → 8" 이
나왔을 것이고 그걸 "규칙이 안 듣는다"로 오독했을 것이다. 다음에 정할 것:
(a) 이 복사를 worktree 셋업 절차로 문서화하거나, (b) 규칙을 추적되는
`.claude/settings.json` 으로 옮기거나. **아직 안 정했다.**

### dev 브랜치가 하루 만에 사라졌다 (2026-08-27)

실측을 시작하려는데 앱이 `GET / 500` 으로 죽었다. 사인은
`password authentication failed for user 'neondb_owner'` (`28P01`).

**코드도 worktree 도 아니었다** — 메인 체크아웃의 `.env` 로 직접 붙어도 같은 실패였고,
DNS·TLS 는 정상이라 **호스트까지 도달한 뒤 비밀번호 단계에서 거절**당한 것이었다.
원인은 **8-26 에 만든 Neon `dev` 브랜치가 없어진 것**이다. 새로 만든 뒤 엔드포인트가
`ep-billowing-base-az7yo9mr` → `ep-aged-king-az3kh35r` 로 바뀐 것으로 확정됐다.

**브랜치가 저절로 지워지는 경로는 만료(expiration/TTL) 하나뿐이다.** 만들 때 만료를
붙이면 그 시각에 자동 삭제된다. 흔히 삭제로 오해하는 둘은 삭제가 아니다 —
**compute 자동 정지**(접속하면 깨어난다)와 **비활성 브랜치 아카이빙**(목록에는
계속 보인다). 이 리포에는 Neon 브랜치를 건드리는 스크립트가 없다(확인함).
**다음에 브랜치를 만들 때 만료 옵션을 끌 것.** 원인을 확정하려면 Neon 콘솔의
Operations 이력에서 `delete_branch` 를 본다.

> **함정 하나 더 밟았다.** `.env` 를 고친 뒤에도 같은 에러가 났는데, **고친 쪽은
> 메인 체크아웃이고 dev 서버는 worktree 의 `.env` 를 읽고 있었다.** worktree 로
> 복사하고 나서야 `GET / 200` 이 됐다. `.env` 가 두 벌 존재한다는 것을 잊지 말 것.

**새 dev 브랜치의 상태는 설계 전제와 다르다** (판단검증 #28 이 미확인으로 남긴 항목이
이걸로 풀렸다). 운영에서 Include data 로 갈라 나왔으므로 지금 보이는 것은 팀 운영
데이터의 사본이다 — 폴더 5개(`요구사항정의서`·`화면설계서`·`기능명세서`·
`로그인 회원가입 와이어프레임`·`플레이스 와이어프레임`), **별칭은 전부 비어 있고**,
`IA 구조도` 폴더는 없다. 어제 dev 에서 만든 것들이 브랜치와 같이 사라졌다.

**그 목록에 `로그인 회원가입 와이어프레임`·`플레이스 와이어프레임` 이 있다는 것이
중요하다** — 0-1 이 고치려는 "카테고리가 아니라 문서 제목이 폴더 이름이 되는" 증상이
**운영 DB 에 이미 실물로 생겨 있다.** 고칠 이유가 가정이 아니었다는 뜻이다.
(그 두 폴더의 정리는 이번 범위 밖이다. 손으로 옮기면 된다.)

### 배포 성능 실측 (2026-08-25) — 리전 조치 완료

배포 후 "접속이 느리다"는 보고가 있어 재 봤다. **원인은 코드가 아니라 지리적 배치였다.**
함수 리전을 `icn1`(서울)로 옮겼고, 재측정까지 마쳤다. **남은 것은 콜드 스타트뿐이고
그건 무료 티어의 성질이라 코드로 못 고친다.**

**조치 전** (함수가 `iad1`, 미국 동부)

| 측정 | 값 |
|---|---|
| 프로덕션 `/login` 콜드 | 1680ms |
| 프로덕션 `/login` 웜 | ~315ms (이 페이지는 DB 를 안 쓴다) |
| 한국 → Neon(싱가포르) 쿼리 왕복 | 95ms |
| 한국 → Neon 최초 연결 | 1230ms (컴퓨트 절전에서 깨우는 시간) |
| **함수 실행 리전** | **`iad1` (미국 동부)** |

`x-vercel-id: icn1::iad1::...` — 앞은 엣지(서울), **뒤가 함수가 실제로 돈 리전**이다.
`MILESTONES.md` M6 는 싱가포르로 뒀다고 적혀 있었으나 실제로는 적용되지 않았다.
**대시보드에서 고르고 재배포해도 한 번은 안 먹었다** — 그래서 `vercel.json` 으로 못 박았다.

**조치 후** (함수가 `icn1`, 서울 · `/login` 13회)

| 측정 | 값 |
|---|---|
| DNS+TCP+TLS (네트워크 바닥) | **~42ms** |
| `/login` TTFB 최저 (진짜 웜) | **172ms** |
| `/login` TTFB 평균 | **254ms** |
| `/login` TTFB 최고 | **429ms** |
| **함수 실행 리전** | **`icn1` (서울)** — `x-vercel-id: icn1::icn1::...` |

확인 명령: `curl -sI https://nyangmeong-care-dms.vercel.app/login | grep -i x-vercel-id`
**두 번째 칸**을 본다. 첫 칸은 엣지라 항상 `icn1` 이고 리전 판정 근거가 아니다.

**리전으로 산 것은 60~140ms 인데 같은 페이지의 편차가 260ms 다.** 8회를 2초 안에
몰아쳐도 429ms 가 섞였다 — 트래픽이 없어 요청마다 반쯤 콜드로 뜨는 것으로 보인다(추정).
**"느리다"의 주범은 리전이 아니라 콜드 스타트였다.** 리전 최적화를 1순위로 뒀던 것은
아래 "쿼리 3개" 오판에 기댄 것이었다.

**서울로 정한 이유 — 자원이 갈려 있다.**

사용자와 S3(`ap-northeast-2`)는 서울에, Neon 은 싱가포르에 있다. 어느 쪽을 골라도
하나는 멀다. Seoul↔Singapore 왕복 95ms(실측), 같은 리전 내 DB 왕복 ~2ms(추정) 기준:

| 경로 | `icn1` 서울 | `sin1` 싱가포르 |
|---|---|---|
| 목록 (필터 없음) — DB 1 | 5 + 95 = **100ms** | 95 + 2 = **97ms** |
| 목록 (`?folder=`) — DB 2 | 5 + 190 = **195ms** | 95 + 4 = **99ms** |
| 다운로드 presign — DB 1 | **100ms** | **97ms** |
| **업로드 마무리 — DB 1 + S3 1** | 5 + 95 + 5 = **105ms** | 95 + 2 + 95 = **192ms** |
| `/login` — DB 0 | **~5ms** | **~95ms** |

업로드 마무리가 뒤집히는 이유는 `headObjectSize`(`s3.ts:67`)가 S3 에 **실제 네트워크
호출**을 하기 때문이다. presign 자체는 로컬 서명이라 왕복이 없다 — 그래서 다운로드는
영향이 없다.

> **서울의 대가는 코드가 늘 때 드러난다.** 사용자↔함수 왕복은 요청당 **한 번**이지만
> 함수↔DB 왕복은 **쿼리 깊이만큼** 곱해진다. 순차 DB 쿼리를 하나 더 붙일 때마다
> 서울은 +95ms, 싱가포르는 +2ms 다. **깊이가 3 을 넘으면 `sin1` 로 돌아올 것.**
> 그때는 `vercel.json` 의 `regions` 한 줄만 바꾸면 된다.

**목록 페이지의 DB 왕복 깊이는 3 이 아니라 1~2 다.** (2026-08-25 정정)

이 절은 원래 "목록 페이지는 DB 를 3번 **순차로** 친다"고 적혀 있었고, 그 위에서
"쿼리가 3개라 3배로 붙는다"며 싱가포르를 밀었다. **틀렸다.**

- `layout.tsx:28`(`getFolders`)과 `page.tsx` 의 쿼리는 **다른 세그먼트라 병렬**이다 —
  "By default, layouts and pages are rendered in parallel"
  (`node_modules/next/dist/docs/01-app/01-getting-started/06-fetching-data.md:458`)
- 순차인 것은 같은 컴포넌트 안의 `page.tsx:47`(`activeFolder`) → `page.tsx:50`(`getDocuments`)
  둘뿐이고, 앞의 것은 `?folder=` 가 있을 때만 돈다
- `getSession()`(`session.ts:34`)은 JWT 검증뿐이라 **DB 를 안 친다**

남은 항목:

1. ~~함수 리전~~ — **완료** (2026-08-25, `icn1`). `vercel.json` 에 박혀 있다
2. **`page.tsx:47,50` 을 `Promise.all` 로 병렬화** — 서울에서는 이득이 95ms 로 **커졌다**
   (싱가포르였다면 2ms 라 무의미했다). 다만 `?folder=` 가 있을 때만 효과가 있고,
   위 편차 260ms 에 묻힐 수 있다. **실사용 트래픽이 생겨 웜 비율을 잰 뒤에 판단할 것**
3. **Neon 콜드 스타트** — 무료 티어는 몇 분 유휴면 컴퓨트가 0 으로 내려간다. 위 1230ms
   가 그것이다. 코드로 못 고치고, 팀이 실제로 쓰기 시작하면 줄어든다
4. **Vercel 콜드 스타트** — 1680ms vs 웜 172ms. 서버리스의 구조적 특성이다

> 3·4 는 무료 티어의 성질이지 버그가 아니다. **2 를 하기 전에 웜/콜드 비율을 잴 것** —
> 사용자 7명짜리 사내 도구는 대부분의 시간이 유휴라, 콜드가 지배적이면 2 는 헛수고다.

> **Neon 리전은 바꿀 수 없고, 바꿀 곳도 없다.** Neon 은 서울(`ap-northeast-2`)을
> 지원하지 않는다 — 아시아는 싱가포르(`ap-southeast-1`)와 시드니뿐이고 도쿄도 없다.
> 게다가 **기존 프로젝트의 리전은 변경 불가**라 옮기려면 새 프로젝트 + 마이그레이션이다.
> 지금이 이미 최선의 선택지다 (2026-08-25 Neon 공식 문서 확인).

### M6 채택 타임라인 (2026-08-26 확인)

**배포 → 공유 → 팀원 유입 → 방어선 검증 순서가 지켜졌다.** 전부 2026-08-25 하루 안에 일어났다.
아래는 KST 다 (DB 저장값은 UTC — 바로 아래 지뢰 참조).

| KST | 무엇 | 근거 |
|---|---|---|
| 09:01 | 개발자 본인이 먼저 접속해 확인 | `users` 첫 행 |
| 13:44 | Vercel 배포 | 커밋 `008b7da` |
| 13:41 ~ 15:53 | **팀원 5명 유입** — 배포 직후 개발자가 전원에게 공유했다 | `users` 2~6행 |
| 21:00 | 길드 비멤버 거부 확인 (부계정) | 커밋 `f34a56a` |

**M6 의 완료 기준은 이걸로 충족됐다** — 6명이 각자 로그인했고 그중 5명이 문서를 올렸다.
`documents.created_by` 가 5명으로 갈리는 것이 더 강한 증거다. 로그인 행은 개발 중에도
생기지만 **업로드는 사람이 파일을 고른 것**이다.

> **순서에 관한 기존 서술을 정정한다.** "다음 작업" 절이 "URL 은 확인 전에 이미 팀에
> 배포됐다 — 순서가 뒤집혔다"고 적어 뒀는데, **뒤집힌 것은 맞지만 폭은 같은 날 7시간이다**
> (공유 13:41~15:53 → 검증 21:00). 하루 이상 벌어졌던 것으로 읽힐 여지가 있어 시각을 박아 둔다.

---

> **2026-09-06 에 한 절이 더 내려왔다** (폴더 2뎁스 + 카테고리 폴더 본문 스트림을 닫으면서).
> one-in-one-out — 본문에는 상시분과 최신 스트림만 둔다. 이동은 무손실로 검증했다
> (원본 대비 사라진 줄 0).

### xlsx·html 미리보기 스트림 (2026-08-28) — 완료·배포·운영 실측

**기술 내용은 "다음 작업" 3번에 있다.** 여기는 이 스트림에서 **리포 상태가 어떻게
바뀌었는지**만 적는다.

**커밋 5개, 전부 `main` 에 들어갔다.**

| 커밋 | 내용 |
|---|---|
| `c7eb035` | html 을 iframe 미리보기로 연다 |
| `b14e9b1` | xlsx 착수 조건을 실측하고 ExcelJS 를 들인다 |
| `92ad1f2` | xlsx 를 표로 그린다 (11파일 · +1071 −90) |
| `107e558` | main 머지 → Vercel 배포 |
| `7e3f22c` | 운영 오리진 CORS 를 실측으로 닫는다 |

**브랜치를 전부 정리했다 — 원격에 `main` 하나만 남았다.** 네 개를 지웠고, 전부
`origin/main` 의 조상임을 확인한 뒤였다(잃은 커밋 0). 되살리려면
`git push origin <sha>:refs/heads/<이름>` — SHA 는 main 히스토리에 살아 있다.

| 브랜치 | SHA |
|---|---|
| `feature/suggest-name-edit` | `5769ad5` |
| `feature/inline-preview` | `6febe17` |
| `style/geist-tokens` | `d6cc03a` |
| `feature/xlsx-preview` | `92ad1f2` |

**이 스트림에서 착수 전 기록이 두 번 뒤집혔다.** 둘 다 "다음 작업" 3번에 원문을
인용해 남겼다 — ③(채우기색이 theme 이 아니라 argb 다)과 ⑧(`rowSpan` 압축 버그는
코드리뷰가 잡았고, 팀 파일 10건이 통과한 것은 우연이었다).
**착수 전 조사는 실측이 아니다** — 이 리포에서 반복되는 자리다.

**검증 수치 (2026-08-28).** 유닛 290/290 · lint 0 · build 통과 ·
`npm run test:e2e:preview` 10/10 · 팀 xlsx 10건 전수 렌더(로컬·운영 각각 10/10,
CORS 오류 0 · JS 오류 0 · rowSpan 넘침 0).

### 폴더 2뎁스 + 카테고리 폴더 본문 스트림 (2026-09-01 착수 · 2026-09-06 머지) — 완료

> **2026-09-06 에 내려왔다** (구버전 흐리기·사이드바 스트림을 열면서). 상시분 5건은
> `HANDOFF.md` 본문에 남겼다 — 미완 2건(운영 화면 육안 확인 · S6/B4 미검증) · git 신원
> 배포 차단 · `vercel env pull` 자리표시자 · `app-sidebar.tsx:25` aria-current 중복 ·
> 파이프라인 하네스 재동기화. **이동은 무손실로 검증했다** (사라진 줄 0).

> **⚠ 폴더 2뎁스 + 카테고리 폴더 본문 — main 에 들어갔다** (2026-09-06).
> 두 스트림을 **함께** 올렸다. 2뎁스만 먼저 나가면 팀이 빈 화면을 보기 때문이다 —
> 소급 이동 뒤 `화면설계서` 는 문서 0건이고 16건이 전부 하위에 있는데, 목록 쿼리
> (`search.ts:30`)가 직계 완전일치라 자손을 안 보여줬다. 그 후속이 본문에 자식 폴더를
> 그리는 작업이었다. 사양은 `MILESTONES.md` §'폴더 2뎁스 자동 분류'·§'카테고리 폴더 본문'.
>
> **목록 쿼리는 한 글자도 안 바꿨다.** 자손 합산은 명시적으로 버린 대안이다 — `화면설계서` 를
> 열면 16건이 그대로 나와 "폴더가 목록을 좁힌다"는 2뎁스의 목적이 반쯤 죽는다.
>
> **최종 모양은 카드가 아니라 표 안의 행이다.** 처음엔 별도 카드 격자로 만들었는데
> 2026-09-06 화면을 보고 뒤집었다(`7a418f9`) — 테두리가 둘로 갈리면 한 목록으로 안 읽힌다.
> `DocumentTable` 에 optional `folders` prop 을 얹어 문서 행 위에 폴더 행을 그린다.
> 폴더에는 올린 사람·크기·수정일이 없어 `colSpan` 한 칸을 통째로 링크로 쓴다.
> `/search` 는 `folders` 를 안 넘기므로 무변경이다. **`DESIGN.md`·`JUDGE.md` 는 카드
> 격자를 전제로 쓰였다 — 그 두 문서를 읽을 때 이 뒤집기를 감안할 것.**
> 제목·브레드크럼은 크기를 안 섞는다(전 화면 14px, 현재 위치는 굵기·색으로만 구분).
>
> **검증 범위.** `npm test` 26파일 **404건** · `lint` · `build`(라우트 20 · `ƒ Proxy`) ·
> 브라우저 실측 **37/37**(`npm run test:e2e:children`) · 2뎁스 실측 26/26 · dev 소급 이동
> apply 완료(2026-09-04).
>
> **미확인이던 관계 `_count` 의 `where` 는 런타임에서 적용된다** — 실측했다(카드 숫자
> 3 → 휴지통 → 2 → 복구 → 3). Prisma 7 + `@prisma/adapter-pg` 에서 이 리포가 관계
> `_count` 를 처음 쓴 자리였고 **타입검사·린트·빌드가 원리상 못 잡는** 구간이었다.
> groupBy 폴백은 필요 없다.
>
> **S7 — 운영 소급 이동도 끝났다** (2026-09-06). 운영 DB 는 폴더 20개(루트 4 + 2뎁스 16) ·
> 활성 문서 28건 · **미분류 0건** · 껍데기 폴더 0개다. 적용 전후로 문서 총수가 28로 같아
> **하나도 잃지 않았다.** 롤백 스냅샷은
> `scripts/snapshots/reclassify-2026-09-06T07-33-48-636Z.json`
> (되돌리기: `scripts/reclassify-rollback.mjs <스냅샷> --apply`).
>
> **선행 작업 2개는 UI 가 아니라 SQL 로 했다.** 앱 API 와 의미가 같음을 먼저 확인했다 —
> `PATCH /api/folders/[id]` 는 `prisma.folder.update({ data: { name, aliases } })` 뿐이고
> (`route.ts:44-47`), `DELETE` 는 문서가 있어도 막지 않으며(`route.ts:70`) FK 의 `SetNull` 에만
> 의존한다. `Folder` 모델에는 `updatedAt` 이 없어(`schema.prisma:24-32`) raw SQL 이 놓치는
> 필드가 0 이다. 스냅샷은 `scripts/snapshots/prep-2026-09-06T07-26-13-016Z.json`.
>
> **루트 카테고리는 소급 스크립트가 안 만든다.** `02_IA 구조도` 가 미분류로 남길래
> **루트 폴더 `IA구조도` 를 먼저 만들어** 스크립트가 잡게 했다 — 스크립트를 고치지 않았다.
> `classify` 는 이미 `IA 구조도` 를 제안하고 있었고, `normalizeForMatch`(`classify.ts:36-41`)가
> 문자·숫자 이외를 지우므로 **폴더 이름의 공백 유무는 매칭에 무관하다.**


`로그인_화면설계서` 를 `화면설계서 > 로그인` 으로 넣고, 그 폴더를 열면 자식 폴더가
목록에 나온다. **사양과 단계별 표의 정본은 `MILESTONES.md`** 의 두 절
(§'폴더 2뎁스 자동 분류' · §'카테고리 폴더 본문'). 여기는 리포 상태만 적는다.

**커밋은 `pipeline/folder-children-view` 한 줄기에 다 있다** — 그 브랜치가
`feature/folder-depth2` 를 이미 품고 있어서 머지가 한 번으로 끝났다(fast-forward).

| 커밋 | 내용 |
|---|---|
| `df4056a`·`535d120`·`6472a83` | 2뎁스 자동 분류 + 소급 이동 스크립트 |
| `9264b9c`·`7199ccf`·`7f88b00` | 실측에서 나온 수정 3건 + e2e 26건 |
| `425ebae` | 카테고리 폴더 본문에 자식 폴더 (최초 구현 — 카드 격자) |
| `813bc2a` | 브라우저 실측 자동화 (`test:e2e:children`) |
| `7a418f9` | **카드 격자를 걷고 문서 표의 행으로 바꿨다** (화면 보고 뒤집음) |

> **"검증 통과"와 "동작 확인"은 다르다.** 이 스트림이 그 교훈을 두 번 줬다.
> `npm test` 는 순수 함수만 지난다 — `upload-dialog.tsx` 는 한 줄도 실행되지 않았고,
> 카드·표 렌더도 `vitest.config.mts` 가 `environment: 'node'` · `include: src/**/*.test.ts`
> 라 **수집조차 안 된다.** 관계 `_count` 의 `where` 도 타입·빌드가 원리상 못 잡았다.
> 둘 다 브라우저 실측이 잡아 줬다 — e2e 26 + 37 건이 그래서 있다.

**남은 것은 S7(운영 소급 이동) 과 미검증 S6·B4 뿐이다.** 위 "현재 위치" 참조.

**2026-09-06 에 머지·배포하고 브랜치를 전부 정리했다 — 원격에 `main` 하나만 남았다.**
셋 다 `main` 의 조상임을 확인한 뒤 지웠다(잃은 커밋 0). 되살리려면
`git push origin <sha>:refs/heads/<이름>` — SHA 는 main 히스토리에 살아 있다.
워크트리 세 개(`../dms-folder-depth2` · `../dms-folder-depth2-pipeline-folder-children-view` ·
`../dms-s3-orphan`)도 제거했고, **그 안의 `.pipeline/` 산출물은 지우기 전에 메인 리포의
`.pipeline/` 로 복사해 뒀다**(gitignore 대상이라 커밋에는 안 들어간다).

| 브랜치 | SHA | 비고 |
|---|---|---|
| `feature/folder-depth2` | `e162ab5` | 2뎁스 자동 분류 + 소급 이동 |
| `pipeline/folder-children-view` | `9fa0093` | 위 브랜치를 품고 있었다 — 머지가 한 번으로 끝난 이유 |
| `feature/s3-orphan-cleanup` | `633c7d7` | 이미 main 과 같은 지점이라 내용 없음 |

**배포 확인은 커밋 status 로 한다** (실측: `9fa0093` → `Vercel: success`):
`gh api repos/pro047/Nyangmeong_care_dms/commits/<sha>/status --jq '.state'`

**이 스트림에서 얻은 상시 지식 셋** (다음 스트림도 밟는다):

- **커밋 게이트의 `REVIEW_ACK` 은 워크트리에서 경로가 다르다.** `.git` 이 파일이라
  `touch .git/REVIEW_ACK` 가 실패한다 — 훅이 보는 것은 메인 리포의 공용 gitdir
  (`~/Documents/Nyangmeong_care_dms/.git/REVIEW_ACK`) 하나뿐이다. 훅 거부 메시지가
  절대경로를 찍어 주므로 그것을 그대로 쓴다. 토큰은 **커밋 1회마다 소모**되고,
  `touch` 와 커밋을 한 셸 호출에 묶으면 훅이 전체를 막는다. **훅은 명령만 보는 게 아니라
  문자열을 본다** — 문서에 커밋 명령을 그대로 적어 heredoc 으로 넘기면 그것도 막힌다
  (2026-09-06 실측: 이 문단을 쓰다가 걸렸다)
- **파이프라인 기본 모델이 fable 5.1 이다** (`0ba90b1`). 계정 한도 소진은
  `--fallback-model` 이 못 받아낸다 — API 오류가 아니라 CLI 가 메시지를 찍고 종료하는
  경로라 **스트림에 `result` 이벤트가 아예 없다**(뒤따르는 `jq: parse error` 는 같은
  사건의 증상이다). 셸이 감지해 갈아타지만, 세 모델이 다 소진되면 단계가 그냥 죽는다.
  그때는 `RESUME_FROM=verify` 로 이어 붙이거나 한도가 풀린 뒤 다시 돌린다
- **`e2e` 를 만들면 `package.json` 에 스크립트 줄을 같이 붙인다.** 직전 스트림의
  `test/e2e/folder-depth2.mjs` 가 그 줄 없이 남아 있어서 아무도 안 돌린다

### 구버전 흐리기 + 사이드바 정비 스트림 (2026-09-06) — 완료·배포

> **2026-09-06 에 내려왔다** (목록 버전 열 스트림을 열면서). 상시분 2건은 `HANDOFF.md` 의
> "지뢰" 로 올렸다 — 상태를 다룰 때 걸리는 lint 벽 둘 · 테스트가 무언가를 지키는지 확인하는
> 규칙. 미완(운영 화면 육안 확인)은 본문 "이월" 블록에 이미 있다.
> **이동은 무손실로 검증했다** (사라진 줄 0).

> **⚠ 구버전 흐리기 + 사이드바 정비 — main 에 들어가고 배포됐다** (2026-09-06, 커밋 7개
> `12dc74c`~`9655ad5`). 사양과 결정 근거의 정본은 `MILESTONES.md` §'구버전 문서 흐리게'
> ·§'사이드바 정비'. 여기는 리포 상태만 적는다.
>
> **최신에 배지를 붙였다가 되돌렸다.** 첫 구현(`12dc74c`)은 폴더별 최신 1건에 검은 "최신"
> 배지였는데, 배포하고 보니 **화면 12행 중 11행에 배지가 달렸다.** 규칙상 그럴 수밖에
> 없었다 — 폴더당 최신은 항상 1건씩 있어서 활성 28건이 20그룹에 흩어진 지금 구조에서는
> **71%가 최신**이다. 반대쪽(구버전)은 29%다. 착수 전에 `MILESTONES.md` 에 적힌
> `활성 28 · 폴더 20` 을 나눠보기만 했어도 알 수 있었다 — **산수를 안 했다.**
>
> **표시를 구버전으로 뒤집었다**(`c92feee`). 레퍼런스도 같은 방향이다 — Google Drive 는
> 같은 파일명 업로드를 기본값으로 리비전에 합치고, SharePoint 는 버전 번호 열이 opt-in
> 이며, ISO 9001 문서관리는 최신본이 아니라 **폐기본에** 표시를 남긴다.
> 본문 셀을 `opacity-45` 로 통째로 내린다. **제목 색만 내리는 방식은 한 번 실패했다** —
> `#666666` 이 이 표에서 이미 폴더·올린사람·크기·수정 열의 기본색이라 "흐려졌다"가 아니라
> "제목이 다른 열과 같아졌다"로 읽힌다.
>
> **WCAG AA 대비 미달을 알고 받아들였다** (사람이 결정). 제목 2.92:1 · 메타 열 1.94:1
> (기준 4.5:1). **흐림으로 AA 를 지키면서 구분을 만드는 것은 원리상 불가능하다** —
> 계산과 되돌리는 방법은 `MILESTONES.md` §'남은 약점'. 로그인 필수 사내 7인 도구라
> KWCAG 의무 대상이 아니라는 것이 근거고, **"비활성 컨트롤 예외" 조항은 못 쓴다**
> (구버전 행의 제목도 태그 칩도 링크다).
>
> **사이드바를 둘 손봤다.** 루트 폴더 아코디언(`e5c8175`, 기본 접힘)과 드래그 너비
> 조절(`146e991`, 176~400px, `localStorage`). **이 리포에서 상태를 다룰 때 걸리는 lint
> 벽이 둘 있다** — `useEffect` 안의 `setState`(`react-hooks/set-state-in-effect`)와 렌더
> 중 ref 읽기(`react-hooks/refs`). 우회하지 말고 각각 **렌더 중 상태 조정**(React 문서화
> 패턴)과 **상태로 승격**으로 풀었다. 드래그는 `window` 리스너 대신 `setPointerCapture`
> 라 이펙트가 아예 없다.
>
> **`/code-review` 를 두 번 돌렸고 두 번 다 실제 결함이 나왔다.** 1차(`531d388`) — 아코디언이
> 하위 폴더 생성을 안 보이게 만든 회귀 + 조상 펼침이 1단계·마운트 시점뿐. 2차(`d347eda`) —
> 미분류 그룹 오탐(무관한 문서 둘이 폴더 없이 올라오면 먼저 올린 쪽이 흐려진다)과
> **`MILESTONES.md` "확정된 규칙"에 뒤집힌 결정이 남아 있던 것.** 후자가 더 위험했다 —
> 그 목록이 설계 결정의 정본이라 다음 세션이 `row.folderId ?? ''` 를 근거 있는 결정으로
> 알고 되돌린다. **자기 검토가 두 번 부족했다는 기록으로 남긴다.**
>
> **테스트가 통과하는 것과 테스트가 무언가를 지키는 것은 다르다.** 2차 리뷰가 새 테스트
> 2건 중 1건은 옛 구현에서도 통과한다는 것을 실측으로 잡았다. 고친 뒤 **옛 구현으로
> 되돌려 둘 다 실패하는 것을 확인**했다. 새 테스트를 쓸 때 이 확인을 붙일 것.
>
> **`npm run build` 가 깨져 있었고 원인을 찾아 고쳤다**(`9655ad5`). `vercel env pull` 이
> 만든 `.env.production` 의 값이 전부 리터럴 `[SENSITIVE]` 였다 — dev 는 그 파일을 안 읽어서
> **빌드만 죽었다.** 그 김에 `src/lib/env.ts` 의 스키마를 조였다(순수 모듈
> `env-schema.ts` 로 분리 + 테스트 8건). 원래 11개 중 8개가 `min(1)` 이라 `[SENSITIVE]`(11자)를
> 그냥 통과시키고 있었다 — **가짜 `DATABASE_URL` 로 빌드가 진행될 수 있었다.**
> 재발 방지는 `CLAUDE.md` 함정 절.
>
> **검증.** `npm run build`(라우트 20 · `ƒ Proxy`, 진짜 `.env` 로) · `npx vitest run`
> 28파일 **421건**(이전 404) · `tsc --noEmit` · `lint`. 운영 배포 `● Ready`,
> `/login` 200 · `/` 401. **조인 env 스키마가 운영 값도 통과했다** — CLI 로 되읽을 수 없는
> 운영 환경변수를 간접 검증한 셈이다.
>
> **눈으로 안 본 것.** 운영 화면의 흐림·아코디언·리사이즈를 dev 에서만 확인했다.
> 운영은 응답 코드만 봤다.

## 목록 버전 열 (2026-09-06, `a38be7c`) — 완료

> **⚠ 목록 버전 열 — main 에 들어가고 배포됐다** (2026-09-06, `a38be7c`).
> 사양과 결정 근거의 정본은 `MILESTONES.md` §'목록 버전 열'. 여기는 리포 상태만 적는다.
>
> **이 앱에는 "버전"이 둘이었고 목록이 그걸 안 밝히고 있었다.** `DocumentVersion.versionNo`
> (앱이 센 재업로드 횟수)와 파일명 문자열(`v0.3`)이다. 팀은 `v0.2 → v0.3` 을 재업로드가
> 아니라 별개 문서로 올려서 **활성 28건 중 27건이 `versionNo = 1`** 이고, 제목 아래에 있던
> 기존 표시는 `versionNo > 1` 일 때만 렌더돼 **팀 문서에서 한 번도 안 떴다** — 유일하게 뜨는
> 1건이 테스트 산출물(`dms-preview-test-v2.pdf`)이다.
>
> **제목 아래 `v{versionNo}` 를 걷고 파일명 쪽 하나로 통일했다** (사람이 결정). 둘을 한 행에
> 두면 같은 이름으로 다른 말을 한다. **잃는 것은 재업로드된 문서를 목록에서 알아볼 수단**이고,
> 그 정본은 상세 페이지의 버전 타임라인이다(안 건드렸다). 팀이 재업로드를 실제로 쓰기
> 시작하면 이 결정을 다시 볼 것.
>
> **의존 방향이 반대인 이유가 있다.** 정규식은 `classify.ts` 의 `VERSION_TOKEN` **한 벌**에
> 캡처 그룹만 얹어 공유하는데, 정규식을 새 모듈로 빼는 대신 표시 모듈(`file-version.ts`)이
> 분류 모듈을 import 한다 — `classify.ts` 는 **import 0개를 유지해야** 하고
> (`scripts/reclassify-folders.mjs:16` 이 그 전제로 타입 스트리핑만으로 이 파일을 부른다),
> 두 벌 두면 한쪽 요구로 고칠 때 다른 쪽이 조용히 깨진다.
>
> **확장자 처리가 양쪽으로 틀린다.** 확장자를 뗀 것과 원본을 **둘 다 보고 더 길게 읽은 쪽**을
> 쓴다 — `고객센터_기능명세서_v0_2.xlsx` 는 떼야 `v0.2` 가 되고(안 떼면 뒤가 `.` 이라
> lookahead 가 실패해 `v0` 만 남는다), 확장자 없는 `요구사항정의서_v0.3` 은 떼면 `.3` 이
> 확장자로 오인돼 역시 `v0` 이 된다. **둘 다 null 이 아니라 그럴듯한 오답이라 화면만 봐서는
> 안 잡힌다.** 후자는 테스트가 잡았다.
>
> **검증.** `npx vitest run` 29파일 **431건**(이전 421) · `tsc --noEmit` · `lint` ·
> `npm run build`(라우트 20 · `ƒ Proxy`) · `scripts/reclassify-folders.mjs --dry-run` 정상.
> 새 테스트 10건은 **되돌려서 실패하는 것을 확인**했다(원본만 보면 5건, 확장자 뗀 것만 보면
> 1건). 렌더 실측(dev `/` HTML) 28행의 값 분포가 DB 조회와 한 건도 안 어긋난다.
> 운영 배포 `success`, `/login` 200 · `/` 401.
>
> **눈으로 안 본 것.** 화면을 안 봤다 — dev `/` 의 HTML 문자열까지만 봤고 `w-20` 에
> `v0.2b` 가 잘리는지, 열이 늘어 제목 칸이 좁아지는 정도는 확인 못 했다.
> **사람이 "괜찮다"로 닫았다** (2026-09-06).

---

### 권한 경계 스트림 (2026-09-06 1차 · 2026-09-07 2차) — 완료·배포·운영 실측

> **⚠ 권한 경계 — main 에 들어가고 배포됐다** (2026-09-06 1차 · 2026-09-07 2차, `2ba4330`).
> 사양과 결정 근거의 정본은 `MILESTONES.md` §'삭제 권한'. 여기는 리포 상태만 적는다.
>
> **팀원 7명 전원이 남의 문서를 영구삭제할 수 있었다.** 삭제 라우트 3개(소프트 삭제·복구·
> 영구삭제)가 `getSession()` 하나만 봤고 `createdById` 를 인가에 쓰는 코드가 한 줄도
> 없었다(변경 라우트 13개 전수 확인). 방어선은 AlertDialog 확인 문구뿐이었다.
>
> **올린 사람 + `ADMIN_DISCORD_ID` 계정만으로 좁혔다** (사람이 결정, 선택지 3개 중).
> 소유자는 `Document.createdById`(최초 등록자)다 — `DocumentVersion.uploadedById` 를 쓰면
> **남의 문서에 재업로드하는 순간 소유권이 조용히 넘어간다.**
>
> **`CLAUDE.md` 의 불변식을 연 것이다.** *"접근 제어는 디스코드 길드 멤버십 하나뿐,
> 역할·권한 개념을 추가하지 않는다"* 에 예외 경계를 적었다. **삭제 3종만이다** —
> 조회·다운로드·수정·태그·재업로드·폴더 삭제는 여전히 전원 동등이고, 넓히지 말 것.
>
> **폴더 삭제는 일부러 뺐다.** `Folder` 에 작성자 필드가 없어 스키마 변경이 필요하고,
> 자동 분류가 만든 폴더는 소유자가 "우연히 그때 업로드한 사람"이 된다. 폴더 하드 삭제는
> 여전히 전원 가능이고 하위 폴더를 cascade 로 끌고 간다 — 2뎁스가 선 지금 **루트 하나를
> 지우면 그 아래가 통째로 사라진다.** 되돌릴 UI 는 없다(문서는 미분류로 살아남는다).
>
> **404 가 아니라 403 을 낸다.** 이 리포의 다른 라우트는 조건을 `updateMany` 의 where 에
> 얹고 count 로 판정하는데, 소유자 조건을 거기 얹으면 **"없는 문서"와 "남의 문서"가 둘 다
> 404** 가 된다. 권한 실패는 사용자가 이유를 알아야 행동을 바꿀 수 있다. 대가는 조회 1회
> 추가이고 TOCTOU 창은 **`createdById` 를 쓰는 경로가 문서 생성 한 곳뿐이라 실질적으로
> 닫혀 있다**. 기존 테스트의 *"where 가 id + deletedAt 뿐이어야 한다"* 단언은 그대로 통과한다.
>
> **검증.** `npx vitest run` 30파일 **446건**(이전 431) · `tsc --noEmit` · `lint` ·
> `npm run build`(라우트 20 · `ƒ Proxy`). 새 테스트는 **가드를 지워 403 케이스 2건이
> 실패하는 것을 확인**했다. dev HTTP 실측: 남의 문서 3종 전부 403, **대조군**으로 소유자
> 삭제·복구 200, 관리자가 남의 문서 삭제 200. 렌더 실측: sannabi1 세션은 28행 중 삭제
> 버튼 **5개**(소유 5건과 일치), 관리자 세션은 28개. 휴지통은 sannabi1 0행 · 관리자 13행.
> 실측 전후 DB 가 활성 28 · 휴지통 13 으로 같다.
>
> **⚠ `ADMIN_DISCORD_ID` 를 아직 아무 데도 안 넣었다.** env 파일이 이 세션의 권한 밖이라
> 사람이 넣어야 한다 — 로컬 `.env` 와 **Vercel 대시보드 양쪽**이다. 스키마가 optional 이라
> 빠뜨려도 앱은 정상 기동하고 **관리자 예외만 조용히 안 듣는다.** 값은
> `375871831044915200`(ijinseong2798).
>
> **눈으로 안 본 것.** 삭제 칸이 빈 행에서 표가 어떻게 보이는지는 여전히 화면으로 안 봤다.
> 다만 `/code-review` 가 **액션 열이 `w-12` 고정폭이라 빈 `<td>` 가 레이아웃을 무너뜨리지
> 않는다**는 것을 코드로 확인했다 — 남은 것은 인상의 문제뿐이다.
>
> **1차 커밋**: `e472bdd` feat · `3272873` docs.
>
> **2차 — 새 버전 올리기 금지 + 폴더 삭제 문구** (2026-09-07, `225a3e6` feat · `2ba4330` docs). 1차가 삭제만
> 좁혀 놓으니 기대와 실제가 어긋나는 자리 둘이 남았다. 사양은 `MILESTONES.md` §'삭제 권한'
> 의 2차 절.
>
> **새 버전 올리기가 삭제와 같은 경계로 들어왔다.** 판정 함수가 `canDeleteDocument` →
> **`canManageDocument`** 로 바뀌었다. **presign 이 아니라 versions 라우트에서 막는다** —
> 그 시점엔 S3 객체가 이미 올라가 있지만 클라이언트의 `create` 실패 경로가
> `fireDiscard`(`upload-flow.ts:76`)로 discard 를 쏴서 **고아가 안 남는다**(코드 확인 후 결정).
>
> **폴더 삭제 확인이 사라지는 하위 폴더 개수를 말한다.** 예전 문구는 0개일 때도 "하위 폴더도
> 함께 삭제되고"라고 해서 9개 사라지는 경우와 구분이 안 됐다. **직계가 아니라 자손 전체**를
> 센다 — cascade 가 재귀적이라 직계만 세면 실제보다 적게 말한다. DB 왕복은 0회다.
>
> **검증.** `npx vitest run` 30파일 **454건**(이전 448) · `tsc --noEmit` · `lint`.
> versions 가드를 지우면 403 케이스가 실패하는 것을 확인했다. HTTP 실측: 남의 문서 새 버전
> **403**, 대조군은 토큰 검증까지 가서 400. 렌더 실측: 남의 문서 상세에 **새 버전 0 · 휴지통 0**
> (다운로드는 남는다). **폴더 다이얼로그는 사람이 스크린샷으로 확인했다** — `화면설계서`
> 하위 9개가 DB 값과 일치.
>
> **열려 있는 위험 하나.** 남의 문서를 개선하려다 막힌 사람이 **새 문서로 올리면**
> `MILESTONES.md` §'진짜 원인은 안 건드렸다'의 병이 오히려 심해진다. 팀이 쓰는 것을 보고 판단할 것.
>
> **리뷰.** `/code-review high` 는 1차·2차 **둘 다** 돌았고(1차 3건 · 2차 2건 수정, 전부 낮음),
> `/security-review` 는 1차만 돌았다 — **판정은 통과**(새로 만든 취약점 없음). 2차는 같은 가드를
> 라우트 하나에 더 붙인 것이라 다시 돌리지 않았다.
>
> **운영 배포 완료 · 실측 통과** (2026-09-07, `2ba4330`). 배포 `success`, 비로그인 `/login` 200 ·
> `/` 401. **`ADMIN_DISCORD_ID` 는 Vercel 대시보드에 들어가 있고 실제로 먹는다** — 사람이 넣었다.
>
> | 운영 세션 | 목록 | 삭제 버튼 | 휴지통 |
> |---|---|---|---|
> | 관리자(ijinseong2798) | 28행 | **28** | — |
> | 대조군(sannabi1) | 28행 | **5** | **0행** |
>
> **대조군이 결정적이다.** 28/28 하나만 보면 *"관리자 예외가 먹었다"* 와 *"게이팅이 아예 배포가
> 안 됐다"* 를 구분할 수 없다. 같은 화면에서 5로 갈리므로 둘이 동시에 확정된다.
>
> **팀에 아직 안 알렸다.** 오늘부터 남이 올린 문서의 휴지통·새 버전 버튼이 사라진다 —
> 팀원이 물어오기 전에 알릴 것.
---

### 마일스톤 M0~M6 종료 · Geist 토큰 · xlsx 미리보기 배포 (2026-08-28)

> **M0~M6 이 전부 닫혔다** (2026-08-28, `3541acf` 머지·푸시). 마지막까지 남아 있던
> M5 미리보기가 브라우저 실측을 통과했다. **계획된 마일스톤이 더 없다** — 이후 작업은
> 아래 "다음 작업"의 백로그에서 고른다.
>
> 같은 머지에 **Geist 토큰 정렬**(`style/geist-tokens`)도 함께 들어갔다. 화면 전체의
> 모서리·글자 크기·굵기가 바뀐다 — 코드가 아니라 **팀이 보는 인상**이 바뀐 변경이라
> 별도 브랜치로 만들어 되돌릴 수 있게 뒀다. 근거와 값은 `MILESTONES.md`
> "확정된 설계 결정"의 `글자·모서리 토큰` 행.
>
> **그 뒤 html·xlsx 미리보기가 끝나 main 에 들어가고 배포됐다** (2026-08-28,
> `107e558`). 활성 문서 28건 중 미리보기가 되는 것이 3건(전부 테스트 산출물) →
> **28건 전부**가 됐다. **운영에서도 실측했다** — xlsx 10건 전수 정상, CORS 오류 0.
> 상세는 아래 "다음 작업" 3번.
>
---

### `s3-orphan-cleanup` 착수 기록 (2026-08-30)

> **착수한 스트림: `s3-orphan-cleanup`** (2026-08-30). 범위는 아래 **"다음 작업" 4번**
> — 업로드 취소·소프트 삭제 경로의 S3 고아 객체와 `keyToken` 재사용 구멍 두 곳이다.
> worktree `../dms-s3-orphan`(`feature/s3-orphan-cleanup`)에서 파이프라인으로 돈다.
---

### 함수 리전 · UI 정비 · 자동 분류 · 제안 이름 편집 (2026-08-25 ~ 08-28)

**함수 리전을 `icn1`(서울)로 옮겼다** (2026-08-25, `vercel.json` 에 고정). 배포 직후에는
`iad1`(미국 동부)에서 돌고 있었다. `/login` TTFB 가 웜 315ms → 172ms 로 떨어졌지만
**같은 페이지의 편차가 260ms 라 리전 이득이 콜드 스타트에 묻힌다** — `HANDOFF-ARCHIVE.md` 의 "배포 성능 실측" 절.

**UI 정비를 한 번 했다** (2026-08-26, 마일스톤 밖). shadcn/ui 를 들이고 브라우저 기본
다이얼로그를 걷었다 — `window.alert` 7곳 → sonner 토스트, 파괴적 확인 3곳 → AlertDialog,
`window.prompt` 2곳 → Dialog 폼. 색 토큰을 무채색으로 바꾸고 Geist 를 폴백 체인으로 붙였고,
업로드 다이얼로그에 폴더 셀렉트를 넣었다. **`MILESTONES.md` 의 확정된 설계 결정 3건이
여기서 바뀌었다** — 컴포넌트("라이브러리 없이 직접 작성" → shadcn/ui) · 시각 언어 ·
확인·알림 UI. 근거는 그 표와 `MILESTONES.md` 의 "UI 정비" 절.
**색이 깨져 있었고 고쳤다** (2026-08-26). `@theme` 과 shadcn 의 `@theme inline` 이
`--color-border`·`--color-accent` 를 서로 다른 뜻으로 정의해 테두리는 순환 참조로 무효가
되고 주 버튼은 흰 배경에 흰 글씨가 됐다. **배포본에도 나가 있었다.** 근거와 고친 방법은
`MILESTONES.md` "UI 정비" 절, 재발 방지는 아래 "지뢰" 절.
**동작도 확인했다** (2026-08-27, 8종). 토스트 3경로 · Dialog 폼 2곳 · AlertDialog 3곳이
전부 뜨고 동작한다. 여기서 **파괴적 버튼의 빨강이 죽어 있는 것**이 나왔고
**같은 날 고쳤다** (0-2) — `HANDOFF-ARCHIVE.md` 의 "실측에서 나온 결함" 절에 대조표가 있다.

**업로드 자동 분류를 구현했다** (2026-08-26, 파이프라인 `auto-classify` 3차 주행,
브랜치 `feature/auto-classify` 커밋 `a32259c`). 파일명으로 폴더를 정하고 올라가기 전에
미리보기로 확인받는다. `npm test` 169 → **224**, `lint`·`build` 통과.
**이미 `main` 에 머지됐고 `origin/main` 까지 푸시됐다** — 즉 **브라우저 실측이 절반만 된
채로 운영에 나가 있다.** 사양·설계 결정은 `MILESTONES.md` "업로드 자동 분류" 절,
실측 항목 9종은 그 worktree 의 `.pipeline/auto-classify/DESIGN.md` §4.2.
실측 진행분은 `MILESTONES.md` "브라우저 실측" 절, 방법은 `HANDOFF-ARCHIVE.md` 의 "실측 방법" 절.

**브라우저 실측 9종이 끝났다** (2026-08-27). 전항목 통과. 스크린샷은
`test/e2e/shots/AC*.png`·`UI*.png`(gitignore). **아직 안 본 것은 맥 실기 하나뿐이다** —
#9 는 Windows 에서 NFD 파일명을 만들어 올린 프록시라 분류 로직은 실증됐지만
"맥에서 올린 실제 파일"은 아니다. 결과표와 방법은 `MILESTONES.md` 자동 분류 절.

> **이 문단은 원래 "머지도 안 했다"고 적혀 있었다** (2026-08-26 정정). `main` ·
> `feature/auto-classify` · `origin/main` 이 전부 `c318dee` 로 같다. 실측 전에 머지하지
> 않는다는 전제가 이미 깨져 있었던 것이고, **`?error=` 방어선 때와 같은 패턴이다**
> (확인보다 배포가 먼저 나갔다). 사고는 아직 안 났지만 순서가 또 뒤집혔다는 것은 기록해 둔다.

**개발 DB 와 운영 DB 를 나눴다** (2026-08-26). 같은 Neon 프로젝트의 다른 브랜치다 —
운영 `production`(Vercel), 개발 `dev`(로컬 `.env`). **`db push` 가 팀이 쓰는 DB 로
직행하던 것이 계기다.** 절차와 대가(스키마를 두 번 민다)는 `SETUP.md` 1-2 절.

> **M5 가 닫혔다** (2026-08-28). PDF·이미지 인라인 미리보기가 브라우저 실측을 통과했다.
> 실측에서 결함 둘이 나왔고 둘 다 고쳤다 — ① 재업로드한 버전이 새로고침 없이는 미리보기에
> 반영되지 않았다. `router.refresh()` 는 정상 호출됐지만 미리보기 주소가 `?inline=1` 하나뿐이라
> v1 과 v2 가 같은 문자열이었고, `src` 가 안 바뀌니 브라우저가 캐시된 문서를 계속 보여줬다.
> **설계가 적어 둔 "?v 를 안 붙이면 최신을 따라간다"는 새 요청이 일어날 때만 참이었다.**
> `versionNo` 를 주소에 박아 해결. ② 제목이 v1 파일명인 채로 남았다 — 재업로드 라우트가
> `Document.title` 을 아예 안 건드리고 있었다. **자동 생성된 제목일 때만** 새 파일명을
> 따라가게 했다(손으로 고친 제목은 덮지 않는다). 회귀 테스트는 `npm run test:e2e:trash`
> 와 유닛 테스트. 같은 세션에 휴지통 행 버튼 결함(아래 표)도 원인을 찾아 닫았다.

**브라우저 실측은 끝났고, 거기서 나온 결함(0-2 빨강)도 고쳤다** (2026-08-27).
0-3(PowerShell 권한)도 같은 날 끝났다.
**0-1(제안 이름 편집)도 끝났다** (2026-08-28, 아래 블록). **다음은 M5(미리보기)다.**

> **0-1 완료 — 브라우저 실측 11/11 통과 후 `main` 머지** (2026-08-28).
> 파이프라인 4차 주행(`suggest-name-edit`)이 2026-08-27 에 완주해 `8e3ef4b` 가
> origin 에 올라가 있었고, 실측에서 나온 결함 1건을 `5769ad5` 로 고친 뒤 넣었다.
> **이번에는 순서를 지켰다** — 자동 분류 때는 실측 전에 머지가 나갔었다.
>
> **실측에서 결함 1건이 나왔고 같은 날 고쳤다.** 이름을 고치면 셀렉트 라벨은
> `새 폴더 '건강기록 와이어프레임'`(최초 제안) 인데 입력칸은 `와이어프레임`(고친 값)
> 이라 **한 행이 두 이름을 말했다.** 실제로 만들어지는 것은 입력칸 쪽이다.
> 라벨이 `result.proposedName`(담는 순간 고정)을 그리고 있었던 것이고,
> `dest.name` 을 보도록 바꿨다 (`upload-dialog.tsx:411`).
> **자동화로는 안 잡혔다** — "두 라벨이 다르면 실패"라는 assert 를 미리 쓰려면
> 이미 이 결함을 알아야 한다. 대조 스크린샷은 `test/e2e/shots/SNE-label-{before,after}.png`
> (gitignore 라 이 맥에만 있다).
>
> **자동 회귀망이 0에서 11로 올라갔다.** 판단검증 #29 가 짚었듯 기존 e2e 는
> `upload-dialog.tsx` 를 지나지 않아 `npm test` 240개는 순수 함수 쪽만 말했다.
> `npm run test:e2e:sne`(`test/e2e/suggest-name-edit.mjs`)가 그 구멍을 덮는다.
> **원본 `VERIFY.md` §5 는 복구할 수 없어 재구성했다** — 아래 "지뢰" 의
> `.pipeline/` 항목을 볼 것.
>
> 실측 결과 (2026-08-28, dev 브랜치 · 1440×780):
>
> | | 결과 |
> |---|---|
> | 제안 이름 편집 → 그 이름으로 폴더 생성 | 통과 |
> | 기존 폴더명으로 고치면 흡수 (중복 폴더 없음) | 통과 — `화면설계서` 1개 유지 |
> | 끝의 영문 코드 제거 | `08_점검항목_HLT` → `점검항목` |
> | 고친 행을 "만들지 않음"이 안 덮음 | 통과 |
> | 빈 이름 → 시작 잠김 | 통과 |
> | 101자 입력 | **`maxLength` 가 100자로 자른다** — `folderNameError` 의 101자 분기는 화면에서 도달 불가 (그 분기는 `folder.test.ts:105` 가 덮는다) |
> | 첫 화면 판단 대상 | **3.7행** (기준선 2.4행, 스크롤 넘침 상태에서 측정) |
>
> **첫 화면 배분은 실제로 개선됐다.** 처음엔 2.5행으로 쟀는데 그건 **무효한 측정**이었다 —
> 파일 1건이면 `flex-1` 스크롤러가 뷰포트가 아니라 내용 크기로 줄어 "몇 행 들어가는가"가
> 아닌 값이 나온다. 8건을 담아 스크롤을 넘긴 뒤 재니 3.7행이다.
>
> **미검증으로 남긴 것 셋** — 아래 미검증 표에도 넣었다: 별칭 경로 흡수(dev 의 별칭이
> 전부 비어 있다) · 제안 이름의 **적절성**(문자열로 판정 불가) · 목적지를 미분류로
> 바꿨다 되돌리면 편집값이 `proposedName` 으로 리셋되는 것(`upload-dialog.tsx:337`,
> 수용 여부 미정).
---

### 개발 DB Neon 전환과 초기 브라우저 실측 목록 (2026-08-23 ~ 08-26)

> **개발 DB 가 RDS 가 아니라 Neon 이다** (2026-08-25 전환). 이 PC 에 `hymn.pem` 이 없어
> SSH 터널을 못 열었고, `SETUP.md` 가 대안으로 적어 둔 Neon 무료 티어로 갈아탔다.
> `DATABASE_URL` 한 줄만 바뀌었고 `uselibpqcompat=true` 는 뺐다 — 호스트가 실제 도메인이라
> `verify-full` 이 정상 통과한다(실측). **팀 RDS 데이터는 안 보인다.** 아래 M4 실측은
> 전부 빈 Neon DB 에 직접 만든 데이터 위에서 이뤄졌다. **RDS 로 되돌리지 않는다** —
> 배포처를 Vercel 로 바꾸면서 개발도 운영도 같은 Neon 을 쓰기로 했다 (아래 "다음 작업").

실제로 돌려서 확인된 것 (전부 브라우저 실측):

- 디스코드 로그인 → 빈 목록 화면 (`users` 테이블에 행 생성 확인)
- 파일 업로드 → S3 객체와 DB 행 일치 → 다운로드
- 소프트 삭제 → 휴지통 표시 → 복구 → 목록 복귀
- 삭제 상태에서 다운로드 URL 직접 접근이 404, 복구 후에는 파일이 내려옴 (대조 확인)
- **presign 대기 중 업로드 취소 → S3 요청 0건** (2026-08-23, 대조 확인). presign 라우트에
  10초 지연을 임시로 넣어 취소 창을 만들고, 진행바 0% 에서 닫으니 DevTools Network 의 `s3`
  필터에 **preflight(OPTIONS)조차 안 떴다** — `send()` 가 아예 안 불렸다는 뜻이다.
  같은 지연에서 취소하지 않으면 정상 업로드된다(대조). 임시 지연 줄은 삭제했다.
- **잘못된 `?v=` 가 배너로 돌아온다** (2026-08-23, 대조 확인). 같은 문서 id 로
  `?v=1e21` → 배너, `?v=2147483648` → 배너, `?v=1` → 파일이 내려옴. 셋을 대조했으므로
  "id 가 원래 안 열린다"가 아니라 **파서가 걸렀다**는 것이 확정된다.
  셋 다 목록 위 배너로 떴다 — 탭이 JSON 텍스트로 바뀌지 않는다.
- **남이 지운 문서 링크 클릭 → 배너** (2026-08-23). 탭1 삭제 → 탭2 목록에서 제목 클릭.
  위 `?v=` 경로는 DB 조회 **전**, 이쪽은 조회 **후** 로 분기가 다른데 둘 다 같은 배너다.
- **`/?error=<임의 문장>` 으로는 배너가 뜨지 않는다** (2026-08-23, 대조 확인).
  `?error=notfound` 는 배너, `?error=보안 점검으로 재인증이 필요합니다` 는 아무것도 안 뜬다.
- **길드 비멤버는 거부된다** (2026-08-25, 부계정 실측). 길드에 없는 디스코드 부계정으로
  OAuth 로그인을 끝까지 진행하니 `/login?error=팀%20디스코드%20서버%20멤버만%20이용할%20수%20있습니다.`
  로 튕겼다. **판정 근거는 화면 문구가 아니라 주소창의 `?error=` 다** — 같은 문장이
  로그인 화면 하단(`login/page.tsx:44`)에 **상시 안내문으로도 떠 있어서** 눈으로는 구분이
  안 된다. 그 문구를 `?error=` 로 실어 보내는 코드 경로는 `callback/route.ts:24` 하나뿐이고,
  그 줄은 `isGuildMember` 가 false 일 때만 도달한다. **M1 부터 미검증으로 끌려온 항목이었다.**
  안 한 것: 길드 가입 후 같은 계정이 통과하는지(대조), `users` 행이 안 생겼는지(DB 확인)
- **팀원 6명이 각자 로그인했고 5명이 문서를 올렸다** (2026-08-26 DB 확인). **M6 의 완료
  기준이 이걸로 충족됐다.** `users` 6행이 전부 서로 다른 실제 디스코드 계정이고,
  `documents.created_by` 가 5명으로 갈린다. **로그인 행만으로는 개발 중 생긴 것과 구분이
  안 되므로 업로드 쪽을 근거로 삼았다** — 파일을 고르는 건 사람이 한 것이다.
  결정적인 건 분포다: **4명이 08-24 20:22~20:39 의 17분 안에 몰려 있다.**
- **다중 파일 동시 업로드** (2026-08-26). 3개를 한 번에 끌어다 놓아 확인했다.
  M2 부터 미검증으로 끌려온 항목이었다.
- **재업로드 진행 중 취소 (B10)** (2026-08-26). e2e 가 안 덮는 타이밍 의존 항목이라
  수동으로 했다. **취소 창은 두 가지를 겹쳐 만들었다** — DevTools 의 Slow 4G 스로틀링 +
  업로드 지연을 10초로 늘린 임시 코드. 둘 중 하나만으로는 창이 안 열린다.
  M3 부터 미검증으로 끌려온 항목이었다.
- **디스코드 임베드 링크 (B11)** (2026-08-26). `DISCORD_WEBHOOK_URL` 을 채워 재배포했고
  **지금도 업로드할 때마다 알림이 온다.** `discord.ts:78` 의 `NODE_ENV !== 'production'`
  게이트가 Vercel 에서 열려 있다는 것이 이걸로 확정됐다 — **M5 의 알림 항목이 여기서 끝났다.**
- **M3 상세 페이지 9항목 전부 통과** (2026-08-24, Playwright 자동화 · `npm run test:e2e`).
  `DESIGN.md` §7.2 의 B1·B3·B4·B5·B6·B7·B8·B9 + V3. 스크린샷은 `test/e2e/shots/`(gitignore).
  이 중 셋은 **판단검증이 미확인으로 남긴 주장의 첫 실측**이었다:
  - **V3 — 부모 `@updatedAt` 은 중첩 create 로도 갱신된다.** 재업로드 직후 목록 최상단에 왔다.
    설계가 준비해 둔 폴백(`data.updatedAt: new Date()`)은 **필요 없다**. (`JUDGE.md` #27)
  - **`hour12: false` 가 실제로 필요했다.** 화면에 `2026년 8월 24일 21:52` — 12시간제였다면
    `오후 09:52` 였다. (`JUDGE.md` #30)
  - **P2025 → 404 경로가 실동작한다.** 탭에서 상세를 연 채 다른 경로로 삭제한 뒤 재업로드하면
    `문서를 찾을 수 없거나 휴지통에 있습니다.` 가 모달에 뜬다. (`JUDGE.md` #26)
---

### 해소된 미검증 항목 — 미검증 표에서 내림

| 항목 | 방법 |
|---|---|
| ~~토스트·AlertDialog·Dialog 폼 동작~~ | **해소됨** (2026-08-27, 8종). 업로드 폴더 셀렉트가 파일을 담은 뒤 잠기는 것까지 확인 |
| ~~자동 분류 전체 (9종)~~ | **해소됨** (2026-08-27). 맥 실기만 아래 행으로 남았다 |
| ~~휴지통 행 버튼이 세로로 쪼개진다~~ | **해소됨 (2026-08-28).** 원인은 `trash-row-actions.tsx` 가 아니라 **휴지통 페이지 테이블의 액션 열 고정 폭 `w-24`** 였다 — 96px 에서 `px-4` 를 빼면 내용 폭이 64px 뿐인데 텍스트 버튼 두 개를 담고 있었고, 한국어는 단어 경계 없이 줄바꿈되므로 글자당 한 줄로 쌓였다. **아이콘 전용 열(`전체 문서`의 `w-12`)에서 값을 가져온 것으로 보인다** — 그쪽이 멀쩡했던 이유도 같다(아이콘 버튼이라 텍스트가 없다). 폭 고정을 빼고 `whitespace-nowrap` 으로 바꿨다. 회귀 테스트는 `npm run test:e2e:trash` |
---

### §다음 작업 — 끝난 항목들 (0 · 0-1 · 0-2 · 0-3 · 1 · 2 · 3 · 4)

우선순위 순. **M5 만 남았다.** M4 는 2026-08-25 에, M6 은 2026-08-26 에,
브라우저 실측과 0-2(파괴적 버튼 빨강) · 0-3(PowerShell 권한)은 2026-08-27 에,
0-1(제안 이름 편집)은 2026-08-28 에 끝났다. 원래 순서는 M4 → M6 → M5 였다.

> **함수 리전은 끝났다** (2026-08-25, `icn1` 서울 · `vercel.json` 에 고정). 이 목록의
> 1순위였으나 **효과가 예상보다 작았다** — 60~140ms 를 샀는데 콜드 스타트 편차가 260ms 다.
> 근거와 재측정값은 `HANDOFF-ARCHIVE.md` 의 "배포 성능 실측" 절. **성능은 더 파지 말 것** — 팀이 실제로
> 쓰기 시작해 웜/콜드 비율이 잡히기 전까지는 측정 없이 최적화하는 셈이 된다.

> **길드 비멤버 거부는 확인됐다** (2026-08-25 21:00 KST, 부계정 실측 — 위 "현재 위치").
> M1 부터 미검증으로 끌려온 항목이었다. **다만 URL 은 확인 전에 이미 팀에 공유됐다**
> (13:41~15:53 KST) — **같은 날 7시간 차로 순서가 뒤집혔다.** 결과가 통과였으니 사고는
> 안 났다. 다음에 방어선을 검증할 때는 순서를 지킬 것. 타임라인은 `HANDOFF-ARCHIVE.md` 의 "M6 채택 타임라인" 절.

0. ~~**브라우저 실측 — 자동 분류 + UI 정비를 한 번에.**~~ **끝났다 (2026-08-27,
   자동 분류 9종 + UI 정비 8종 전부 통과).** 결과는 위 "현재 위치"와 `MILESTONES.md`,
   실행 중 나온 결함은 `HANDOFF-ARCHIVE.md` 의 "실측에서 나온 결함" 절. 아래는 그때 세운 계획이고 기록으로 남긴다.
   둘 다 같은 화면(업로드 다이얼로그·
   폴더 트리)이라 따로 볼 이유가 없다. **M5 에 손대기 전에 먼저 본다** — M5 가 또 같은
   화면을 건드리므로 깨진 채로 쌓으면 어느 쪽이 깼는지 못 가른다.
   - 자동 분류 9종: `feature/auto-classify` worktree 의 `.pipeline/auto-classify/DESIGN.md` §4.2.
     핵심은 기준선 7건 드롭 → 미리보기 3그룹·근거 줄 / "만들지 않음" 체크 / 미리보기에서
     닫으면 폴더 0·문서 0 / 업로드 직후 취소 시 빈 폴더만 삭제 / 별칭 `와이어프레임` 실증
   - UI 정비: 토스트 7곳 · AlertDialog 3곳 · Dialog 폼 2곳 · 업로드 폴더 셀렉트
   - **`dev` 브랜치를 보므로 마음껏 해도 팀 문서에 안 닿는다** (2026-08-26 분리)
   - **방법은 `HANDOFF-ARCHIVE.md` 의 "실측 방법" 절에 정해 뒀다** — 파이프라인을 쓰지 않고, 하네스를 먼저
     짓지도 않는다. **눈으로 먼저 훑고 고칠 대상이 정해진 뒤에 굳힌다.** 손대기 전에
     읽어야 할 것 셋(기존 e2e 파손 가능성 · 폴더 teardown 부재 · Playwright 브라우저
     미설치)도 그 절에 있다
   - ~~통과하면 `main` 머지 → 푸시 → 배포~~ — **이미 머지·푸시·배포됐다.** 실측은
     운영에 나가 있는 것을 사후에 확인하는 셈이 됐다 (위 "현재 위치" 정정)

0-1. ~~**자동 분류의 제안 폴더 이름을 고친다.**~~ **끝났다 (2026-08-28).**
   파이프라인 4차 주행 `suggest-name-edit` 완주(`8e3ef4b`) → 브라우저 실측 11/11 →
   거기서 나온 라벨 결함 수정(`5769ad5`) → `main` 머지. 결과와 남은 미검증은 위
   "0-1 완료" 블록. 회귀 테스트는 `npm run test:e2e:sne`.
   **아래는 착수 전 사양이고 기록으로 남긴다.**
   **구현이 설계에 더한 것 하나**: `findExistingFolderByName` — 고친 이름이 기존 폴더의
   이름·별칭과 정규화 기준으로 맞으면 그 폴더로 흡수한다. 없으면 정확일치 이름은
   생성이 409 로 튕겨 **미분류로 폴백**(사용자 의도 배반)하고, 정규화만 같은 이름
   (`요구사항 정의서` vs `요구사항정의서`)은 **유사 중복 폴더가 조용히 생긴다.**
   아래는 착수 전에 적어 둔 사양이고 그대로 유효하다.

   원문: 제안이
   카테고리가 아니라 문서 제목으로 나온다 (`건강기록 와이어프레임 HLT`).
   **파일명만으로는 카테고리와 제목을 구분할 신호가 없어 정규식으로는 못 푼다.**
   방향: **미리보기에서 제안 이름을 편집 가능하게** 한다 — `DESIGN.md` §6 이 뺀
   것인데 그 근거("틀린 제안은 만들지 않음이나 미분류로")를 폐기한다. 그건 폴더를
   포기한다는 뜻이라 이 기능이 있는 이유와 어긋난다.
   **"2건 이상 모일 때만 제안"은 검토했고 버렸다** — `plannedFolderNames` 가 배치
   단위로 세므로 같은 종류를 따로 올리면 양쪽 다 미분류가 되고, **새 카테고리의 첫
   파일은 구조적으로 폴더를 못 얻는다.** 1건이어도 분류되는 것이 의도다.
   근거와 표는 `MILESTONES.md` 자동 분류 절의 "실측에서 드러난 것".
   **미리보기 첫 화면 배분도 같이 처리한다** — 이름 편집칸이 행마다 붙으면 한 행이
   지금(100px)보다 커져 이 문제는 나빠진다. 노트북(1440×780)에서 **판단 대상이 2.4개만
   보인다**(측정값). 드롭존 140px 을 한 줄로 접고, 그룹 요약(`7 / 1 / 0`)을 상단 고정하는
   방향. **스크롤 자체는 건드리지 말 것** — 8건이면 1542px 라 어떤 화면에도 안 들어간다.
   숫자와 근거는 같은 절.

0-2. ~~**파괴적 버튼의 빨강을 되살린다.**~~ **끝났다 (2026-08-27).** `className` 을
   Action 이 아니라 **Button 에 넘겨** `cn`(twMerge)이 충돌 클래스를 떨어뜨리게 했고,
   `AlertDialogCancel` 도 같은 구조라 같이 고쳤다. `폴더 삭제`·`영구삭제` 둘 다
   `rgb(197,0,0)` 으로 나온다. **되돌린 상태와 대조해서 판정했다** — 회귀 테스트는
   `npm run test:e2e:danger`. 대조표와 근거는 `HANDOFF-ARCHIVE.md` 의 "실측에서 나온 결함" 절.
   **파이프라인을 쓰지 않았다** — verify 세 게이트가 이 결함을 원리상 못 잡는다(같은 절).

0-3. ~~**`.claude/settings.json` 에 `PowerShell(...)` 규칙 추가.**~~ **끝났다 (2026-08-27).**
   allow 가 전부 `Bash(...)` 라 에이전트가 PowerShell 로 부르면 안 걸렸다 — 3차 주행
   권한 거부 8건 중 4건이 이것이다. 근거는 `HANDOFF-ARCHIVE.md` 의 "파이프라인 3차 주행 실측".

   **`.claude/settings.json` 이 아니라 `.claude/settings.local.json` 에 넣었다.** PowerShell 은
   이 PC 에서만 쓰므로 팀이 공유하는 추적 파일에 넣을 이유가 없고, 넣으면 **커밋하지 않는 한
   워킹 트리가 계속 더러운 채로 남아 다음 `git add` 에 딸려 들어간다.** local 은 전역
   gitignore(`**/.claude/settings.local.json`)로 이미 무시된다.

   `npm test` · `npm run lint` · `npm run build` 를 **정확일치와 접두 와일드카드 둘 다** 넣어
   6줄이다 — 기존 Bash 규칙이 그 쌍 형태라 한쪽만 넣으면 두 도구의 동작이 갈린다.

   **효과는 아직 실측 못 했다.** 규칙이 듣는지는 파이프라인 에이전트가 PowerShell 도구로
   부를 때 드러나므로 **다음 주행에서 권한 거부가 8 → 4 로 떨어지는지**로 확인한다.
   0-1 주행이 그 자리다.

1. ~~**`DISCORD_WEBHOOK_URL` 을 채워 재배포.**~~ **끝났다** (2026-08-26). 지금도 업로드할
   때마다 알림이 온다. **M5 의 알림 항목이 이걸로 닫혔다** — M5 에 남은 것은 미리보기뿐이다.

2. ~~**M5 — 미리보기 + 디스코드 알림**~~ **끝났다 (2026-08-28).** 파이프라인
   `inline-preview` 완주($8.74) → 브라우저 실측 통과 → `main` 머지(`3541acf`).
   **서버 부품은 전부 이미 있었고 없던 건 UI 뿐이었다** — 스키마·패키지·라우트
   시그니처 변경 0. 회귀 테스트는 `preview.test.ts`·`title.test.ts`.

   **실측이 코드 리뷰가 못 잡은 것 둘을 잡았다.** 기록해 둘 값이 있다.
   - **미리보기 주소가 버전과 무관했다.** 설계가 "`?v` 를 안 붙이면 라우트가 최신을
     고르므로 재업로드하면 따라간다"고 적었고 판단검증도 통과시켰는데, **그 말은 새
     요청이 일어날 때만 참**이었다. `router.refresh()` 로 서버 컴포넌트가 다시 그려져도
     `src` 문자열이 같아 브라우저가 캐시된 v1 을 계속 보여줬다. `versionNo` 를 주소에
     박아 해결. 부수 효과로 표에 보이는 버전과 미리보기가 어긋날 수 없게 됐다
   - **제목이 v1 파일명인 채 남았다.** 재업로드 라우트가 `Document.title` 을 아예
     안 건드리고 있었다. **자동 생성된 제목일 때만** 새 파일명을 따라가게 했다 —
     항상 덮는 안은 버렸다(사람이 고친 제목을 지우면 되돌릴 수단이 없다). 이 변경이
     `CLAUDE.md` 불변식에 닿아서 문장을 고치고 예외의 경계를 적었다

3. **xlsx 미리보기** — ✅ **완료 (2026-08-28), 브라우저 실측 통과.**
   `feature/xlsx-preview` 브랜치. html 은 `c7eb035`, xlsx 본체는 그 뒤 커밋이다.
   `npm run test:e2e:preview` 가 10/10 통과하고, 팀 xlsx **10건 전부**를 브라우저로
   열어 값·병합·색이 붙는 것을 확인했다 (JS 오류 0건).

   **① CORS 는 문제가 아니었다 — 우회 라우트를 만들지 않았다.**
   원래 우려는 "교차 오리진 리다이렉트에서 `Origin: null` 이 가서 S3 가 거부한다"였고
   회피책으로 서명 URL 을 JSON 으로 주는 라우트를 두자고 적혀 있었다. **실측 결과
   `fetch('/api/.../download?inline=1&v=N')` 이 그대로 통과한다** (로컬 307 → presigned
   S3 체인, 41897바이트 수신). 대조로 curl 로 `Origin: null` 을 보내면 S3 는 ACAO 를
   안 준다 — 즉 브라우저가 진짜 오리진을 보냈다. same-origin 으로 시작한 fetch 는
   response tainting 이 `basic` 이라 리다이렉트에서 origin 이 강등되지 않는다.
   **운영 오리진도 확인됐다** (2026-08-28, 배포 후 실측) — `nyangmeong-care-dms.vercel.app`
   에서 xlsx 10건을 열어 **CORS 오류 0건 · S3 200 · JS 오류 0건**이었다.
   버킷의 실제 CORS 설정은 **읽을 수 없다** — `dms-app` IAM 에 `s3:GetBucketCORS` 가
   없다(실측). `infra/s3-cors.json` 은 의도일 뿐이므로, 확인 수단은 브라우저로 쏴 보는
   것뿐이다. 오리진을 늘리면 같은 방식으로 다시 볼 것.

   **② `numFmt` 엔진은 만들지 않았다 — 날짜 변환 하나로 끝났다.**
   팀 xlsx 10개 실측: 값 있는 숫자셀 549개 중 날짜 43개(7.8%), 그중 41개가
   `01_요구사항 정의서` 한 파일에 몰려 있다. 쓰이는 서식은 `yyyy-mm-dd` · `m/d/yyyy` ·
   builtin 14 **3종뿐**이고 통화·퍼센트·소수는 **0개**다. ExcelJS 가 시리얼을 이미 JS
   `Date` 로 준다. **표기는 3종을 재현하지 않고 `YYYY-MM-DD` 하나로 통일했다** —
   미리보기에서는 원본과 같아 보이는 것보다 한 가지로 읽히는 편이 낫다는 판단이다.
   **`getUTC*` 로 읽는다** — ExcelJS 의 Date 는 UTC 자정이라 로컬 게터면 KST 에서
   하루 밀린다 (위 "DB 시각 9시간" 과 같은 자리).

   **③ 이 항목은 실측으로 뒤집혔다 — 채우기는 `argb` 가 다수파다.**
   > 원래 이 자리에는 *"진짜 함정은 채우기색이 `argb` 가 아니라 `theme`+`tint` 인 것"*,
   > *"팀 파일 테마 팔레트가 3종으로 갈리므로 하드코딩하면 절반이 틀린 색으로 나온다"*
   > 고 적혀 있었다. **2026-08-28 전수 실측 결과 둘 다 과장이었다.**

   실제 분포 (활성 xlsx 10건 전수):

   | | argb | theme | 그 밖 |
   |---|---|---|---|
   | 채우기 fgColor | **1450 (95%)** | 76 (5%) | — |
   | 글꼴색 | 1067 (31%) | **2433 (69%)** | — |
   | 테두리색 | **6760** | 0 | `indexed:64` 6599 · 색없음 4893 |

   - theme 채우기를 쓰는 파일은 **10건 중 5건**이고, 그 5건은 **전부 Office 2013
     팔레트**(accent1=`5B9BD5`)다. "절반이 틀린 색"이 되는 조합은 현재 데이터에 없다.
   - 실제로 쓰이는 theme 인덱스는 **3개뿐** — `theme0`(=lt1, 흰 배경 20셀),
     `theme1`(=dk1, 검은 글자 2433셀), `theme8`(=accent5 + tint≈0.7999, 연파랑 헤더 56셀).
   - **그래도 `_themes` 파싱은 넣었다.** 새 파일이 2007 팔레트로 `theme8` 을 쓰면
     `4BACC6` 이어야 할 색이 `4472C4` 로 나온다. 파싱은 정규식 12줄이라 값이 싸다.
     대신 **읽기 실패 시 2013 팔레트로 폴백**한다(`DEFAULT_PALETTE`) — 사설 필드라
     ExcelJS 버전이 오르면 사라질 수 있고, 그때 색이 빠지는 건 몰라도 화면이 죽으면 안 된다.
   - **`theme=` 인덱스는 XML 순서와 다르다** — 0=lt1, 1=dk1, 2=lt2, 3=dk2, 4~9=accent1~6.
     앞 두 쌍이 뒤집혀 있다. 실측으로도 확인된다(theme0 셀 20개가 전부 흰 배경).
   - **`dk1`·`lt1` 이 `sysClr` 로 오는 파일이 10건 중 5건이다.** `srgbClr` 만 찾으면
     글꼴색 2433셀이 통째로 날아간다 — `lastClr` 속성도 봐야 한다.
   - `tint` 는 ECMA-376 HLS 식(`tint>0 → L' = L(1-t)+t`). **채도 0 분기를 빼먹으면
     회색·검정·흰색에서 hue 가 NaN 이 되어 색이 통째로 깨진다.**

   **④ 그 밖에 확인한 API 모양.** 병합은 `ws.model.merges` 가 `["C210:C211", …]`
   (한 파일 최대 129개) — 이건 `index.d.ts:995` 에 정식 타입이 있다. `workbook._themes`
   와 `workbook.views[0].activeTab` 은 **타입에 없다**. `ws.rowCount` 는 빈 스타일 행까지
   세므로(표지 시트가 4셀인데 38행) 값 기준으로 실사용 범위를 다시 구한다.
   테두리색의 `indexed:64` 는 "시스템 기본"이라 칠할 색이 없다 — 검정으로 넘기면 표가
   온통 검은 격자가 되므로 앱의 `--color-border-strong` 으로 대체한다.

   **⑤ 크기 가드는 사실상 필요 없었다** (측정 후 결정, 2026-08-28). 실사용 격자
   최대가 **2,279셀**(`04_기능명세서`, 277×8)이다. `MAX_CELLS = 50_000` 상한을 두긴
   했지만 현재 최대의 22배라 발동하지 않는다 — 코드 3줄짜리 안전망이다.

   **⑥ 행 높이가 커 보이는 것은 버그가 아니다.** `01_요구사항 정의서` 의 행 높이가
   원본에 `156pt`·`165pt` 로 들어 있다. 96dpi 환산(×4/3)으로 208px·220px 이 되는데
   렌더 결과와 정확히 일치한다. **원본 재현이 맞다** — 줄이려 들지 말 것.

   **⑦ 첫 화면은 워크북이 저장한 활성 탭이다.** `workbook.views[0].activeTab` 을 쓴다.
   팀 파일 대부분이 `표지`(값 4셀)를 1번 시트로 두고 있어서, 무조건 0번을 열면
   "빈 화면"처럼 보인다. 같은 이유로 **e2e 는 값이 가장 많은 시트로 옮겨 가서**
   병합·색을 단언한다 — 표지에서 단언하면 통과해도 아무것도 증명하지 못한다.

   **버전을 `4.4.0` 으로 고정했다** (caret 제거, 사람 결정). `_themes`·`activeTab` 이
   타입에 없는 사설 필드라 마이너 업데이트에서 조용히 사라지면 타입 검사가 못 잡는다.
   npm 의 `xlsx`(SheetJS CE)는 0.18.5 에 멈춰 있고(SheetJS 가 npm 을 떠났다) 그 뒤
   고쳐진 프로토타입 오염·ReDoS 수정이 없다. `xlsx-js-style` 은 그 0.18 포크라 같은
   문제를 물려받고 2022-05 이후 방치다. ExcelJS 브라우저 dist 는 925KB / gzip 252KB
   (실측)라 동적 import 로 감당된다.

   **구조.** `src/lib/xlsx-view.ts` 는 **ExcelJS 를 import 하지 않는다** — 테마 파싱 ·
   tint · 색 해석 · 열너비 환산 · 날짜 · 병합 인덱스가 전부 평범한 값만 받는 순수 함수라
   xlsx 픽스처도 925KB 번들도 없이 유닛 테스트 33개로 덮인다. ExcelJS 를 만지는 코드는
   `src/components/spreadsheet-preview.tsx` 한 곳뿐이고, 사설 API 가 깨져도 파손이
   거기 갇힌다.

   **⑧ `/code-review high` 에서 나온 것 (2026-08-28). 고친 것 6건.**

   - **[높음] `rowSpan` 이 어긋나 셀이 밀리는 버그.** 처음 구현은 빈 행을 버리고
     `<tr>` 을 압축했는데, span 은 `buildMergeLayout` 이 준 **원본 절대 행번호**로
     계산된다. 병합 범위 안의 행이 하나라도 빠지면 남는 span 이 다음 행을 침범해
     셀이 통째로 오른쪽으로 밀린다. **팀 파일 10건이 통과한 건 우연이다** — 그
     파일들의 병합 셀에 테두리가 있어 행이 안 버려졌을 뿐이다.
     **고침: `1..maxRow` 를 빠짐없이 낸다.** 좌표가 어긋날 여지가 사라지고, Excel 도
     빈 행을 접지 않으므로 더 충실해진다. 회귀 감시는 e2e 스윕의 **"넘침"** 항목 —
     각 `<tr>` 의 `colSpan` 점유 합이 `colgroup` 열 수를 넘는지 센다 (현재 전부 0).
   - **[중간] 크기 게이트가 없었다.** `MAX_CELLS` 는 **파싱이 끝난 뒤에야** 걸려서
     탭이 굳는 것을 못 막는다. 업로드 상한이 100MB(`s3.ts`)라 그런 파일이 들어올 수
     있다. **`MAX_BYTES = 10MB` 를 fetch 앞에 뒀다** — 팀 파일 최대 41KB 의 250배다.
   - **[중간] 글꼴 크기를 pt 가 아니라 px 로 쓰고 있었다.** 행 높이는 pt→px 를 4/3 로
     환산하는데 글꼴만 그대로였다 — 11pt 가 11px(8.25pt)로 그려져 25% 작았다.
   - **[낮음] 숨김 시트가 탭에 나왔다.** `ws.state` 로 거른다.
   - **[낮음] e2e 단언 3건이 헐거웠다.** X3 은 이름표와 다른 것을 검사하고 시트 1개
     파일에서 오탐했다. X7 은 `cells > 0` 이라 항진명제였다 — 탭마다 값 개수가
     **달라지는지**로 바꿨다. 대기 셀렉터가 "표시할 내용이 없습니다" 분기를 안 덮어
     throw 가 나면 집계에 닿지 못하고 결과표 없이 죽었다.
   - **[낮음] lockfile 이 핀을 안 따라갔다.** `package.json` 만 `4.4.0` 이고 lock 은
     `^4.4.0` 이었다. 설치는 안 깨지지만 다음 `npm install` 이 무관한 diff 를 만든다.

   **고치지 않고 기록만 한 것 2건 — 둘 다 실측으로 영향 0.**

   - **병합 블록의 오른쪽·아래 테두리 (리뷰의 추정).** xlsx 가 병합 상자 테두리를
     구성 셀마다 기록하면 앵커만 그리는 지금 방식이 변을 잃는다는 지적. **전수 확인
     결과 병합 310개 중 0개다** — 팀 파일은 앵커가 네 변을 다 갖고 있다. 앵커에
     끝 셀의 테두리를 합치는 코드는 안 넣었다(발생하지 않는 경우를 위한 복잡도).
     다른 도구로 만든 xlsx 가 들어오면 다시 볼 것.
   - **`numFmt` 미구현 (퍼센트·통화·소수).** `15%` 가 `0.15` 로, `₩1,234,567` 이
     `1234567` 로 보이는 자리. **전수 실측: 숫자셀 506개 중 505개가 General 이고
     퍼센트·통화·소수 서식은 0종이다** (소수 값 자체는 1셀). 날짜만 처리하는 현
     설계로 오늘 틀리게 보이는 셀은 없다. 그런 서식이 든 파일이 올라오면 그때 붙인다.

   **⑨ 운영 실측 (2026-08-28, `107e558` 배포 후).** xlsx 10건 전수를 운영 브라우저로
   열었고 **10/10 정상**이다 — 값·색·병합 수치가 로컬과 한 자리도 다르지 않았다
   (예: `01_요구사항 정의서` 값 822 · 색 336 · 병합 22). CORS 오류 0 · JS 오류 0 ·
   rowSpan 넘침 0. ~~운영 DB 가 개발과 같은 Neon 이라 같은 파일을 본 것이다.~~
   **이 근거는 틀렸다** (2026-08-30 실측, 위 "현재 위치" 참조) — 두 DB 는 Neon 브랜치로
   갈려 있고, 그 10건이 **분기(08-25 22:40 이후) 이전** 파일이라 양쪽에 다 있었을 뿐이다.
   결론(10/10 정상)은 그대로 유효하다.

   **남은 것.** 없다. docx 는 여전히 범위 밖이고, `numFmt` 는 위 ⑧ 참고.

4. **S3 고아 객체 정리** — ✅ **완료 (2026-08-30), 브라우저 실측 9/9.**
   `feature/s3-orphan-cleanup`. 신설 `POST /api/uploads/discard` + `keyToken` 재사용 차단
   두 곳 + `@@unique([s3Key])`. **소프트 삭제 경로는 손대지 않았다** — 휴지통 객체는 복구를
   위해 살아 있어야 하고, 실제 삭제 지점인 영구삭제가 이미 담당한다(원문의 "소프트 삭제
   경로" 요구는 그래서 폐기했다).


> **항목 4 의 "⚠ 운영 배포 전에 할 일" 블록은 여기 없다.** 아직 살아 있어
> `HANDOFF.md` 본문 §다음 작업 2번에 남겼다 — 갈라지지 않게 한쪽에만 둔다.


   **실측에서 나온 것 — 하네스가 세 번 가짜 PASS 를 만들었다.** 전부 "통과"로 틀렸다.
   - **없는 객체의 HeadObject 는 404 가 아니라 403 이다** (`s3:ListBucket` 없음). 404 만
     "없음"으로 보면 스크립트가 죽는다. 대조 프로브를 매 회차에 넣었다
   - **"객체가 남았다"는 고아와 정상 문서를 구분하지 못한다.** DB 버전행 확인을 붙이고
     `SKIP`(측정 못 함)을 만들었다 — 없었으면 회차마다 갈리는 결과를 실패로 보고했을 것이다
   - **`upload-dialog.tsx:306` 의 `window.confirm` 을 Playwright 가 자동 거절한다.**
     `close()` 가 첫 줄에서 돌아와 **취소가 아예 안 일어났는데**, 업로드가 아직 안 끝나
     "객체 없음"으로 통과했다. `page.on('dialog', d => d.accept())` 가 필요하다
     (아카이브 "실측 방법"의 그 함정과 같은 자리)

   **CDP 로는 S3 PUT 을 못 조인다** — `Network.emulateNetworkConditions` 의
   `uploadThroughput`·`latency` 둘 다 안 먹어 무효 회차가 3번 나왔다. 취소 창을 여는 방법은
   `page.route` 로 PUT 을 붙잡는 것뿐이다(X3·X8 이 그렇게 한다).

   **`P2002` 의 뜻이 둘이 됐다.** 유일 제약이 두 개라 `versionCreateFailure` 가
   `meta.target` 을 봐야 한다 — s3_key 면 400, documentId+versionNo 면 409. 안 가르면
   재사용 사고가 *"같은 문서에 다른 버전이 동시에 올라왔습니다"* 로 보고돼 원인을 못 찾는다.

   아래는 착수 전 기록이다. — 업로드 취소와 소프트 삭제 경로에 `deleteObject` 연결.
   "휴지통 비우기"는 `MILESTONES.md` 에 없는 기능이므로 범위를 넓히지 말 것.
   **먼저 위 지뢰의 `s3:ListBucket` 항목을 읽을 것** — 사후에 훑어서 지우는 방식은 지금
   권한으로 불가능하고, 만든 쪽이 그 자리에서 지우는 형태로 설계해야 한다.
   `keyToken` 재사용 구멍(`documents/route.ts:37` · `[id]/versions/route.ts:35`)도 이때
   **두 곳을 같이** 막는다 — 정리 배치가 붙는 순간 한 문서를 지우면 다른 문서 파일이 사라진다.

